# Zaikon 正式引継ぎメモ

最終更新: 2026-08-12（JST）

このファイルは、Zaikonアプリ群を別チャット・別担当者・Claude Codeへ引き継ぐ際の最優先資料です。作業開始前に必ず全文を読み、正式リポジトリの `main` 最新版から作業してください。

## 1. 正式環境

- GitHub: `booandmoomoo1011-maker/zaiko-app`
- 正式ブランチ: `main`
- Vercel: `https://zaiko-app-two.vercel.app`
- 2026-08-03時点のスタッフ専用URL機能実装コミット: `e06c94396ed69474e4c74df0bbe27da0ecc5cc3a`
- オーナー／スタッフ認証分離コミット: `3e8023c063ce6e46806a8670753b78a0fa96da21`
- 名簿画面スクロール修正コミット: `67d75a769f479fb070beadaabb9cf9331effaefb`
- 過去のLINEプレビュー作業開始基準: `c70d595accc4fdc80bb1af78d6c5d6e2bc90b1f5`
- 毎日在庫ヘッダー正式アイコン反映コミット: `0ee3e83c16dec404e5790a881d9e56f60ccd3a24`
- 売上入力の携帯インストールボタン修正コミット: `86df33833dcc64e8562b7d52007d17f0ec70defe`
- 全アプリ記録確認の開始コミット: `d5a1dcab09d943d2b9669bd96d85b9255ec5893f`
- 全アプリ自動検査の拡充コミット: `d3ceac16969d1e4b310df9920abdac7f5a2c2f1a`
- 正式構成資料の現行化コミット: `6ac16ff7d4f746c96c86eb078f99b23acc77edda`

古い添付ファイルや過去チャットのコピーを直接デプロイせず、必ずGitHub `main` の現行ファイルを取得して差分を確認すること。

## 2. アプリ構成とURL

| アプリ | 正式URL | 主なファイル |
|---|---|---|
| 私の管理 | `https://zaiko-app-two.vercel.app/my-management.html` | `my-management.html` |
| 毎日在庫 | `https://zaiko-app-two.vercel.app/` | `index.html` |
| 在庫管理 | `https://zaiko-app-two.vercel.app/inventory.html` | `inventory.html` |
| 売上入力 | `https://zaiko-app-two.vercel.app/sales-input.html` | `sales-input.html` |
| 売上オーナー | `https://zaiko-app-two.vercel.app/sales-owner.html` | `sales-owner.html` |
| シフト・スタッフ | `https://zaiko-app-two.vercel.app/shift/` | `shift/index.html` |
| シフト・オーナー | `https://zaiko-app-two.vercel.app/shift/owner.html` | `shift/owner.html` |

### 在庫管理の店舗固定URL

- 管理者（全店舗）: `https://zaiko-app-two.vercel.app/inventory.html?role=admin`
- 大須・編集者: `https://zaiko-app-two.vercel.app/inventory.html?store=osu&role=editor`
- 那古野・編集者: `https://zaiko-app-two.vercel.app/inventory.html?store=nagono&role=editor`
- 鉄板・編集者: `https://zaiko-app-two.vercel.app/inventory.html?store=teppan&role=editor`
- 鎌倉・編集者: `https://zaiko-app-two.vercel.app/inventory.html?store=kamakura&role=editor`

店舗ごとにHTMLを複製せず、4 URLとも正式な `inventory.html` を使用する。固定URLでは該当店舗だけを表示し、端末側にも最後の固定店舗を記憶する。
`role=editor` は暗証番号なしで編集者画面を開く。`role=admin` は管理者パスワード認証後だけ管理機能を表示する。画面上で役割を切り替えない。

### 携帯ホーム画面用の互換URL

- シフト・スタッフ: `https://zaiko-app-two.vercel.app/shift-staff.html`
- シフト・オーナー: `https://zaiko-app-two.vercel.app/shift-owner.html`

重要: 携帯用PWAの `start_url` はルート側の互換HTMLを開く。シフトを更新するときは、次のペアを必ず同じ内容・同じバージョンにすること。

- `shift/index.html` と `shift-staff.html`
- `shift/owner.html` と `shift-owner.html`

片方だけ更新すると、PCでは最新版、携帯アプリでは旧版という不具合が再発する。

## 3. 絶対に守ること

1. アプリを統合しない。使用者が違うため、在庫・売上・シフトの各アプリは別々に維持する。
2. 正式ファイル名、正式URL、互換URLを勝手に変更・削除しない。
3. `zaiko-app/owner.html`（売上オーナー）と `shift/owner.html`（シフト・オーナー）を混同・上書きしない。
4. Firebase設定、保存先、保存キー、LocalStorage、IndexedDB、Cookie、共有データ形式を、明示依頼なしに変更しない。
5. LINE共有URL、固定共有URL、Vercelプロジェクト、GitHubリポジトリを勝手に変更しない。
6. シフト・在庫・売上の既存機能を、今回の依頼範囲外で変更しない。
7. アイコンは正式アイコン一式を使用し、別デザインへ勝手に差し替えない。
8. 公開前に対象ファイルと変更範囲を確認し、無関係なファイルを同時更新しない。
9. バージョンアップ時は、コードだけでなくこの `ZAIKON-HANDOFF.md` も同時に更新する。

## 4. 現在の主要アプリ最新版

- 私の管理: `v1.5`
- 毎日在庫: `v10.64`
- 在庫管理: `v4.8.0`
- 売上入力: `v2.71`
- シフト・スタッフ: `v1.64`
- シフト・オーナー: `v1.71`

### シフト・スタッフの現在の正式機能

- スタッフはオーナーが発行した本人専用URLから開く。店舗と名前はURLに紐づく本人へ固定され、変更できない。
- トークンなしの共通URLは停止済みで、入力・閲覧・保存はできない。
- Firebase匿名認証とRealtime Databaseルールにより、本人の希望・備考・要請だけを読み書きできる。
- 削除・無効化・認証失敗・権限エラー・有効性を確認できないオフライン状態では、画面と入力を直ちに停止する。
- `shift/index.html` と `shift-staff.html` は必ず同一内容・同一バージョンにする。
- v1.54: 入力済みの日に「この日の希望を解除」を追加し、日付ポップアップ右上に「閉じる」を追加した。
- v1.55: カレンダーを左へスワイプで翌月、右へスワイプで前月へ移動できる。日付タップ・縦スクロールとの誤判定を防止している。
- v1.56: カレンダー上部中央に表示中の年月を大きく表示し、スワイプ直後は黄色く変化する。「最終更新」は右側へ小さく表示する。
- v1.57: スタッフ専用URL、匿名認証、名前・店舗固定、共通URL停止、削除時の完全無効化に対応した。
- v1.58: スタッフ専用URLが停止表示になる原因を特定するため、認証段階別の確認番号と「もう一度確認」を追加した。Firebaseルール・既存データ・URLは変更していない。
- v1.59: 「◎」で保存した一日OKをスタッフカレンダー上では `9:00〜23:00` と時間表示に変更した。幅480px以下では7列が画面内に収まるよう、余白・文字・マス・上部年月欄を小さくした。
- v1.60: 元のカレンダーサイズを維持したまま、携帯の2本指による拡大・縮小を許可した。携帯時の曜日を20pxへ拡大し、固定された店舗名・スタッフ名を濃い太字で表示するようにした。
- v1.61: 「全部 ◎ 一日OK」で追加した日にだけ `bulkAllDay:true` を付け、再度ボタンを押すと専用確認ポップアップ後に一括追加分だけ解除できるようにした。個別時間・「×」・備考・既存入力は保護する。日付入力ポップアップの見出しを14pxから21pxへ拡大した。
- v1.62: v1.61以前の「全部」入力には識別印がないため解除できなかった問題を修正した。月の半数以上が一日OKなら従来の一括入力として解除ボタンを表示し、確認後はその月の一日OKをすべて解除する。個別時間・「×」・備考は維持する。
- v1.63: 実際の今日の日付マスに黄色い太枠と「今日」を表示し、カレンダー下に「本日のシフト」を追加した。未確定は「未定」、休み確定は「お休み」、勤務確定は店舗名と時間を表示する。
- v1.64: 7列の携帯幅を維持したまま、カレンダーを淡いピンクのカード調へ変更した。曜日を色付き見出しにし、日付マスへ丸み・薄い影、日曜へ淡いピンク、土曜へ淡い水色を追加した。

### シフト・オーナーの現在の正式機能

- 未提出スタッフが名簿にいても、日付ヘッダーをタップすると日別画面が開く。
- 同じ店舗内では、月表の名前を約0.45秒長押しして上下に並べ替えできる。順番は保存され、別店舗へは移動しない。
- 日別画面の大須・那古野・鉄板すべてに「＋名前を追加」がある。
- 所属店舗以外へ入るスタッフとAM・PM時間を登録できる。
- 同じスタッフの時間が全店舗間で重なった場合、該当スタッフ名の横に赤い `⚠` だけを表示する。文章、点滅、追加ポップアップは出さない。
- 旧来の鉄板配置データとの互換性を維持する。
- 公開済みの応援店舗と時間はスタッフ画面にも表示する。
- Google認証済みの管理者だけがオーナー画面とシフト全体データを利用できる。
- 名簿の各スタッフに「専用URLをコピー」があり、十分長いランダムトークンのURLを発行する。
- 名前削除時はスタッフデータ、全店舗の名簿、トークン対応を一括更新し、古い専用URLを即時無効化する。
- v1.65: 名前登録画面を画面高以内に収め、一覧部分だけを縦スクロール可能にした。右上の閉じるボタンも常時表示する。
- v1.66: 名前登録画面で選択中店舗の全スタッフ名と本人専用URLを、LINEグループ用の一覧としてまとめてコピーできる。
- v1.67: Google登録なしで使える2人分のシフト編集者専用URLを追加した。編集者はシフト作成・編集・コピー・要請・公開を利用できるが、スタッフ登録・削除・スタッフ専用URL発行・全データ消去は画面とRealtime Databaseルールの両方で禁止する。
- v1.68: 店舗全員のスタッフ専用URL一覧を、名前の次の行にURL、その次に空行を入れるLINE向け形式へ変更した。
- v1.69: ブラウザ標準の確認・完了・エラー表示を、黄色・ピンク基調の見やすいアプリ専用ポップアップへ統一した。
- v1.70: 個別のスタッフ専用URLコピーへ本人名を追加し、LINEで別々の吹き出しとして送れる「1人ずつコピーして送る」順番式操作を追加した。
- v1.71: 名簿の各スタッフへ「新しいURL」ボタンを追加し、その人の古いURLだけを停止して新しい名前付きURLを発行・コピーできるようにした。

### 2026-08-01に解決した原因

- 通常URLと携帯用URLの同期時に、携帯側の新しい名前選択式 `v1.52` ではなく古い通常側 `v1.48` を基準にしたため、GitHub上のスタッフ画面が後退した。正しい `v1.52` を復元し、名簿順の即時反映と引っ張って更新を含む `v1.53` として両ファイルを再同期した。

- 日別画面が開かなかった原因は、名簿だけに存在する未提出スタッフに対して、`decided`、`req`、`hold` などを直接参照してJavaScriptエラーになっていたこと。
- 携帯アプリだけ更新されなかった原因は、通常URLの `shift/owner.html` と携帯PWA用の `shift-owner.html` が別ファイルで、片方だけ更新されていたこと。

### 再発防止の自動検査

- GitHub Actions の `.github/workflows/shift-sync-check.yml` で、更新のたびにZaikonアプリを自動検査する。
- 対象は「私の管理」「毎日在庫」「売上入力」「売上オーナー」「シフト・スタッフ」「シフト・オーナー」と `sw.js`。
- 正式HTMLの欠落、OGP・manifestの欠落、OGP URL/画像の重複、シフト通常版と携帯版の不一致、名前選択・名簿同期・引っ張って更新の消失、売上入力の店舗固定の消失、`sw.js` の `no-store` 消失、バージョン番号の後退を検出した場合は検査を失敗にする。
- 在庫管理 `inventory.html` は別作業中のため、ユーザー指示により今回の自動検査対象から除外している。許可なく変更・検査追加しない。

## 5. 自動更新の正式仕様

- `sw.js` はナビゲーション時に `cache: 'no-store'` でネットから最新版HTMLを取得する。
- 全アプリは、閉じて再度開いたときに最新版を取得する。
- 入力中・操作中に突然自動再読み込みはしない。入力消失を防ぐため、この方針を維持する。
- シフト・スタッフとシフト・オーナーには「引っ張って更新」がある。
- 画面上端から下へ引っ張ると「↓ 引っ張って更新」→「離して更新」と表示し、指を離すとキャッシュ回避付きで再読み込みする。
- 表の横スクロール、入力欄、ボタン、モーダル操作では引っ張って更新を開始しない。

## 6. アイコン・LINEプレビュー

- 「私の管理」のヘッダーは黒。
- 6アプリはそれぞれ専用の一覧アイコン、16/32/180/192/512pxアイコンを使用する。
- 7つの正式画面はそれぞれ専用の `og:title`、`og:description`、`og:url`、`og:image` を持つ。
- LINEプレビュー画像は各アプリ専用。すべてが「毎日在庫」画像にならないようにする。
- 送信済みLINEメッセージには古いプレビューが残ることがある。確認時は新しいメッセージとしてURLを送る。
- `inventory.html` の古い埋め込み `apple-touch-icon` と動的manifest上書き処理は復活させない。
- アプリ起動前に大きなアイコン画像が割り込むスプラッシュ表示を再追加しない。

## 7. バージョンアップ時の必須手順

1. GitHub `main` の最新ファイルと最新コミットを取得する。
2. 変更対象と禁止事項をこのファイルで確認する。
3. 依頼された範囲だけ変更する。
4. 画面に表示されるバージョンと、コード内のバージョン定数がある場合は両方を同じ番号へ更新する。
5. シフト画面の場合は通常URL用・携帯用の2ファイルを同期する。
6. HTML/JavaScriptの構文、主要画面、コンソールエラー、画像切れ、横はみ出しを確認する。
7. `main` へ反映し、Vercel本番で実際のURLを開いて確認する。
8. 携帯用URLも必ず開き、通常URLと同じバージョン・機能になっていることを確認する。
9. `sw.js` を変更した場合は、本番の `/sw.js` も確認する。
10. この `ZAIKON-HANDOFF.md` の日付、バージョン、最新機能、注意点、基準コミットを更新する。
11. ユーザーへ、変更内容、確認結果、現在バージョンを簡潔に報告する。

## 8. 本番確認チェックリスト

- [ ] 私の管理から6アプリがそれぞれ正しいURLで開く
- [ ] 各アプリの専用アイコンとOGP画像が読み込める
- [ ] シフト・スタッフの通常URLと携帯用URLが同じバージョン
- [ ] シフト・オーナーの通常URLと携帯用URLが同じバージョン
- [ ] シフト・オーナーの日付タップで日別画面が開く
- [ ] 日別画面の大須・那古野・鉄板に「＋名前を追加」がある
- [ ] 全店舗の時間重複時に名前横へ `⚠` だけが表示される
- [ ] 名前の長押し並べ替えが同一店舗内だけで動く
- [ ] 引っ張って更新がシフト2画面で動く
- [ ] Firebase接続、保存、共有URL、既存データが維持される
- [ ] コンソールにアプリ由来のエラーがない

## 9. 2026-08-01対応済み: 売上入力の店舗固定

対象: `sales-input.html`（インストールボタン対応時 `v2.69`、現在 `v2.71`）

### 対応結果

- 初回だけ店舗選択画面を表示する。
- 店舗名なしの共通URLでも、端末の `last_store` を読み、最後に選んだ店舗を次回から自動表示する。
- `?store=` 付きURLはURL指定店舗を最優先する。
- 戻るボタンを押すと `last_store` を解除して店舗選択画面へ戻り、選び直した店舗を新たに保存する。
- 売上データ、履歴、下書き、自動保存、Firebase、GAS URL、パスワード、日報、共有URL、デザインは変更していない。

### 対応済み仕様

1. 初回だけ店舗選択画面を表示する。
2. ユーザーが選んだ店舗をその端末の `last_store` に保存し、次回以降は店舗名なしの共通URLでも同じ店舗を自動表示する。
3. `?store=大須`、`?store=那古野`、`?store=鉄板`、`?store=鎌倉` がある場合はURL指定を最優先し、その店舗を固定表示する。
4. 戻るボタンまたは設定から別店舗を選んだ場合は、`last_store` を新しい店舗へ更新し、次回から新しい店舗を開く。
5. 売上データ、履歴、下書き、自動保存、Firebase、GAS URL、パスワード、項目表示設定、日報、共有URLを変更しない。
6. 画面デザインは変更しない。
7. `APP_VERSION` と画面表示バージョンを同じ番号へ更新する。
8. 修正後は、共通URLで店舗選択 → アプリを閉じて再度開く → 同じ店舗が自動表示されることを実機相当で確認する。
9. 店舗名付き4 URLもすべて確認する。
10. デプロイ時にこの `ZAIKON-HANDOFF.md` のバージョン・対応結果・基準コミットも更新する。

## 10. 2026-08-01対応済み: 在庫管理の同期保護

対象: `inventory.html`（対応時 `v4.2.1`）

### 対応結果

- 起動時のFirestoreリアルタイム監視で、端末側の保存日時より古いクラウドデータを適用しない。
- 端末側のデータが新しい場合は、古いクラウドデータで画面を上書きせず、端末側の最新版をFirestoreへ保存する。
- 通常操作中のFirestore保存は従来どおり3秒のデバウンスを維持する。
- 画面非表示、ページ離脱、アプリ終了時は、3秒待たずに即時保存を試みる。
- 保存中に追加変更が発生した場合は、保存完了後に最新状態をもう一度保存する。
- Firestoreから受信した最新版はlocalStorageにも保存するが、同じ内容をFirestoreへ再送信しない。
- Firebase設定、Firestore保存先 `inventory/main`、localStorageキー、IndexedDB、既存データ形式、画面デザイン、LINE共有URLは変更していない。

### 確認事項

- JavaScript構文エラーがないこと。
- 画面表示バージョンと `APP_VERSION` がともに `4.2.1` であること。
- 古いFirestoreデータを受信しても新しい端末データを上書きしないこと。
- 通常保存の3秒デバウンスと、終了時の即時保存が両立していること。

## 11. 2026-08-01対応済み: 在庫管理の安全表示と保存状態

対象: `inventory.html`（対応時 `v4.3.0`）

### 対応結果

- 店舗名、商品名、カテゴリー名をHTMLへ表示するときの特殊文字処理を統一した。
- `'`、`"`、`<`、`>`、`&` を含む名前でも、タブ、編集画面、カテゴリー画面、カテゴリー選択が崩れない。
- カテゴリー名をインライン `onclick` へ直接埋め込まず、イベントリスナーで安全に選択する。
- 画面左下に「端末保存済み」「クラウド保存中」「クラウド保存済み」「オフライン・端末保存済み」「クラウド保存失敗」を表示する。
- オンラインへ戻ったときは、待たずにクラウド保存を再試行する。
- Firebase設定、Firestore保存先 `inventory/main`、localStorageキー、IndexedDB、既存データ形式、画面構成、LINE共有URLは変更していない。

### 確認結果

- JavaScript構文エラーなし。
- 特殊文字の変換テストに合格。
- 古いFirestoreデータの上書き防止テストに合格。
- クラウド保存中・保存済み・オフライン表示の切り替えテストに合格。
- `APP_VERSION` と画面表示バージョンはともに `4.3.0`。

## 12. 2026-08-01対応済み: 在庫管理の軽量化と更新表示

対象: `inventory.html`（対応時 `v4.3.1`）

### 対応結果

- ヘッダーに埋め込まれていた1024px・約30KBのJPEGデータを削除した。
- ヘッダーは正式アイコン `/icons/inventory-32.png?v=3` を使用する。
- HTML内の画像形式指定と実データ形式の不一致を解消した。
- 更新通知を実際の動作に合わせて「最新版に更新されました」「確認」へ変更した。
- 保存データ、Firebase、Firestore、localStorage、IndexedDB、画面機能、LINE共有URLは変更していない。

### 確認結果

- JavaScript構文エラーなし。
- HTML内の埋め込み `data:image` は0件。
- 正式ヘッダーアイコン参照を確認。
- `APP_VERSION` と画面表示バージョンはともに `4.3.1`。

## 13. 2026-08-01対応済み: 在庫管理の複数端末同期

対象: `inventory.html`（対応時 `v4.4.0`、`v4.5.0`へ統合して公開）

### 対応結果

- Firestoreの保存を単純な全件上書きからトランザクション保存へ変更した。
- 2台の端末が別の商品をほぼ同時に変更した場合、双方の変更を商品ID単位で統合して残す。
- 同じ商品を同時に変更した場合は、保存を実行した端末側の変更を優先する。
- 保存処理中に追加変更が発生した場合も、クラウド側の別変更を保持して再統合する。
- 店舗、商品、カテゴリーの追加・変更・削除を、最後に取得したクラウド状態との差分として統合する。
- Firebase設定、Firestore保存先 `inventory/main`、localStorageキー、IndexedDB、既存商品IDは変更していない。

### 確認結果

- 別商品の同時変更が両方残ることを確認。
- 商品の追加・削除が別端末の変更と統合されることを確認。
- 同じ商品の競合時に端末側の変更が優先されることを確認。
- 保存中の追加変更が次の保存へ引き継がれることを確認。

## 14. 2026-08-01対応済み: 在庫管理の店舗別構造

対象: `inventory.html`（対応時 `v4.5.0`）

### 採用した構造

- アプリとFirestore保存先は1つのまま維持し、商品データは `storeId` ごとに独立させる。
- 大須・那古野・鉄板は、他の2店舗にある商品名を「商品候補」として利用できる。
- 商品候補から追加すると、在庫数は0から開始し、仕入値・単位は参考値を選べる。追加後の価格、在庫数、カテゴリー、完了状態は店舗ごとに独立する。
- 鎌倉は3店舗の商品候補へ混ぜず、鎌倉で以前使用した商品の再追加だけに限定する。
- 商品を完全削除せず「この店舗では使用しない」にすると `active:false` で保持し、通常の棚卸、集計、Excel出力から除外する。後から商品候補より再追加できる。
- 既存834商品を調査した結果、大須・那古野・鉄板では商品名の重複が155件あり、そのうち59件は店舗間で価格が異なったため、商品名候補だけ共有して価格を共通化しない構造とした。
- 鎌倉と他3店舗の商品名重複は4件だけだったため、鎌倉は独立候補とした。
- 店舗ごとにHTMLやFirestore文書を4つへ分裂させていない。共通修正を1回で全店舗へ反映できる。

### 店舗固定URLの仕様

- `?store=osu`、`?store=nagono`、`?store=teppan`、`?store=kamakura` を正式対応する。
- 日本語の店舗名指定も内部的に解決できる。
- 固定URLでは他店舗タブ、全店舗合計、全店舗一括出力、全店舗一括ゼロ操作を表示しない。
- LINE共有は、現在開いている店舗の固定URLをコピーする。
- 共通manifestからホーム画面起動した場合も、端末に記憶した固定店舗を開く。

### 確認結果

- JavaScript全スクリプトの構文エラーなし。
- 大須・那古野・鉄板の商品候補へ鎌倉商品が混ざらないことを確認。
- 商品候補追加後も元店舗の商品・価格・在庫数が変わらないことを確認。
- 非表示商品が通常集計から除外され、商品候補から在庫数0で再追加できることを確認。
- `active:false` と別商品の同時更新がトランザクションで両方残ることを確認。
- `APP_VERSION` と画面表示バージョンはともに `4.5.0`。

## 15. 2026-08-02対応済み: 在庫スプレッドシート安全連携

対象ファイル:

- `inventory-spreadsheet-sync.gs`
- `INVENTORY-SPREADSHEET-SYNC.md`

### 現在の状態

- Googleスプレッドシートには元データの「大須」「那古野」「鉄板」「鎌倉」があり、これら4シートと既存の数字は変更しない。
- Apps Scriptの初期設定と手動同期は実行済みで、`連携_大須`、`連携_那古野`、`連携_鉄板`、`連携_鎌倉`、`Zaikon連携設定` が作成されている。
- 鎌倉だけ元シートの列順が異なっていたため、固定列番号を廃止し、見出し名から列を検出するよう修正した。`#######` 表示の問題は解消済み。
- GitHub mainの最新版には、年1回運用向けの年度別確定保存を追加済み。ただしユーザー側Apps Scriptへこの最新版を再貼り付ける作業は未実施。

### 安全同期と年度保存の仕様

- 通常の書き込み先は連携4シートだけ。元の4店舗シートは保管場所・税率・備考の読み取りだけに使用する。
- Firestore `inventory/main` をREST APIで読み、店舗ID 1=大須、2=那古野、3=鉄板、4=鎌倉として分ける。
- 連携シートの棚卸額8%／10%は数式で計算する。
- 年度は在庫アプリで入力した西暦4桁を使用する。
- 年度確定保存では `2026大須`、`2026那古野`、`2026鉄板`、`2026鎌倉` のような4シートを値固定で作成する。
- 同じ年度の保存先が1つでも存在する場合は4店舗すべての作成を中止し、既存年度を上書きしない。
- 現在の元4シートは「④ 元4シートを年度名でコピー保存」で、元シートを変更せず `2025大須`、`2025那古野`、`2025鉄板`、`2025鎌倉` として固定保存できる。
- 年度確定4タブの作成がすべて成功した後、連携4シートは翌年度用に初期化する。商品ID・物品名・カテゴリ／仕入先・保管場所・単位を残し、単価・数量・税率・棚卸額・更新日・備考・状態を空にする。
- 確定タブは固定値で自動更新しない。翌年度の確定タブはその年度の棚卸が確定した時点で初めて作成する。
- 年1回運用のため、1時間ごとの自動同期は使用しない。既存トリガーがあれば「自動同期を停止（年1回運用）」で削除する。
- スプレッドシートからFirestoreへ書き戻す処理は作らない。

### 次の作業

1. Google Apps Scriptの `コード.gs` をGitHub mainの `inventory-spreadsheet-sync.gs` 全文へ貼り替える。
2. スプレッドシートを再読み込みし、「Zaikon連携」→「自動同期を停止（年1回運用）」を1回押す。
3. 最初に「④ 元4シートを年度名でコピー保存」を押して `2025` と入力し、2025年3月の元データを4店舗分固定保存する。
4. アプリで `2026` を入力して棚卸し、作業中は「② 連携シートへ安全同期」で確認する。
5. 全店舗確定後だけ「③ 入力した年度で4店舗を保存」を押し、`2026大須` など4タブを固定保存する。
6. その後、アプリの管理者設定で「翌年度を開始（単価・数量をリセット）」を押す。アプリは自動で2027年度になり、商品マスターを残して単価・数量・入力済み状態をリセットする。

## 16. 2026-08-02対応済み: 在庫管理の年度・入力済み表示

対象: `inventory.html`（現在 `v4.8.0`）

### 対応結果

- 棚卸年を西暦4桁で必須入力し、全店舗・Firestore・Excel・年度履歴・スプレッドシート保存で共通利用する。
- アプリ上部へ「2026年 棚卸」のように常時表示し、未入力時は「年度未設定」と目立つ表示にする。
- 年度未入力のまま棚卸完了、Excel出力、ゼロリセット、スプレッドシート送信は実行できない。
- 数量を入力した商品は自動で完了扱いにし、カード・表の行を薄グレー、状態を「入力済み」にする。
- 数量が前回と同じでも再入力すれば入力済みになる。取り消しでは数量と以前の完了状態を一緒に戻す。
- 上部の戻るボタンは常に押せる。戻せる操作がない場合は案内を表示し、入力後は直前の数量と入力済み状態を戻す。
- 入力済み商品は一覧の下側へ並び、ゼロリセット時は次年度に備えて完了状態も解除する。
- 管理者の「翌年度を開始」は、管理者パスワード・最終確認・安全バックアップ成功後だけ実行する。年度を自動で1年進め、全商品の単価・数量・入力済み状態・更新日時をリセットし、商品名・カテゴリ／仕入先・単位・店舗・商品IDは保持する。
- 西暦入力欄を手で変更しただけでは単価・数量を消さない。誤操作による全件消去を防止する。
- 設定画面の通常表示は「LINEで共有する」「スプレッドシートに送信」の2操作に整理し、従来の管理機能は「詳細設定」に折りたたむ。
- バックアップ保存・復元・写真整理は管理者だけに表示する。復元は管理者パスワードと最終確認が必要。
- 復元前に店舗ID、商品ID、数量、単価、年度、カテゴリー、年度別データ、履歴の形式を検査し、異常なファイルは拒否する。
- バックアップには店舗・商品・数量・単価・年度・カテゴリー・アプリ内年度別データ・履歴を含める。端末内の写真は含めない。
- Googleドライブへの自動保存と誤解される導線、および保存後に店舗削除が表示される不要な画面を削除した。
- 「スプレッドシートに送信」は最新データをFirestoreへ即時保存する操作。実際のシート取込はスプレッドシート側の「② 連携シートへ安全同期」で行う。
- 編集者モードと年度欄の補足説明を削除し、年度と入力欄だけを簡潔に表示する。
- 「棚卸完了」は商品一覧の操作列から設定画面へ移動し、管理者・編集者だけが実行できる。
- カテゴリー検索、カテゴリー管理、商品追加・編集、商品候補の「閉じる／戻る」は右上へ統一する。
- 管理者と編集者をURLで分離し、画面上の管理者・編集者・閲覧者の切替ボタンを廃止する。
- 編集者URLは暗証番号なしで開き、管理者URLだけ管理者パスワードを要求する。旧入室パスワード・アクセスパスワードは使用しない。
- LINE共有でコピーするURLは、現在の店舗に固定した編集者URLとする。管理者URLを誤ってスタッフへ共有しない。
- Firebase設定、Firestore保存先 `inventory/main`、既存商品ID、元の4店舗シート、店舗を固定する `store` 値は変更していない。

### 確認結果

- HTML内の全JavaScriptとApps Scriptの構文エラーなし。
- 同じ数量の再入力で完了になること、取り消しで元の完了状態へ戻ることを確認。
- 年度保存は保存先の事前確認を行い、既存年度がある場合はシート作成前に中止することを確認。
- 翌年度開始で2026から2027へ進み、単価・数量・完了状態だけが初期化され、商品名・仕入先／カテゴリー・単位が保持されることを確認。
- `APP_VERSION` と画面表示バージョンは `4.8.0`。

## 17. 2026-08-03 対応済み: スタッフ専用URLと削除時の完全無効化

### 対応結果

- スタッフ全員へ同じ共通URLを配る方式から、スタッフごとの専用URLへ切り替える。
- オーナー画面でスタッフごとの専用URLを発行・コピーできるようにする。
- オーナーが名前を削除したら、そのスタッフの専用URLを即時無効化する。
- 無効化された古いURLでは、希望入力・シフト閲覧・クラウド保存を一切できない。「このURLは利用停止されています」と表示する。
- 他スタッフの専用URLとオーナーURLには影響させない。
- スタッフ画面では、URLに紐づく本人以外の名前・店舗へ切り替えられないようにする。
- まだ運用前で、現行のスタッフ共通URLは誰にも配布していない。移行期間は不要で、専用URL公開と同時に共通URLのスタッフ利用を停止してよい。
- 正式バージョンはシフト・スタッフ `v1.57`、シフト・オーナー `v1.65`。
- オーナーはGoogle認証、スタッフはFirebase匿名認証を使用する。両画面のFirebaseアプリ名を分離し、認証状態が干渉しないようにした。
- Firebase AuthenticationでGoogle・匿名プロバイダを有効化し、承認済みドメインへ `zaiko-app-two.vercel.app` を登録済み。匿名アカウントの自動クリーンアップも有効化済み。
- `database.rules.json` と `firebase.json` を追加し、Realtime Databaseルールを本番公開済み。`zaiko/shift` は認証とトークンで保護し、既存の在庫・売上保存先は従来の読み書きを維持した。
- 名簿画面は画面高以内に収まり、スタッフ一覧だけ縦スクロールする。通常版と携帯互換版へ同じ修正を適用済み。

### 解決した問題

- 現在のスタッフURL `/shift/` は全員共通で、名前の選択によって本人を決めている。
- オーナーの表で名前を削除しても、名簿 `roster` に名前が残る場合がある。
- スタッフ端末のlocalStorageに以前の名前が残り、一覧にない名前を再追加する処理がある。
- `ensureStaff()` が削除済みスタッフを再作成できるため、現在は名前削除だけでは利用停止にならない。
- `ensureStaff()`、localStorageの旧名復元、共通URLによる再作成経路を止め、削除済みスタッフを復活できないようにした。

### 本番確認結果

1. オーナー `v1.65`へGoogle管理者ログインでき、既存名簿・既存シフトを表示できることを確認した。
2. テストスタッフ「動作確認」の専用URLを発行し、名前・店舗が固定された状態で開けることを確認した。
3. 専用URLで一日OKを保存し、オーナー画面の「動作確認」行へ `9:00-23:00` が反映されることを確認した。
4. オーナー画面で「動作確認」を削除後、同じ専用URLを再読み込みすると「このURLは利用停止されています。入力・閲覧・保存はできません。」となることを確認した。
5. トークンなしの共通スタッフURL `/shift/` も同じ停止画面になり、入力・閲覧・保存できないことを確認した。
6. 未認証のRESTアクセスで `zaiko/shift` が401、既存の `zaiko/items` と `zaiko/sales` が200のままであることを確認した。
7. `shift/index.html` と `shift-staff.html`、`shift/owner.html` と `shift-owner.html` はそれぞれ完全一致し、本番でもスタッフ `v1.57`、オーナー `v1.65` を確認した。
8. GitHub Actions「Zaikon app safety check」は専用URL・認証・失効・ルール・通常／互換版同期を含めて成功した。

### 運用上の注意

- スタッフには必ず本人の「専用URLをコピー」で取得したURLだけを送る。共通URLは使用しない。
- 専用URLは本人用の鍵として扱う。通常は個別配布を推奨する。店舗LINEグループへ一覧共有する場合は、全員が他スタッフのURLも開けることを承知のうえ、所属店舗のグループ内だけで扱う。
- 名前を削除すると、その人の希望データと専用URLが失効する。削除前に対象名を必ず確認する。
- 匿名認証の自動クリーンアップで古い匿名アカウントが削除されても、有効な専用URLは再度開くと新しい匿名認証で利用を開始できる。
- Realtime Databaseルールを再び全面 `true` に戻してはいけない。変更時は `database.rules.json` と本番ルールを一致させる。

### チャットの長さに関する運用ルール

- 会話が長くなり、作業履歴や注意点の見落としリスクが高くなる前に、アシスタント側から新しいチャットへの移動を案内する。
- 移動前に、完了内容・未完了内容・正式URL・バージョン・基準コミット・禁止事項をこの引継ぎメモへ更新する。
- 新しいチャットでは、古い添付HTMLではなくGitHub `main` とこの引継ぎメモを基準に再開する。

## 18. 2026-08-03対応済み: 毎日在庫ヘッダー正式アイコン

対象: `index.html`（アイコン対応時 `v10.63`、現在 `v10.64`）

### 対応結果

- ヘッダーに残っていた緑色背景の埋め込みJPEGを削除した。
- ヘッダー画像を正式アイコン `/icons/daily-inventory-32.png?v=4` へ変更した。
- 正式アイコンは、水色背景・白い牛乳パック・青い「MILK」のデザイン。
- `APP_VERSION` を `v10.63`、更新日を `2026-08-03` に更新した。
- Firebase設定、保存先、保存キー、在庫・販売・入荷・温度・わらび餅の各機能、OGP、manifest、正式URLは変更していない。

### 確認項目

- ヘッダーが正式アイコンを参照していること。
- 旧ヘッダーの埋め込みJPEGが削除されていること。
- 本番URLで `v10.63 2026-08-03` と正式アイコンが表示されること。
- GitHub Actionsの自動検査が成功すること。

## 19. 2026-08-03対応済み: 売上入力の携帯インストールボタン

対象: `sales-input.html`（現在 `v2.69`）

### 対応結果

- LINE内ブラウザ、iPhone Safari、Android Chromeを含むすべての携帯で、緑の「ホーム画面に追加」ボタンを常時表示する。
- Android Chromeなど直接インストールに対応する環境では、ボタンからブラウザ標準のインストール画面を開く。
- LINE内ブラウザとiPhoneでは、ボタンからSafari／Chromeで開いてホーム画面へ追加する手順を表示する。
- LINE判定へ `LIFF` を追加し、iPadOSのデスクトップ形式ユーザーエージェントもiOSとして判定する。
- URLに残っていた古い更新番号 `v=2.20` を現在の `v=2.69` に更新した。
- インストール済みのホーム画面アプリでは追加ボタンを表示しない。
- 売上データ、履歴、下書き、Firebase、GAS URL、パスワード、店舗固定、LINE共有URL、manifest、Service Workerは変更していない。

### 確認項目

- Android版LINE、iPhone版LINE、iPhone Safari、Android Chromeでボタン表示条件が成立すること。
- 直接インストールできない環境では外部ブラウザで開く手順が表示されること。
- 本番URLで `v2.69` と新しいボタン表示処理が公開されること。
- Vercelの公開処理が成功すること。

## 20. 2026-08-10対応済み: 店舗別スタッフ専用URL一覧コピー

対象: `shift/owner.html` と `shift-owner.html`（`v1.66`）

### 対応結果

- 名前登録画面に、選択中店舗の全スタッフ分をまとめてコピーするボタンを追加した。
- コピー内容は、スタッフ名の次の行に本人専用URLを置き、スタッフごとに空行を入れる形式で、店舗LINEグループへまとめて貼り付けられる。
- 一覧の先頭に、本人の名前のURLだけを開き、他スタッフのURLを開かないよう注意書きを付ける。
- 各URLは従来の推測困難なランダムトークンを使用し、名前と店舗の固定、スタッフ削除時の即時無効化を維持する。
- Firebase設定、Realtime Databaseルール、保存先、保存キー、既存シフトデータ、オーナーURL、スタッフ画面は変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- HTML内の全JavaScriptに構文エラーがないこと。
- 選択中店舗だけの名簿から一覧を作ること。
- スタッフ名と専用URLが1行ずつ対応していること。
- 個別の「専用URLをコピー」と削除時のURL無効化が残っていること。
- GitHub ActionsとVercel本番公開が成功すること。

## 21. 2026-08-10対応済み: 全アプリのGitHub記録・安全確認

### 確認結果

- 私の管理、毎日在庫、在庫管理、売上入力、売上オーナー、シフト・スタッフ、シフト・オーナーの正式HTMLは、すべてGitHub `main` に記録済み。
- シフト・スタッフの `shift/index.html` と `shift-staff.html`、シフト・オーナーの `shift/owner.html` と `shift-owner.html` は、それぞれ同一内容・同一バージョン。
- 7つのwebmanifest、Service Worker、各アプリの正式16/32/180/192/512pxアイコン、専用OGP画像は記録済み。
- `firebase.json`、`database.rules.json`、`OFFICIAL-STRUCTURE.md`、`CLAUDE.md`、本ファイル、在庫スプレッドシート連携コードと説明書は記録済み。
- Vercel本番で、私の管理 `v1.5`、毎日在庫 `v10.64`、在庫管理 `v4.8.0`、売上入力 `v2.71`、シフト・スタッフ `v1.57`、シフト・オーナー `v1.66` を確認。
- スタッフ共通URLは利用停止表示となり、通常版と携帯互換版の表示バージョンが一致することを確認。
- `sw.js` のナビゲーション時 `cache: 'no-store'` と全manifestの正式 `start_url` をVercel本番で確認。
- デスクトップショートカット「私の管理」は正式URL `https://zaiko-app-two.vercel.app/my-management.html` を開く設定。

### 今回補った記録・検査

- `OFFICIAL-STRUCTURE.md` と本ファイルの古いバージョン記載を、GitHub実ファイルとVercel本番へ一致させた。
- GitHub Actionsの対象へ `inventory.html`、全webmanifest、正式アイコン、運用資料を追加した。
- 在庫管理を含む全正式HTMLについて、ファイル、OGP、manifest、バージョン後退を検査する。
- manifestのJSON形式、`start_url`、参照アイコン、および正式アイコン全サイズの欠落を自動検出する。
- アプリ本体、Firebaseルール、保存先、保存キー、LINE共有URL、既存データ、シフト機能は変更していない。

### 継続して注意する安全事項

- Realtime Databaseの `zaiko/items`、`zaiko/sales` など在庫・売上系保存先は、従来仕様として認証なし読み書きが残っている。シフト保存先だけは認証と専用トークンで保護済み。
- 毎日在庫には画面側の初期暗証番号、売上入力には店舗パスワード取得処理がある。公開HTML内の暗証番号を強い秘密情報として扱うことはできない。
- シフト・スタッフHTMLには非表示の旧オーナー画面コードが残るが、Firebaseルールにより匿名スタッフはオーナー権限を持たない。分離コードの整理は、シフト機能へ影響するため別作業として慎重に行う。
- 上記3点は認証方式・保存データへ影響するため、今回のGitHub記録作業では変更していない。
- GitHubへ、発行済みスタッフ専用URL、アクセストークン、実データ、個人情報、データベースバックアップ、秘密鍵、サービスアカウントJSON、`.env` を追加しない。

## 22. 2026-08-10対応中: 在庫・売上系の段階的認証（第1段階）

### 第1段階で追加する内容

- 毎日在庫 `v10.65` と売上入力 `v2.72` に、推測困難な店舗専用URLを検証する匿名Firebase認証を追加する。
- トークンなしの既存URLとインストール済みアプリは、移行中に止めないため従来動作を維持する。
- 売上オーナーはシフト・オーナーと同じ `zaiko/shift/access/admins` の複数Google管理者だけが開ける。
- 売上オーナーから店舗別売上入力URLと鎌倉の毎日在庫URLを発行・再発行できる。
- 旧互換 `owner.html` は、クエリ文字列とハッシュを保持して正式な `sales-owner.html` へ転送する。
- `database.rules.json` に `zaiko/app_access` のトークン・店舗対応・匿名セッション規則を追加する。

### 移行中の安全条件

- `zaiko/history`、`zaiko/items`、`zaiko/sales`、`zaiko/temp_records`、`zaiko/warabi_master`、`zaiko/warabi_records` は、第1段階では認証なし読み書きを閉じない。
- 理由は、毎日在庫・売上入力の旧URLおよびリポジトリにソースがないGoogle Apps Script／スプレッドシート連携を先に止めないため。
- 専用URLを全対象端末で確認し、Google Apps Script側へ認証を追加してスプレッドシートからの記入・参照を確認した後にだけ、上記保存先をFirebaseルールで閉じる。
- 発行済み専用URL、トークン、管理者UID、実データはGitHubへ記録しない。

### 完了前の確認項目

- Firebase Authenticationで匿名認証とGoogle認証が利用可能であること。
- 本番Realtime Databaseへ `app_access` 規則を反映した後、売上オーナーの全管理者がログインできること。
- 店舗別専用URLで店舗固定となり、保存・再表示できること。再発行後は旧URLが停止すること。
- トークンなしの現在のデスクトップ／携帯アプリが、移行中は従来どおり開いて保存できること。
- GitHub ActionsとVercel公開が成功すること。

### 2026-08-10 第1段階の進捗

- Realtime Database本番へ `zaiko/app_access` 規則を公開済み。移行中の安全条件にある6つの既存保存先は、認証なし読み書きを閉じずに維持している。
- Firebase匿名認証、売上オーナーのGoogle管理者ログイン、4店舗の売上入力専用URL、鎌倉の毎日在庫専用URLを実機で確認済み。
- 店舗URLの再発行後、旧URLが `PERMISSION_DENIED` で停止し、新URLが開くことを確認済み。
- 売上入力GASは既存デプロイURLを維持したままバージョン9、日報GASは既存デプロイURLを維持したままバージョン6へ更新した。どちらも `requireForAllRequests: false` の第1段階で、Firebase IDトークンが届いた場合は必ず検証し、トークンなしの従来通信は引き続き許可する。
- 売上入力 `v2.73` は、専用URLの匿名Firebase IDトークンを売上入力GASと日報GASへ渡す。従来URLでは通信内容を変えない。
- 鎌倉店の専用URLを実機で開き、`v2.73` のFirebase受信と、認証付き売上入力GASによる2026-08-08のスプレッドシート既存データ読み込みを確認済み。
- 売上入力GASと日報GASの両プロジェクトで、`UrlFetchApp` の外部通信権限をGoogle管理アカウントから付与し、権限確認用関数の実行完了を確認済み。
- 既存値を変更せずに鎌倉店専用URLから上書き保存し、売上入力GASがエラーなく完了した。鎌倉店スプレッドシートの対象行で売上値と担当者欄を確認し、認証付きの日報GAS書き込みも完了した。重要伝達事項の未入力時は空欄で正常。
- 専用URL導入前からデスクトップに保存されている売上入力 `v2.71`（トークンなし）で、鎌倉店の既存データ読み込みと、値を変更しない上書き保存が成功した。第1段階の従来URL・既存端末互換を確認済み。
- 専用URL導入前からデスクトップに保存されている毎日在庫 `v10.64`（トークンなし）で、温度・わらび餅・本日の在庫の既存記録と「同期済み」「本日入力完了ずみ」を確認した。重複履歴を避けるため再保存は行わず、従来端末の読み込み・同期維持を確認済み。
- 売上オーナーから発行済みの毎日在庫専用URLを実機で開き、正式版 `v10.65`、匿名Firebase接続、「同期済み」表示、既存在庫の読み込みを確認した。重複履歴を避けるため再保存は行っていない。
- コードと本番相当ルールを再確認した結果、専用URL版は匿名認証する一方、旧売上入力 `v2.71` と旧毎日在庫 `v10.64` はトークンなし・Firebase未認証のまま既存保存先へ接続する。温度・わらび餅も旧毎日在庫と同じ保存経路を使用するため、6つの公開規則を現時点で閉じると確認済みの旧アプリが停止する。第1段階では規則を閉じないと判断した。
- 2026-08-11、確認用Windowsパソコンで、店舗専用URL版の毎日在庫 `v10.65` と鎌倉店売上入力 `v2.73` をEdgeアプリ（PWA）としてインストールした。デスクトップの新しい正式アイコンから再起動し、専用URL版が開くことを実機確認した。
- 同じパソコンのデスクトップに「私の管理」「在庫管理」「毎日在庫」「売上入力」「売上オーナー」「シフト・オーナー」の新しい正式アイコンが揃ったことを確認した。旧ローカルHTML／旧Edgeショートカットは新旧を取り違えないよう個別確認して整理中で、一括削除はしない。
- 上記は1台の確認用パソコンでの置き換え確認であり、他店舗・スマートフォン・他パソコンの移行完了を意味しない。温度・わらび餅を含む全実運用端末の確認が終わるまでは、6つの公開規則とGASの段階移行設定を維持する。
- GitHubコミット `2b5d938cac4ccc8ff58f323684bf942c6707a24f` とVercelの2つの公開結果はいずれも成功。
- **未完了:** 旧デスクトップアプリを専用URL版へ安全に置き換え、実運用端末と温度・わらび餅を含む全連携経路を認証対応へ移行する。旧版を廃止できる状態になった後にだけ、必須認証への切替可否を判断する。
- **禁止:** 旧デスクトップアプリと温度・わらび餅を含む連携経路の移行が終わるまでは、既存保存先の公開規則を閉じず、GASの `requireForAllRequests` を `true` にしない。

## 23. 2026-08-11対応: シフト編集者専用URL

対象: `shift/owner.html`、`shift-owner.html`、`database.rules.json`（`v1.67`）

### 対応内容

- オーナー画面へ「編集者」ボタンを追加し、編集者1・編集者2の2人分の専用URLを個別に発行できるようにした。
- 編集者はGoogleアカウント登録なしで、推測困難な専用URLとFirebase匿名認証を使って開く。
- 編集者は、シフト作成・編集・コピー・要請・公開・印刷を利用できる。
- 編集者画面では、名前登録、スタッフ削除、スタッフ専用URL発行、全データ消去を表示しない。
- Realtime Databaseルールでも、編集者による名簿変更、スタッフ新規作成、スタッフ削除、氏名・所属店舗の変更、アクセストークン操作を拒否する。
- 2人のURLは別トークンで管理する。「再発行」した枠の古いURLだけを停止できる。
- 既存のGoogle管理者、スタッフ専用URL、スタッフ削除時のURL失効、既存シフトデータ、在庫・売上系の段階移行設定は変更しない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にする。

### 公開前後の確認項目

- GitHub Actions「Zaikon app safety check」が成功すること。
- Firebase本番へ更新後の `database.rules.json` を反映すること。
- オーナーから編集者1・編集者2のURLを発行できること。
- 編集者URLで既存シフトの読み込み、編集、コピー、要請、保存ができること。
- 編集者画面に名前登録・削除・全消去が出ず、直接通信でも名簿とスタッフ作成・削除が拒否されること。
- 片方を再発行すると、その枠の旧URLだけが停止し、もう片方とオーナー・スタッフURLは動き続けること。

### 2026-08-11 本番確認状況

- Firebase本番ルールの公開は完了している。
- 編集者1の専用URLはInPrivate実機確認まで完了している。
- 編集者1・編集者2の専用URLは、まだ他の人へ送っていない。
- 発行済みURLとトークンはGitHubへ記録しない。

## 24. 2026-08-11対応済み: LINE用スタッフURL一覧の整形

対象: `shift/owner.html` と `shift-owner.html`（`v1.68`）

### 対応結果

- 「店舗全員のURLをまとめてコピー」は、従来どおり1回の操作で全員分をコピーする。
- LINEへ貼り付けたとき、各スタッフを「名前」「次の行にURL」「空行」の順で表示する。
- スタッフ専用URLの発行・失効、名簿、シフトデータ、Firebase設定、Realtime Databaseルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- コピー文字列がスタッフごとに名前・URL・空行の順になること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 25. 2026-08-12対応済み: シフト・オーナーの専用ポップアップ

対象: `shift/owner.html` と `shift-owner.html`（`v1.69`）

### 対応結果

- ブラウザ名が出る標準ポップアップを廃止し、シフト画面の黄色・ピンクに合わせたアプリ専用デザインへ変更した。
- シフト公開・公開取消、スタッフ削除、全データ消去・復元、編集者URL再発行の確認画面を統一した。
- 完了・お知らせ・エラー表示も同じデザインへ統一し、内容に応じてアイコンと色を切り替える。
- URLの自動コピーが使えない場合は、コピー対象を読みやすい専用画面内に表示する。
- 「キャンセル」「実行」ボタンと右上の閉じるボタンを設け、パソコン・携帯の両方で押しやすくした。
- 確認後に行う公開・削除・復元・再発行などの既存処理は変更していない。
- Firebase設定、Realtime Databaseルール、既存シフトデータ、発行済みURL、在庫・売上系の公開ルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- ブラウザ標準の `confirm`、`alert`、URL表示用 `prompt` が残っていないこと。
- 公開確認で「キャンセル」は変更せず、「公開する」だけが従来処理を実行すること。
- 危険な操作はピンク・赤系、通常の公開は黄色系で表示すること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 26. 2026-08-12対応済み: スタッフURLの名前付き個別送信

対象: `shift/owner.html` と `shift-owner.html`（`v1.70`）

### 対応結果

- 名簿の「専用URLをコピー」は、URLだけでなく「スタッフ名＋改行＋本人専用URL」をコピーする。
- 名前登録画面へ「1人ずつコピーして送る」を追加した。
- 順番式操作では、1人分をコピーするたびにLINEへ貼り付けて送信し、アプリへ戻って「次の人をコピー」を押す。
- 最後のスタッフでは「完了」を表示する。途中で「終了」または右上の閉じるを押すと安全に中止できる。
- LINEの仕様上、1回の貼り付けを自動で複数の吹き出しへ分割できないため、別々の吹き出しにする場合はこの順番式操作を使用する。
- 従来の「店舗全員のURLをまとめてコピー」も残し、1つのメッセージで送りたい場合に使用できる。
- スタッフ専用URLの発行・失効、名簿、既存シフトデータ、Firebase設定、Realtime Databaseルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- 個別コピーがスタッフ名と本人専用URLの2行になること。
- 順番式操作が選択中店舗の名簿順で1人ずつ進むこと。
- キャンセル時に次のスタッフへ進まないこと。
- 従来の全員まとめてコピーが残っていること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 27. 2026-08-12対応済み: スタッフ専用URLの個別再発行

対象: `shift/owner.html` と `shift-owner.html`（`v1.71`）

### 対応結果

- 名前登録画面の各スタッフ行へ「新しいURL」ボタンを追加した。
- 確認画面で実行した場合だけ、そのスタッフの古いURLを利用停止にし、新しいランダムトークンの専用URLを発行する。
- 新しいURLは「スタッフ名＋改行＋本人専用URL」の形式で自動コピーする。
- 再発行した本人以外のスタッフURL、編集者URL、オーナーURLには影響しない。
- 通常の「URLコピー」、店舗全員まとめてコピー、1人ずつコピーして送る機能は残している。
- Firebase設定、Realtime Databaseルール、既存シフトデータ、名簿は変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- 「新しいURL」で必ず確認画面が表示されること。
- 再発行後は旧URLだけが停止し、新URLが有効になること。
- 他スタッフのURLと既存シフトデータへ影響しないこと。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 28. 2026-08-12対応済み: スタッフ専用URL停止の確認番号

対象: `shift/index.html` と `shift-staff.html`（`v1.58`）

### 現在の状況

- オーナー `v1.71` の「新しいURL」で店長のURLを実際に再発行したが、新しいURLでも「このURLは利用停止されています」と表示された。
- スタッフ `v1.58` で同じ新URLを再確認し、確認番号 `M1`（シフト公開情報 `meta` の読取拒否）を確認した。匿名認証・セッション保存・URLトークン読取と有効性確認までは成功している。
- `database.rules.json` のスタッフセッショントークン参照9箇所を、Firebase公式例と同じスラッシュ連結の明示パス形式へ変更した。在庫・売上・温度・わらび餅の6公開ルール、既存データ、既存URLは変更していない。
- 2026-08-12、修正済みルール全文をFirebase Realtime Database本番へ公開した。画面から「公開されていない変更」の帯が消えたことを確認済み。次は同じ店長URLで実機確認する。
- 公開後も `M1` が継続した。原因は、スタッフ認証時には存在しない編集者セッション値を先に `.child()` へ渡す式が、スタッフ許可判定より前に失敗すること。編集者1はセッションが存在するため通り、スタッフ全員だけ失敗する状況と一致する。
- `database.rules.json` の編集者セッショントークン参照9箇所も、空値でエラーにならない明示パス形式へ修正した。
- 2026-08-12、この2回目の修正版ルールもFirebase Realtime Database本番へ公開し、「公開されていない変更」の帯が消えたことを確認済み。次は同じ店長URLで再確認する。
- 2回目の公開後も `M1` が継続。明示パス形式でも空値を文字列へ組み立てる前に型エラーとなるため、編集者セッション9箇所・スタッフセッション5箇所すべてに `.isString()` の先行ガードを追加した（コミット `fa53c26ff6a87f851840c1c4cb3dfc11e24efa55`）。
- 2026-08-12、この3回目の修正版ルールをFirebase Realtime Database本番へ公開した。同じ店長の新URLで `M1` が解消し、店舗「大須」・名前「店長」が固定されたカレンダー画面の読込成功をWindows Edge実機で確認した。
- スタッフ専用URLの匿名認証・セッション保存・トークン確認・公開情報読取・本人データ読取は正常。
- 店長URLで2026年8月31日にテストの「×」を保存し、5秒後のブラウザ再読込でも残ることをWindows Edge実機で確認した。その後「この日の希望を解除」を実行し、「×」が消えてテストデータが残っていないことも確認済み。
- 店長URLについて、閲覧・入力・保存・再読込・解除までの実機確認が完了した。
- 続けてヒナキの既存専用URLを別ウィンドウで開き、店舗「大須」・名前「ヒナキ」が固定されたカレンダー画面の読込成功を確認した。
- 別スタッフの既存URLも有効なため、店長以外の全員分を再発行する必要はない。店長は再発行後の新URLを使用し、他スタッフは現在表示されている既存URLを配布できる。
- Firebase本番ルールは `database.rules.json` と一致しており、在庫・売上・温度・わらび餅の既存公開ルールも維持されている。
- 原因を推測で変更せず、匿名認証・セッション保存・トークン読取・データ読取のどこで停止したかを画面の「確認番号」で判別できるようにした。
- 停止画面に「もう一度確認」ボタンを追加した。これは再読込だけを行い、URLの再発行やデータ変更はしない。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。
- 調査中のため、スタッフURLを他の人へ配布するのは原因解決と実機確認が終わるまで待つ。

### 次の確認

- 店長URLとヒナキの既存URLの実機確認は完了済み。
- 名前付き個別コピーまたは「1人ずつコピーして送る」で配布を開始できる。
- 店長には再発行後の新URL、他スタッフには現在の既存URLを送る。不要な再発行はしない。
- 追加の停止画面が出た場合は「確認番号」を記録する。
- 確認番号に対応する認証段階だけを調査し、Firebaseルールや既存データを推測で変更しない。
- 既存の在庫・売上・温度・わらび餅の公開ルールを閉じない。

## 29. 2026-08-12対応済み: 一日OKの時間表示と携帯カレンダー幅

対象: `shift/index.html` と `shift-staff.html`（`v1.59`）

### 対応結果

- 入力ポップアップの「◎」は従来どおり一日OKとして保存する。
- スタッフカレンダーでは「◎」ではなく `9:00〜23:00` を2行の時間表記で表示する。
- 幅480px以下では7曜日が画面内に収まるよう、カレンダー列を `minmax(0,1fr)` にし、余白・列間隔・マス・文字・店舗表示・上部年月欄を小さくした。
- 画面全体の不要な横スクロールを止めた。
- Firebaseルール、専用URL、認証、既存シフトデータ、保存形式は変更していない。既存の `allday:true` データも時間表記になる。
- Windows Edge実機で、既存の一日OKが各日 `9:00〜23:00` の2行表示へ変わったことを確認済み。携帯縦画面での7列表示は実機確認待ち。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。

### 確認項目

- 「◎」を押した日のカレンダー表示が `9:00〜23:00` になること。
- 携帯の縦画面で日曜から土曜まで7列すべてが欠けずに見えること。
- 日付タップ、月スワイプ、保存、解除が従来どおり動くこと。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 30. 2026-08-12対応済み: 携帯の拡大縮小・曜日・固定名表示

対象: `shift/index.html` と `shift-staff.html`（`v1.60`）

### 対応結果

- カレンダーの通常サイズは変更せず、携帯で2本指を広げると拡大、つまむと縮小できるよう `pinch-zoom` を許可した。
- 幅480px以下の曜日文字を10pxから20pxへ拡大し、太字にした。
- 専用URLで固定された店舗名とスタッフ名は、無効化された選択欄でも薄くならないよう、濃い文字・太字・白背景へ変更した。
- 日付タップ、月スワイプ、保存、認証、既存データは変更していない。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。

### 確認項目

- 携帯で曜日が大きく表示され、7列が画面内に収まること。
- 店舗名と本人名が濃く見えること。
- カレンダー上で2本指の拡大・縮小ができること。
- 1本指の月スワイプと日付タップが従来どおり動くこと。
- GitHub ActionsとVercel本番公開が成功すること。

## 31. 2026-08-12対応済み: 全部一日OKの安全な解除と日付拡大

対象: `shift/index.html` と `shift-staff.html`（`v1.61`）

### 対応結果

- 「全部 ◎ 一日OK」は空欄の日だけに `allday:true, bulkAllDay:true` を保存し、既存の個別時間・「×」・備考を上書きしない。
- 初回のv1.61実機確認では、以前の一括入力に `bulkAllDay` がないため解除されないことを確認した。v1.62では一日OKが月の半数以上ある従来データも一括状態として認識する。
- 一括追加分がある場合はボタンを「全部一日OKを解除」へ変更する。
- 解除ボタンを押すと専用確認ポップアップを表示し、「解除する」を選んだ場合だけ、その月の `allday:true` を解除する。
- 個別に設定した一日OK、個別時間、「×」、備考は解除対象にしない。
- 日付入力ポップアップの見出しを14pxから21pxへ拡大した。
- Firebaseルール、専用URL、認証、確定シフト、既存データは変更していない。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。

### 確認項目

- 空欄と個別入力が混在する月で「全部」を押しても個別入力が維持されること。
- 2回目は確認ポップアップが出て、戻るでは変化せず、解除するで一括追加分だけ消えること。
- 日付ポップアップの見出しが約1.5倍になっていること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 32. 2026-08-12対応済み: 今日の強調・本日のシフト・カレンダー装飾

対象: `shift/index.html` と `shift-staff.html`（`v1.64`）

### 対応結果

- 実際の今日と表示中の年月が一致する場合、今日の日付マスを黄色い太枠・淡い黄色背景・「今日」バッジで強調する。
- 表示中の月に関係なく、カレンダー下へ実際の今日を対象にした「本日のシフト」カードを常時表示する。
- シフト未公開または今日が未確定の場合は「未定」と表示する。本人が希望で「×」を提出していても、確定前は「未定」とする。
- 今日が休みとして確定した場合、または確定済みで勤務時間がない場合は「お休み」と表示する。
- 勤務確定時は所属店舗名と時間を表示し、鉄板応援があれば鉄板と時間も追加表示する。
- カレンダーを淡いピンクのカード調にし、曜日見出し・丸い日付マス・薄い影・日曜の淡いピンク・土曜の淡い水色を追加した。
- 携帯で7列が収まる幅、2本指拡大、月スワイプ、日付タップ、保存、認証、既存データは維持した。
- Firebaseルール、専用URL、確定シフトデータは変更していない。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。
- v1.65として試した7色曜日・水玉・丸い日付・花付きカードは、ユーザー確認により取り消し、v1.64のデザインへ復帰した。

### 確認項目

- 今日のマスが黄色い枠と「今日」で分かること。
- カレンダー下の本日のシフトが「未定」「お休み」または店舗・時間で正しく表示されること。
- 携帯で7列が画面内に収まり、かわいい装飾と大きい曜日が見えること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 33. 次のチャットへ渡す文章

次の文章だけで開始できる。ただし、作業者は必ずこのファイルを全文読むこと。

> `booandmoomoo1011-maker/zaiko-app` の `main` にある `ZAIKON-HANDOFF.md` を最初に全文読んでください。私の管理v1.5、毎日在庫v10.65、在庫管理v4.8.0、売上入力v2.73、シフト・スタッフv1.64、シフト・オーナーv1.71が正式版です。スタッフ専用URLの停止問題はFirebase本番ルール修正で解消し、店長URLの閲覧・入力・保存・再読込・解除と、ヒナキの既存URLの読込まで実機確認済みです。v1.59では一日OKをカレンダー上で9:00〜23:00と表示し、携帯縦画面へ7列を収めました。セクション23のシフト編集者専用URLは、Firebase本番ルール公開と編集者1のInPrivate実機確認まで完了し、編集者1・編集者2のURLはまだ他の人へ送っていません。セクション21の全アプリGitHub記録確認は完了しています。セクション22の在庫・売上系段階的認証は、既存端末とスプレッドシートを止めない第1段階です。Firebase本番 `app_access`、匿名認証、Google管理者認証、2つのGAS認証対応と外部通信権限付与、鎌倉専用URLからの認証付き売上データ読み込み・上書き保存・日報保存、旧売上入力v2.71からの読み込み・保存、旧毎日在庫v10.64と専用URL版v10.65の既存記録・同期確認までは完了しています。2026-08-11に確認用Windowsパソコンへ専用URL版の毎日在庫v10.65と鎌倉店売上入力v2.73をPWAとして設置し、正式アイコンからの再起動も確認しました。ほかの実運用端末と温度・わらび餅を含む全経路の移行は未完了です。コード確認により旧版はFirebase未認証で、今規則を閉じると停止することを確認したため、第1段階では6つの公開規則を維持しています。全実運用端末と連携経路の認証移行が終わるまでは、既存保存先を閉じず、GASの必須認証を有効にしないでください。シフトの認証済み機能、Firebase設定、既存データ、通常版／互換版同期を壊さず、変更後はGitHub ActionsとVercel本番を確認してください。
