# Zaikon 正式引継ぎメモ

最終更新: 2026-09-04（JST）

このファイルは、Zaikonアプリ群を別チャット・別担当者・Claude Codeへ引き継ぐ際の最優先資料です。作業開始前に必ず全文を読み、正式リポジトリの `main` 最新版から作業してください。

## 1. 正式環境

- GitHub: `booandmoomoo1011-maker/zaiko-app`
- 正式ブランチ: `main`
- Vercel: `https://zaiko-app-two.vercel.app`
- 2026-08-03時点のスタッフ専用URL機能実装コミット: `e06c94396ed69474e4c74df0bbe27da0ecc5cc3a`
- オーナー／スタッフ認証分離コミット: `3e8023c063ce6e46806a8670753b78a0fa96da21`
- 名簿画面スクロール修正コミット: `67d75a769f479fb070beadaabb9cf9331effaefb`
- 過去のLINEプレビュー作業開始基準: `c70d595accc4fdc80bb1af78d6c5d6e2bc90b1f5`
- 毎日在庫ヘッダー正式アイコン反映コミット: `0ee3e83c16dec404e5790a881d9e56f60ccd3a24`
- 売上入力の携帯インストールボタン修正コミット: `86df33833dcc64e8562b7d52007d17f0ec70defe`
- 全アプリ記録確認の開始コミット: `d5a1dcab09d943d2b9669bd96d85b9255ec5893f`
- 全アプリ自動検査の拡充コミット: `d3ceac16969d1e4b310df9920abdac7f5a2c2f1a`
- 正式構成資料の現行化コミット: `6ac16ff7d4f746c96c86eb078f99b23acc77edda`

古い添付ファイルや過去チャットのコピーを直接デプロイせず、必ずGitHub `main` の現行ファイルを取得して差分を確認すること。

## 2. アプリ構成とURL

| アプリ | 正式URL | 主なファイル |
|---|---|---|
| 私の管理 | `https://zaiko-app-two.vercel.app/my-management.html` | `my-management.html` |
| 毎日在庫 | `https://zaiko-app-two.vercel.app/` | `index.html` |
| 在庫管理 | `https://zaiko-app-two.vercel.app/inventory.html` | `inventory.html` |
| 売上入力 | `https://zaiko-app-two.vercel.app/sales-input.html` | `sales-input.html` |
| 売上オーナー | `https://zaiko-app-two.vercel.app/sales-owner.html` | `sales-owner.html` |
| シフト・スタッフ | `https://zaiko-app-two.vercel.app/shift/` | `shift/index.html` |
| シフト・オーナー | `https://zaiko-app-two.vercel.app/shift/owner.html` | `shift/owner.html` |

### 在庫管理の店舗固定URL

- 管理者（全店舗）: `https://zaiko-app-two.vercel.app/inventory.html?role=admin`
- 大須・編集者: `https://zaiko-app-two.vercel.app/inventory.html?store=osu&role=editor`
- 那古野・編集者: `https://zaiko-app-two.vercel.app/inventory.html?store=nagono&role=editor`
- 鉄板・編集者: `https://zaiko-app-two.vercel.app/inventory.html?store=teppan&role=editor`
- 鎌倉・編集者: `https://zaiko-app-two.vercel.app/inventory.html?store=kamakura&role=editor`

店舗ごとにHTMLを複製せず、4 URLとも正式な `inventory.html` を使用する。固定URLでは該当店舗だけを表示し、端末側にも最後の固定店舗を記憶する。
`role=editor` は暗証番号なしで編集者画面を開く。`role=admin` は管理者パスワード認証後だけ管理機能を表示する。画面上で役割を切り替えない。

### 携帯ホーム画面用の互換URL

- シフト・スタッフ: `https://zaiko-app-two.vercel.app/shift-staff.html`
- シフト・オーナー: `https://zaiko-app-two.vercel.app/shift-owner.html`

重要: 携帯用PWAの `start_url` はルート側の互換HTMLを開く。シフトを更新するときは、次のペアを必ず同じ内容・同じバージョンにすること。

- `shift/index.html` と `shift-staff.html`
- `shift/owner.html` と `shift-owner.html`

片方だけ更新すると、PCでは最新版、携帯アプリでは旧版という不具合が再発する。

## 3. 絶対に守ること

1. アプリを統合しない。使用者が違うため、在庫・売上・シフトの各アプリは別々に維持する。
2. 正式ファイル名、正式URL、互換URLを勝手に変更・削除しない。
3. `zaiko-app/owner.html`（売上オーナー）と `shift/owner.html`（シフト・オーナー）を混同・上書きしない。
4. Firebase設定、保存先、保存キー、LocalStorage、IndexedDB、Cookie、共有データ形式を、明示依頼なしに変更しない。
5. LINE共有URL、固定共有URL、Vercelプロジェクト、GitHubリポジトリを勝手に変更しない。
6. シフト・在庫・売上の既存機能を、今回の依頼範囲外で変更しない。
7. アイコンは正式アイコン一式を使用し、別デザインへ勝手に差し替えない。
8. 公開前に対象ファイルと変更範囲を確認し、無関係なファイルを同時更新しない。
9. バージョンアップ時は、コードだけでなくこの `ZAIKON-HANDOFF.md` も同時に更新する。

## 4. 現在の主要アプリ最新版

- 私の管理: `v2.0`
- 毎日在庫: `v10.74`
- 在庫管理: `v4.9.0`
- 売上入力: `v2.73`
- シフト・スタッフ: `v1.90`
- シフト・オーナー: `v1.180`

### シフト・スタッフの現在の正式機能

- スタッフはオーナーが発行した本人専用URLから開く。店舗と名前はURLに紐づく本人へ固定され、変更できない。
- トークンなしの共通URLは停止済みで、入力・閲覧・保存はできない。
- Firebase匿名認証とRealtime Databaseルールにより、本人の希望・備考・要請だけを読み書きできる。
- 削除・無効化・認証失敗・権限エラー・有効性を確認できないオフライン状態では、画面と入力を直ちに停止する。
- `shift/index.html` と `shift-staff.html` は必ず同一内容・同一バージョンにする。
- v1.54: 入力済みの日に「この日の希望を解除」を追加し、日付ポップアップ右上に「閉じる」を追加した。
- v1.55: カレンダーを左へスワイプで翌月、右へスワイプで前月へ移動できる。日付タップ・縦スクロールとの誤判定を防止している。
- v1.56: カレンダー上部中央に表示中の年月を大きく表示し、スワイプ直後は黄色く変化する。「最終更新」は右側へ小さく表示する。
- v1.57: スタッフ専用URL、匿名認証、名前・店舗固定、共通URL停止、削除時の完全無効化に対応した。
- v1.58: スタッフ専用URLが停止表示になる原因を特定するため、認証段階別の確認番号と「もう一度確認」を追加した。Firebaseルール・既存データ・URLは変更していない。
- v1.59: 「◎」で保存した一日OKをスタッフカレンダー上では `9:00〜23:00` と時間表示に変更した。幅480px以下では7列が画面内に収まるよう、余白・文字・マス・上部年月欄を小さくした。
- v1.60: 元のカレンダーサイズを維持したまま、携帯の2本指による拡大・縮小を許可した。携帯時の曜日を20pxへ拡大し、固定された店舗名・スタッフ名を濃い太字で表示するようにした。
- v1.61: 「全部 ◎ 一日OK」で追加した日にだけ `bulkAllDay:true` を付け、再度ボタンを押すと専用確認ポップアップ後に一括追加分だけ解除できるようにした。個別時間・「×」・備考・既存入力は保護する。日付入力ポップアップの見出しを14pxから21pxへ拡大した。
- v1.62: v1.61以前の「全部」入力には識別印がないため解除できなかった問題を修正した。月の半数以上が一日OKなら従来の一括入力として解除ボタンを表示し、確認後はその月の一日OKをすべて解除する。個別時間・「×」・備考は維持する。
- v1.63: 実際の今日の日付マスに黄色い太枠と「今日」を表示し、カレンダー下に「本日のシフト」を追加した。未確定は「未定」、休み確定は「お休み」、勤務確定は店舗名と時間を表示する。
- v1.64: 7列の携帯幅を維持したまま、カレンダーを淡いピンクのカード調へ変更した。曜日を色付き見出しにし、日付マスへ丸み・薄い影、日曜へ淡いピンク、土曜へ淡い水色を追加した。
- v1.66: スタッフカレンダーの開始月を2026年9月にし、終了した月を日本時間で自動的に閲覧専用へ切り替える。
- v1.69: オーナー要請への回答を1本の開始・終了時間欄と「この時間で承認」「✕ 否認」に整理した。回答後は月提出済み扱いとなり、同じ日の通常希望を別に再提出しない。

### シフト・オーナーの現在の正式機能

- 未提出スタッフが名簿にいても、日付ヘッダーをタップすると日別画面が開く。
- 同じ店舗内では、月表の名前を約0.45秒長押しして上下に並べ替えできる。順番は保存され、別店舗へは移動しない。
- 日別画面の大須・那古野・鉄板すべてに「＋名前を追加」がある。
- 所属店舗以外へ入るスタッフとAM・PM時間を登録できる。
- 同じスタッフの時間が全店舗間で重なった場合、該当スタッフ名の横に赤い `⚠` だけを表示する。文章、点滅、追加ポップアップは出さない。
- 旧来の鉄板配置データとの互換性を維持する。
- 公開済みの応援店舗と時間はスタッフ画面にも表示する。
- Google認証済みの管理者だけがオーナー画面とシフト全体データを利用できる。
- 名簿の各スタッフに「専用URLをコピー」があり、十分長いランダムトークンのURLを発行する。
- 名前削除時はスタッフデータ、全店舗の名簿、トークン対応を一括更新し、古い専用URLを即時無効化する。
- v1.65: 名前登録画面を画面高以内に収め、一覧部分だけを縦スクロール可能にした。右上の閉じるボタンも常時表示する。
- v1.66: 名前登録画面で選択中店舗の全スタッフ名と本人専用URLを、LINEグループ用の一覧としてまとめてコピーできる。
- v1.67: Google登録なしで使える2人分のシフト編集者専用URLを追加した。編集者はシフト作成・編集・コピー・要請・公開を利用できるが、スタッフ登録・削除・スタッフ専用URL発行・全データ消去は画面とRealtime Databaseルールの両方で禁止する。
- v1.68: 店舗全員のスタッフ専用URL一覧を、名前の次の行にURL、その次に空行を入れるLINE向け形式へ変更した。
- v1.69: ブラウザ標準の確認・完了・エラー表示を、黄色・ピンク基調の見やすいアプリ専用ポップアップへ統一した。
- v1.70: 個別のスタッフ専用URLコピーへ本人名を追加し、LINEで別々の吹き出しとして送れる「1人ずつコピーして送る」順番式操作を追加した。
- v1.71: 名簿の各スタッフへ「新しいURL」ボタンを追加し、その人の古いURLだけを停止して新しい名前付きURLを発行・コピーできるようにした。
- v1.72: オーナー・編集者カレンダーを2026年9月開始にし、終了月を日本時間で自動的に閲覧専用へ切り替える。
- v1.83: 月表と日別画面へ回答待ち・承認・否認の紙飛行機を表示し、オーナーが選択した要請1件だけを変更・削除できるようにした。

### 2026-08-01に解決した原因

- 通常URLと携帯用URLの同期時に、携帯側の新しい名前選択式 `v1.52` ではなく古い通常側 `v1.48` を基準にしたため、GitHub上のスタッフ画面が後退した。正しい `v1.52` を復元し、名簿順の即時反映と引っ張って更新を含む `v1.53` として両ファイルを再同期した。

- 日別画面が開かなかった原因は、名簿だけに存在する未提出スタッフに対して、`decided`、`req`、`hold` などを直接参照してJavaScriptエラーになっていたこと。
- 携帯アプリだけ更新されなかった原因は、通常URLの `shift/owner.html` と携帯PWA用の `shift-owner.html` が別ファイルで、片方だけ更新されていたこと。

### 再発防止の自動検査

- GitHub Actions の `.github/workflows/shift-sync-check.yml` で、更新のたびにZaikonアプリを自動検査する。
- 対象は「私の管理」「毎日在庫」「売上入力」「売上オーナー」「シフト・スタッフ」「シフト・オーナー」と `sw.js`。
- 正式HTMLの欠落、OGP・manifestの欠落、OGP URL/画像の重複、シフト通常版と携帯版の不一致、名前選択・名簿同期・引っ張って更新の消失、売上入力の店舗固定の消失、`sw.js` の `no-store` 消失、バージョン番号の後退を検出した場合は検査を失敗にする。
- 在庫管理 `inventory.html` は別作業中のため、ユーザー指示により今回の自動検査対象から除外している。許可なく変更・検査追加しない。

## 5. 自動更新の正式仕様

- `sw.js` はナビゲーション時に `cache: 'no-store'` でネットから最新版HTMLを取得する。
- 全アプリは、閉じて再度開いたときに最新版を取得する。
- 入力中・操作中に突然自動再読み込みはしない。入力消失を防ぐため、この方針を維持する。
- シフト・スタッフとシフト・オーナーには「引っ張って更新」がある。
- 画面上端から下へ引っ張ると「↓ 引っ張って更新」→「離して更新」と表示し、指を離すとキャッシュ回避付きで再読み込みする。
- 表の横スクロール、入力欄、ボタン、モーダル操作では引っ張って更新を開始しない。

## 6. アイコン・LINEプレビュー

- 「私の管理」のヘッダーは黒。
- 6アプリはそれぞれ専用の一覧アイコン、16/32/180/192/512pxアイコンを使用する。
- 7つの正式画面はそれぞれ専用の `og:title`、`og:description`、`og:url`、`og:image` を持つ。
- LINEプレビュー画像は各アプリ専用。すべてが「毎日在庫」画像にならないようにする。
- 送信済みLINEメッセージには古いプレビューが残ることがある。確認時は新しいメッセージとしてURLを送る。
- `inventory.html` の古い埋め込み `apple-touch-icon` と動的manifest上書き処理は復活させない。
- アプリ起動前に大きなアイコン画像が割り込むスプラッシュ表示を再追加しない。

## 7. バージョンアップ時の必須手順

1. GitHub `main` の最新ファイルと最新コミットを取得する。
2. 変更対象と禁止事項をこのファイルで確認する。
3. 依頼された範囲だけ変更する。
4. 画面に表示されるバージョンと、コード内のバージョン定数がある場合は両方を同じ番号へ更新する。
5. シフト画面の場合は通常URL用・携帯用の2ファイルを同期する。
6. HTML/JavaScriptの構文、主要画面、コンソールエラー、画像切れ、横はみ出しを確認する。
7. `main` へ反映し、Vercel本番で実際のURLを開いて確認する。
8. 携帯用URLも必ず開き、通常URLと同じバージョン・機能になっていることを確認する。
9. `sw.js` を変更した場合は、本番の `/sw.js` も確認する。
10. この `ZAIKON-HANDOFF.md` の日付、バージョン、最新機能、注意点、基準コミットを更新する。
11. ユーザーへ、変更内容、確認結果、現在バージョンを簡潔に報告する。

## 8. 本番確認チェックリスト

- [ ] 私の管理から6アプリがそれぞれ正しいURLで開く
- [ ] 各アプリの専用アイコンとOGP画像が読み込める
- [ ] シフト・スタッフの通常URLと携帯用URLが同じバージョン
- [ ] シフト・オーナーの通常URLと携帯用URLが同じバージョン
- [ ] シフト・オーナーの日付タップで日別画面が開く
- [ ] 日別画面の大須・那古野・鉄板に「＋名前を追加」がある
- [ ] 全店舗の時間重複時に名前横へ `⚠` だけが表示される
- [ ] 名前の長押し並べ替えが同一店舗内だけで動く
- [ ] 引っ張って更新がシフト2画面で動く
- [ ] Firebase接続、保存、共有URL、既存データが維持される
- [ ] コンソールにアプリ由来のエラーがない

## 9. 2026-08-01対応済み: 売上入力の店舗固定

対象: `sales-input.html`（インストールボタン対応時 `v2.69`、現在 `v2.71`）

### 対応結果

- 初回だけ店舗選択画面を表示する。
- 店舗名なしの共通URLでも、端末の `last_store` を読み、最後に選んだ店舗を次回から自動表示する。
- `?store=` 付きURLはURL指定店舗を最優先する。
- 戻るボタンを押すと `last_store` を解除して店舗選択画面へ戻り、選び直した店舗を新たに保存する。
- 売上データ、履歴、下書き、自動保存、Firebase、GAS URL、パスワード、日報、共有URL、デザインは変更していない。

### 対応済み仕様

1. 初回だけ店舗選択画面を表示する。
2. ユーザーが選んだ店舗をその端末の `last_store` に保存し、次回以降は店舗名なしの共通URLでも同じ店舗を自動表示する。
3. `?store=大須`、`?store=那古野`、`?store=鉄板`、`?store=鎌倉` がある場合はURL指定を最優先し、その店舗を固定表示する。
4. 戻るボタンまたは設定から別店舗を選んだ場合は、`last_store` を新しい店舗へ更新し、次回から新しい店舗を開く。
5. 売上データ、履歴、下書き、自動保存、Firebase、GAS URL、パスワード、項目表示設定、日報、共有URLを変更しない。
6. 画面デザインは変更しない。
7. `APP_VERSION` と画面表示バージョンを同じ番号へ更新する。
8. 修正後は、共通URLで店舗選択 → アプリを閉じて再度開く → 同じ店舗が自動表示されることを実機相当で確認する。
9. 店舗名付き4 URLもすべて確認する。
10. デプロイ時にこの `ZAIKON-HANDOFF.md` のバージョン・対応結果・基準コミットも更新する。

## 10. 2026-08-01対応済み: 在庫管理の同期保護

対象: `inventory.html`（対応時 `v4.2.1`）

### 対応結果

- 起動時のFirestoreリアルタイム監視で、端末側の保存日時より古いクラウドデータを適用しない。
- 端末側のデータが新しい場合は、古いクラウドデータで画面を上書きせず、端末側の最新版をFirestoreへ保存する。
- 通常操作中のFirestore保存は従来どおり3秒のデバウンスを維持する。
- 画面非表示、ページ離脱、アプリ終了時は、3秒待たずに即時保存を試みる。
- 保存中に追加変更が発生した場合は、保存完了後に最新状態をもう一度保存する。
- Firestoreから受信した最新版はlocalStorageにも保存するが、同じ内容をFirestoreへ再送信しない。
- Firebase設定、Firestore保存先 `inventory/main`、localStorageキー、IndexedDB、既存データ形式、画面デザイン、LINE共有URLは変更していない。

### 確認事項

- JavaScript構文エラーがないこと。
- 画面表示バージョンと `APP_VERSION` がともに `4.2.1` であること。
- 古いFirestoreデータを受信しても新しい端末データを上書きしないこと。
- 通常保存の3秒デバウンスと、終了時の即時保存が両立していること。

## 11. 2026-08-01対応済み: 在庫管理の安全表示と保存状態

対象: `inventory.html`（対応時 `v4.3.0`）

### 対応結果

- 店舗名、商品名、カテゴリー名をHTMLへ表示するときの特殊文字処理を統一した。
- `'`、`"`、`<`、`>`、`&` を含む名前でも、タブ、編集画面、カテゴリー画面、カテゴリー選択が崩れない。
- カテゴリー名をインライン `onclick` へ直接埋め込まず、イベントリスナーで安全に選択する。
- 画面左下に「端末保存済み」「クラウド保存中」「クラウド保存済み」「オフライン・端末保存済み」「クラウド保存失敗」を表示する。
- オンラインへ戻ったときは、待たずにクラウド保存を再試行する。
- Firebase設定、Firestore保存先 `inventory/main`、localStorageキー、IndexedDB、既存データ形式、画面構成、LINE共有URLは変更していない。

### 確認結果

- JavaScript構文エラーなし。
- 特殊文字の変換テストに合格。
- 古いFirestoreデータの上書き防止テストに合格。
- クラウド保存中・保存済み・オフライン表示の切り替えテストに合格。
- `APP_VERSION` と画面表示バージョンはともに `4.3.0`。

## 12. 2026-08-01対応済み: 在庫管理の軽量化と更新表示

対象: `inventory.html`（対応時 `v4.3.1`）

### 対応結果

- ヘッダーに埋め込まれていた1024px・約30KBのJPEGデータを削除した。
- ヘッダーは正式アイコン `/icons/inventory-32.png?v=3` を使用する。
- HTML内の画像形式指定と実データ形式の不一致を解消した。
- 更新通知を実際の動作に合わせて「最新版に更新されました」「確認」へ変更した。
- 保存データ、Firebase、Firestore、localStorage、IndexedDB、画面機能、LINE共有URLは変更していない。

### 確認結果

- JavaScript構文エラーなし。
- HTML内の埋め込み `data:image` は0件。
- 正式ヘッダーアイコン参照を確認。
- `APP_VERSION` と画面表示バージョンはともに `4.3.1`。

## 13. 2026-08-01対応済み: 在庫管理の複数端末同期

対象: `inventory.html`（対応時 `v4.4.0`、`v4.5.0`へ統合して公開）

### 対応結果

- Firestoreの保存を単純な全件上書きからトランザクション保存へ変更した。
- 2台の端末が別の商品をほぼ同時に変更した場合、双方の変更を商品ID単位で統合して残す。
- 同じ商品を同時に変更した場合は、保存を実行した端末側の変更を優先する。
- 保存処理中に追加変更が発生した場合も、クラウド側の別変更を保持して再統合する。
- 店舗、商品、カテゴリーの追加・変更・削除を、最後に取得したクラウド状態との差分として統合する。
- Firebase設定、Firestore保存先 `inventory/main`、localStorageキー、IndexedDB、既存商品IDは変更していない。

### 確認結果

- 別商品の同時変更が両方残ることを確認。
- 商品の追加・削除が別端末の変更と統合されることを確認。
- 同じ商品の競合時に端末側の変更が優先されることを確認。
- 保存中の追加変更が次の保存へ引き継がれることを確認。

## 14. 2026-08-01対応済み: 在庫管理の店舗別構造

対象: `inventory.html`（対応時 `v4.5.0`）

### 採用した構造

- アプリとFirestore保存先は1つのまま維持し、商品データは `storeId` ごとに独立させる。
- 大須・那古野・鉄板は、他の2店舗にある商品名を「商品候補」として利用できる。
- 商品候補から追加すると、在庫数は0から開始し、仕入値・単位は参考値を選べる。追加後の価格、在庫数、カテゴリー、完了状態は店舗ごとに独立する。
- 鎌倉は3店舗の商品候補へ混ぜず、鎌倉で以前使用した商品の再追加だけに限定する。
- 商品を完全削除せず「この店舗では使用しない」にすると `active:false` で保持し、通常の棚卸、集計、Excel出力から除外する。後から商品候補より再追加できる。
- 既存834商品を調査した結果、大須・那古野・鉄板では商品名の重複が155件あり、そのうち59件は店舗間で価格が異なったため、商品名候補だけ共有して価格を共通化しない構造とした。
- 鎌倉と他3店舗の商品名重複は4件だけだったため、鎌倉は独立候補とした。
- 店舗ごとにHTMLやFirestore文書を4つへ分裂させていない。共通修正を1回で全店舗へ反映できる。

### 店舗固定URLの仕様

- `?store=osu`、`?store=nagono`、`?store=teppan`、`?store=kamakura` を正式対応する。
- 日本語の店舗名指定も内部的に解決できる。
- 固定URLでは他店舗タブ、全店舗合計、全店舗一括出力、全店舗一括ゼロ操作を表示しない。
- LINE共有は、現在開いている店舗の固定URLをコピーする。
- 共通manifestからホーム画面起動した場合も、端末に記憶した固定店舗を開く。

### 確認結果

- JavaScript全スクリプトの構文エラーなし。
- 大須・那古野・鉄板の商品候補へ鎌倉商品が混ざらないことを確認。
- 商品候補追加後も元店舗の商品・価格・在庫数が変わらないことを確認。
- 非表示商品が通常集計から除外され、商品候補から在庫数0で再追加できることを確認。
- `active:false` と別商品の同時更新がトランザクションで両方残ることを確認。
- `APP_VERSION` と画面表示バージョンはともに `4.5.0`。

## 15. 2026-08-02対応済み: 在庫スプレッドシート安全連携

対象ファイル:

- `inventory-spreadsheet-sync.gs`
- `INVENTORY-SPREADSHEET-SYNC.md`

### 現在の状態

- Googleスプレッドシートには元データの「大須」「那古野」「鉄板」「鎌倉」があり、これら4シートと既存の数字は変更しない。
- Apps Scriptの初期設定と手動同期は実行済みで、`連携_大須`、`連携_那古野`、`連携_鉄板`、`連携_鎌倉`、`Zaikon連携設定` が作成されている。
- 鎌倉だけ元シートの列順が異なっていたため、固定列番号を廃止し、見出し名から列を検出するよう修正した。`#######` 表示の問題は解消済み。
- GitHub mainの最新版には、年1回運用向けの年度別確定保存を追加済み。ただしユーザー側Apps Scriptへこの最新版を再貼り付ける作業は未実施。

### 安全同期と年度保存の仕様

- 通常の書き込み先は連携4シートだけ。元の4店舗シートは保管場所・税率・備考の読み取りだけに使用する。
- Firestore `inventory/main` をREST APIで読み、店舗ID 1=大須、2=那古野、3=鉄板、4=鎌倉として分ける。
- 連携シートの棚卸額8%／10%は数式で計算する。
- 年度は在庫アプリで入力した西暦4桁を使用する。
- 年度確定保存では `2026大須`、`2026那古野`、`2026鉄板`、`2026鎌倉` のような4シートを値固定で作成する。
- 同じ年度の保存先が1つでも存在する場合は4店舗すべての作成を中止し、既存年度を上書きしない。
- 現在の元4シートは「④ 元4シートを年度名でコピー保存」で、元シートを変更せず `2025大須`、`2025那古野`、`2025鉄板`、`2025鎌倉` として固定保存できる。
- 年度確定4タブの作成がすべて成功した後、連携4シートは翌年度用に初期化する。商品ID・物品名・カテゴリ／仕入先・保管場所・単位を残し、単価・数量・税率・棚卸額・更新日・備考・状態を空にする。
- 確定タブは固定値で自動更新しない。翌年度の確定タブはその年度の棚卸が確定した時点で初めて作成する。
- 年1回運用のため、1時間ごとの自動同期は使用しない。既存トリガーがあれば「自動同期を停止（年1回運用）」で削除する。
- スプレッドシートからFirestoreへ書き戻す処理は作らない。

### 次の作業

1. Google Apps Scriptの `コード.gs` をGitHub mainの `inventory-spreadsheet-sync.gs` 全文へ貼り替える。
2. スプレッドシートを再読み込みし、「Zaikon連携」→「自動同期を停止（年1回運用）」を1回押す。
3. 最初に「④ 元4シートを年度名でコピー保存」を押して `2025` と入力し、2025年3月の元データを4店舗分固定保存する。
4. アプリで `2026` を入力して棚卸し、作業中は「② 連携シートへ安全同期」で確認する。
5. 全店舗確定後だけ「③ 入力した年度で4店舗を保存」を押し、`2026大須` など4タブを固定保存する。
6. その後、アプリの管理者設定で「翌年度を開始（単価・数量をリセット）」を押す。アプリは自動で2027年度になり、商品マスターを残して単価・数量・入力済み状態をリセットする。

## 16. 2026-08-02対応済み: 在庫管理の年度・入力済み表示

対象: `inventory.html`（現在 `v4.8.0`）

### 対応結果

- 棚卸年を西暦4桁で必須入力し、全店舗・Firestore・Excel・年度履歴・スプレッドシート保存で共通利用する。
- アプリ上部へ「2026年 棚卸」のように常時表示し、未入力時は「年度未設定」と目立つ表示にする。
- 年度未入力のまま棚卸完了、Excel出力、ゼロリセット、スプレッドシート送信は実行できない。
- 数量を入力した商品は自動で完了扱いにし、カード・表の行を薄グレー、状態を「入力済み」にする。
- 数量が前回と同じでも再入力すれば入力済みになる。取り消しでは数量と以前の完了状態を一緒に戻す。
- 上部の戻るボタンは常に押せる。戻せる操作がない場合は案内を表示し、入力後は直前の数量と入力済み状態を戻す。
- 入力済み商品は一覧の下側へ並び、ゼロリセット時は次年度に備えて完了状態も解除する。
- 管理者の「翌年度を開始」は、管理者パスワード・最終確認・安全バックアップ成功後だけ実行する。年度を自動で1年進め、全商品の単価・数量・入力済み状態・更新日時をリセットし、商品名・カテゴリ／仕入先・単位・店舗・商品IDは保持する。
- 西暦入力欄を手で変更しただけでは単価・数量を消さない。誤操作による全件消去を防止する。
- 設定画面の通常表示は「LINEで共有する」「スプレッドシートに送信」の2操作に整理し、従来の管理機能は「詳細設定」に折りたたむ。
- バックアップ保存・復元・写真整理は管理者だけに表示する。復元は管理者パスワードと最終確認が必要。
- 復元前に店舗ID、商品ID、数量、単価、年度、カテゴリー、年度別データ、履歴の形式を検査し、異常なファイルは拒否する。
- バックアップには店舗・商品・数量・単価・年度・カテゴリー・アプリ内年度別データ・履歴を含める。端末内の写真は含めない。
- Googleドライブへの自動保存と誤解される導線、および保存後に店舗削除が表示される不要な画面を削除した。
- 「スプレッドシートに送信」は最新データをFirestoreへ即時保存する操作。実際のシート取込はスプレッドシート側の「② 連携シートへ安全同期」で行う。
- 編集者モードと年度欄の補足説明を削除し、年度と入力欄だけを簡潔に表示する。
- 「棚卸完了」は商品一覧の操作列から設定画面へ移動し、管理者・編集者だけが実行できる。
- カテゴリー検索、カテゴリー管理、商品追加・編集、商品候補の「閉じる／戻る」は右上へ統一する。
- 管理者と編集者をURLで分離し、画面上の管理者・編集者・閲覧者の切替ボタンを廃止する。
- 編集者URLは暗証番号なしで開き、管理者URLだけ管理者パスワードを要求する。旧入室パスワード・アクセスパスワードは使用しない。
- LINE共有でコピーするURLは、現在の店舗に固定した編集者URLとする。管理者URLを誤ってスタッフへ共有しない。
- Firebase設定、Firestore保存先 `inventory/main`、既存商品ID、元の4店舗シート、店舗を固定する `store` 値は変更していない。

### 確認結果

- HTML内の全JavaScriptとApps Scriptの構文エラーなし。
- 同じ数量の再入力で完了になること、取り消しで元の完了状態へ戻ることを確認。
- 年度保存は保存先の事前確認を行い、既存年度がある場合はシート作成前に中止することを確認。
- 翌年度開始で2026から2027へ進み、単価・数量・完了状態だけが初期化され、商品名・仕入先／カテゴリー・単位が保持されることを確認。
- `APP_VERSION` と画面表示バージョンは `4.8.0`。

## 17. 2026-08-03 対応済み: スタッフ専用URLと削除時の完全無効化

### 対応結果

- スタッフ全員へ同じ共通URLを配る方式から、スタッフごとの専用URLへ切り替える。
- オーナー画面でスタッフごとの専用URLを発行・コピーできるようにする。
- オーナーが名前を削除したら、そのスタッフの専用URLを即時無効化する。
- 無効化された古いURLでは、希望入力・シフト閲覧・クラウド保存を一切できない。「このURLは利用停止されています」と表示する。
- 他スタッフの専用URLとオーナーURLには影響させない。
- スタッフ画面では、URLに紐づく本人以外の名前・店舗へ切り替えられないようにする。
- まだ運用前で、現行のスタッフ共通URLは誰にも配布していない。移行期間は不要で、専用URL公開と同時に共通URLのスタッフ利用を停止してよい。
- 正式バージョンはシフト・スタッフ `v1.57`、シフト・オーナー `v1.65`。
- オーナーはGoogle認証、スタッフはFirebase匿名認証を使用する。両画面のFirebaseアプリ名を分離し、認証状態が干渉しないようにした。
- Firebase AuthenticationでGoogle・匿名プロバイダを有効化し、承認済みドメインへ `zaiko-app-two.vercel.app` を登録済み。匿名アカウントの自動クリーンアップも有効化済み。
- `database.rules.json` と `firebase.json` を追加し、Realtime Databaseルールを本番公開済み。`zaiko/shift` は認証とトークンで保護し、既存の在庫・売上保存先は従来の読み書きを維持した。
- 名簿画面は画面高以内に収まり、スタッフ一覧だけ縦スクロールする。通常版と携帯互換版へ同じ修正を適用済み。

### 解決した問題

- 現在のスタッフURL `/shift/` は全員共通で、名前の選択によって本人を決めている。
- オーナーの表で名前を削除しても、名簿 `roster` に名前が残る場合がある。
- スタッフ端末のlocalStorageに以前の名前が残り、一覧にない名前を再追加する処理がある。
- `ensureStaff()` が削除済みスタッフを再作成できるため、現在は名前削除だけでは利用停止にならない。
- `ensureStaff()`、localStorageの旧名復元、共通URLによる再作成経路を止め、削除済みスタッフを復活できないようにした。

### 本番確認結果

1. オーナー `v1.65`へGoogle管理者ログインでき、既存名簿・既存シフトを表示できることを確認した。
2. テストスタッフ「動作確認」の専用URLを発行し、名前・店舗が固定された状態で開けることを確認した。
3. 専用URLで一日OKを保存し、オーナー画面の「動作確認」行へ `9:00-23:00` が反映されることを確認した。
4. オーナー画面で「動作確認」を削除後、同じ専用URLを再読み込みすると「このURLは利用停止されています。入力・閲覧・保存はできません。」となることを確認した。
5. トークンなしの共通スタッフURL `/shift/` も同じ停止画面になり、入力・閲覧・保存できないことを確認した。
6. 未認証のRESTアクセスで `zaiko/shift` が401、既存の `zaiko/items` と `zaiko/sales` が200のままであることを確認した。
7. `shift/index.html` と `shift-staff.html`、`shift/owner.html` と `shift-owner.html` はそれぞれ完全一致し、本番でもスタッフ `v1.57`、オーナー `v1.65` を確認した。
8. GitHub Actions「Zaikon app safety check」は専用URL・認証・失効・ルール・通常／互換版同期を含めて成功した。

### 運用上の注意

- スタッフには必ず本人の「専用URLをコピー」で取得したURLだけを送る。共通URLは使用しない。
- 専用URLは本人用の鍵として扱う。通常は個別配布を推奨する。店舗LINEグループへ一覧共有する場合は、全員が他スタッフのURLも開けることを承知のうえ、所属店舗のグループ内だけで扱う。
- 名前を削除すると、その人の希望データと専用URLが失効する。削除前に対象名を必ず確認する。
- 匿名認証の自動クリーンアップで古い匿名アカウントが削除されても、有効な専用URLは再度開くと新しい匿名認証で利用を開始できる。
- Realtime Databaseルールを再び全面 `true` に戻してはいけない。変更時は `database.rules.json` と本番ルールを一致させる。

### チャットの長さに関する運用ルール

- 会話が長くなり、作業履歴や注意点の見落としリスクが高くなる前に、アシスタント側から新しいチャットへの移動を案内する。
- 移動前に、完了内容・未完了内容・正式URL・バージョン・基準コミット・禁止事項をこの引継ぎメモへ更新する。
- 新しいチャットでは、古い添付HTMLではなくGitHub `main` とこの引継ぎメモを基準に再開する。

## 18. 2026-08-03対応済み: 毎日在庫ヘッダー正式アイコン

対象: `index.html`（アイコン対応時 `v10.63`、現在 `v10.64`）

### 対応結果

- ヘッダーに残っていた緑色背景の埋め込みJPEGを削除した。
- ヘッダー画像を正式アイコン `/icons/daily-inventory-32.png?v=4` へ変更した。
- 正式アイコンは、水色背景・白い牛乳パック・青い「MILK」のデザイン。
- `APP_VERSION` を `v10.63`、更新日を `2026-08-03` に更新した。
- Firebase設定、保存先、保存キー、在庫・販売・入荷・温度・わらび餅の各機能、OGP、manifest、正式URLは変更していない。

### 確認項目

- ヘッダーが正式アイコンを参照していること。
- 旧ヘッダーの埋め込みJPEGが削除されていること。
- 本番URLで `v10.63 2026-08-03` と正式アイコンが表示されること。
- GitHub Actionsの自動検査が成功すること。

## 19. 2026-08-03対応済み: 売上入力の携帯インストールボタン

対象: `sales-input.html`（現在 `v2.69`）

### 対応結果

- LINE内ブラウザ、iPhone Safari、Android Chromeを含むすべての携帯で、緑の「ホーム画面に追加」ボタンを常時表示する。
- Android Chromeなど直接インストールに対応する環境では、ボタンからブラウザ標準のインストール画面を開く。
- LINE内ブラウザとiPhoneでは、ボタンからSafari／Chromeで開いてホーム画面へ追加する手順を表示する。
- LINE判定へ `LIFF` を追加し、iPadOSのデスクトップ形式ユーザーエージェントもiOSとして判定する。
- URLに残っていた古い更新番号 `v=2.20` を現在の `v=2.69` に更新した。
- インストール済みのホーム画面アプリでは追加ボタンを表示しない。
- 売上データ、履歴、下書き、Firebase、GAS URL、パスワード、店舗固定、LINE共有URL、manifest、Service Workerは変更していない。

### 確認項目

- Android版LINE、iPhone版LINE、iPhone Safari、Android Chromeでボタン表示条件が成立すること。
- 直接インストールできない環境では外部ブラウザで開く手順が表示されること。
- 本番URLで `v2.69` と新しいボタン表示処理が公開されること。
- Vercelの公開処理が成功すること。

## 20. 2026-08-10対応済み: 店舗別スタッフ専用URL一覧コピー

対象: `shift/owner.html` と `shift-owner.html`（`v1.66`）

### 対応結果

- 名前登録画面に、選択中店舗の全スタッフ分をまとめてコピーするボタンを追加した。
- コピー内容は、スタッフ名の次の行に本人専用URLを置き、スタッフごとに空行を入れる形式で、店舗LINEグループへまとめて貼り付けられる。
- 一覧の先頭に、本人の名前のURLだけを開き、他スタッフのURLを開かないよう注意書きを付ける。
- 各URLは従来の推測困難なランダムトークンを使用し、名前と店舗の固定、スタッフ削除時の即時無効化を維持する。
- Firebase設定、Realtime Databaseルール、保存先、保存キー、既存シフトデータ、オーナーURL、スタッフ画面は変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- HTML内の全JavaScriptに構文エラーがないこと。
- 選択中店舗だけの名簿から一覧を作ること。
- スタッフ名と専用URLが1行ずつ対応していること。
- 個別の「専用URLをコピー」と削除時のURL無効化が残っていること。
- GitHub ActionsとVercel本番公開が成功すること。

## 21. 2026-08-10対応済み: 全アプリのGitHub記録・安全確認

### 確認結果

- 私の管理、毎日在庫、在庫管理、売上入力、売上オーナー、シフト・スタッフ、シフト・オーナーの正式HTMLは、すべてGitHub `main` に記録済み。
- シフト・スタッフの `shift/index.html` と `shift-staff.html`、シフト・オーナーの `shift/owner.html` と `shift-owner.html` は、それぞれ同一内容・同一バージョン。
- 7つのwebmanifest、Service Worker、各アプリの正式16/32/180/192/512pxアイコン、専用OGP画像は記録済み。
- `firebase.json`、`database.rules.json`、`OFFICIAL-STRUCTURE.md`、`CLAUDE.md`、本ファイル、在庫スプレッドシート連携コードと説明書は記録済み。
- Vercel本番で、私の管理 `v1.5`、毎日在庫 `v10.64`、在庫管理 `v4.8.0`、売上入力 `v2.71`、シフト・スタッフ `v1.57`、シフト・オーナー `v1.66` を確認。
- スタッフ共通URLは利用停止表示となり、通常版と携帯互換版の表示バージョンが一致することを確認。
- `sw.js` のナビゲーション時 `cache: 'no-store'` と全manifestの正式 `start_url` をVercel本番で確認。
- デスクトップショートカット「私の管理」は正式URL `https://zaiko-app-two.vercel.app/my-management.html` を開く設定。

### 今回補った記録・検査

- `OFFICIAL-STRUCTURE.md` と本ファイルの古いバージョン記載を、GitHub実ファイルとVercel本番へ一致させた。
- GitHub Actionsの対象へ `inventory.html`、全webmanifest、正式アイコン、運用資料を追加した。
- 在庫管理を含む全正式HTMLについて、ファイル、OGP、manifest、バージョン後退を検査する。
- manifestのJSON形式、`start_url`、参照アイコン、および正式アイコン全サイズの欠落を自動検出する。
- アプリ本体、Firebaseルール、保存先、保存キー、LINE共有URL、既存データ、シフト機能は変更していない。

### 継続して注意する安全事項

- Realtime Databaseの `zaiko/items`、`zaiko/sales` など在庫・売上系保存先は、従来仕様として認証なし読み書きが残っている。シフト保存先だけは認証と専用トークンで保護済み。
- 毎日在庫には画面側の初期暗証番号、売上入力には店舗パスワード取得処理がある。公開HTML内の暗証番号を強い秘密情報として扱うことはできない。
- シフト・スタッフHTMLには非表示の旧オーナー画面コードが残るが、Firebaseルールにより匿名スタッフはオーナー権限を持たない。分離コードの整理は、シフト機能へ影響するため別作業として慎重に行う。
- 上記3点は認証方式・保存データへ影響するため、今回のGitHub記録作業では変更していない。
- GitHubへ、発行済みスタッフ専用URL、アクセストークン、実データ、個人情報、データベースバックアップ、秘密鍵、サービスアカウントJSON、`.env` を追加しない。

## 22. 2026-08-10対応中: 在庫・売上系の段階的認証（第1段階）

### 第1段階で追加する内容

- 毎日在庫 `v10.65` と売上入力 `v2.72` に、推測困難な店舗専用URLを検証する匿名Firebase認証を追加する。
- トークンなしの既存URLとインストール済みアプリは、移行中に止めないため従来動作を維持する。
- 売上オーナーはシフト・オーナーと同じ `zaiko/shift/access/admins` の複数Google管理者だけが開ける。
- 売上オーナーから店舗別売上入力URLと鎌倉の毎日在庫URLを発行・再発行できる。
- 旧互換 `owner.html` は、クエリ文字列とハッシュを保持して正式な `sales-owner.html` へ転送する。
- `database.rules.json` に `zaiko/app_access` のトークン・店舗対応・匿名セッション規則を追加する。

### 移行中の安全条件

- `zaiko/history`、`zaiko/items`、`zaiko/sales`、`zaiko/temp_records`、`zaiko/warabi_master`、`zaiko/warabi_records` は、第1段階では認証なし読み書きを閉じない。
- 理由は、毎日在庫・売上入力の旧URLおよびリポジトリにソースがないGoogle Apps Script／スプレッドシート連携を先に止めないため。
- 専用URLを全対象端末で確認し、Google Apps Script側へ認証を追加してスプレッドシートからの記入・参照を確認した後にだけ、上記保存先をFirebaseルールで閉じる。
- 発行済み専用URL、トークン、管理者UID、実データはGitHubへ記録しない。

### 完了前の確認項目

- Firebase Authenticationで匿名認証とGoogle認証が利用可能であること。
- 本番Realtime Databaseへ `app_access` 規則を反映した後、売上オーナーの全管理者がログインできること。
- 店舗別専用URLで店舗固定となり、保存・再表示できること。再発行後は旧URLが停止すること。
- トークンなしの現在のデスクトップ／携帯アプリが、移行中は従来どおり開いて保存できること。
- GitHub ActionsとVercel公開が成功すること。

### 2026-08-10 第1段階の進捗

- Realtime Database本番へ `zaiko/app_access` 規則を公開済み。移行中の安全条件にある6つの既存保存先は、認証なし読み書きを閉じずに維持している。
- Firebase匿名認証、売上オーナーのGoogle管理者ログイン、4店舗の売上入力専用URL、鎌倉の毎日在庫専用URLを実機で確認済み。
- 店舗URLの再発行後、旧URLが `PERMISSION_DENIED` で停止し、新URLが開くことを確認済み。
- 売上入力GASは既存デプロイURLを維持したままバージョン9、日報GASは既存デプロイURLを維持したままバージョン6へ更新した。どちらも `requireForAllRequests: false` の第1段階で、Firebase IDトークンが届いた場合は必ず検証し、トークンなしの従来通信は引き続き許可する。
- 売上入力 `v2.73` は、専用URLの匿名Firebase IDトークンを売上入力GASと日報GASへ渡す。従来URLでは通信内容を変えない。
- 鎌倉店の専用URLを実機で開き、`v2.73` のFirebase受信と、認証付き売上入力GASによる2026-08-08のスプレッドシート既存データ読み込みを確認済み。
- 売上入力GASと日報GASの両プロジェクトで、`UrlFetchApp` の外部通信権限をGoogle管理アカウントから付与し、権限確認用関数の実行完了を確認済み。
- 既存値を変更せずに鎌倉店専用URLから上書き保存し、売上入力GASがエラーなく完了した。鎌倉店スプレッドシートの対象行で売上値と担当者欄を確認し、認証付きの日報GAS書き込みも完了した。重要伝達事項の未入力時は空欄で正常。
- 専用URL導入前からデスクトップに保存されている売上入力 `v2.71`（トークンなし）で、鎌倉店の既存データ読み込みと、値を変更しない上書き保存が成功した。第1段階の従来URL・既存端末互換を確認済み。
- 専用URL導入前からデスクトップに保存されている毎日在庫 `v10.64`（トークンなし）で、温度・わらび餅・本日の在庫の既存記録と「同期済み」「本日入力完了ずみ」を確認した。重複履歴を避けるため再保存は行わず、従来端末の読み込み・同期維持を確認済み。
- 売上オーナーから発行済みの毎日在庫専用URLを実機で開き、正式版 `v10.65`、匿名Firebase接続、「同期済み」表示、既存在庫の読み込みを確認した。重複履歴を避けるため再保存は行っていない。
- コードと本番相当ルールを再確認した結果、専用URL版は匿名認証する一方、旧売上入力 `v2.71` と旧毎日在庫 `v10.64` はトークンなし・Firebase未認証のまま既存保存先へ接続する。温度・わらび餅も旧毎日在庫と同じ保存経路を使用するため、6つの公開規則を現時点で閉じると確認済みの旧アプリが停止する。第1段階では規則を閉じないと判断した。
- 2026-08-11、確認用Windowsパソコンで、店舗専用URL版の毎日在庫 `v10.65` と鎌倉店売上入力 `v2.73` をEdgeアプリ（PWA）としてインストールした。デスクトップの新しい正式アイコンから再起動し、専用URL版が開くことを実機確認した。
- 同じパソコンのデスクトップに「私の管理」「在庫管理」「毎日在庫」「売上入力」「売上オーナー」「シフト・オーナー」の新しい正式アイコンが揃ったことを確認した。旧ローカルHTML／旧Edgeショートカットは新旧を取り違えないよう個別確認して整理中で、一括削除はしない。
- 上記は1台の確認用パソコンでの置き換え確認であり、他店舗・スマートフォン・他パソコンの移行完了を意味しない。温度・わらび餅を含む全実運用端末の確認が終わるまでは、6つの公開規則とGASの段階移行設定を維持する。
- GitHubコミット `2b5d938cac4ccc8ff58f323684bf942c6707a24f` とVercelの2つの公開結果はいずれも成功。
- **未完了:** 旧デスクトップアプリを専用URL版へ安全に置き換え、実運用端末と温度・わらび餅を含む全連携経路を認証対応へ移行する。旧版を廃止できる状態になった後にだけ、必須認証への切替可否を判断する。
- **禁止:** 旧デスクトップアプリと温度・わらび餅を含む連携経路の移行が終わるまでは、既存保存先の公開規則を閉じず、GASの `requireForAllRequests` を `true` にしない。

## 23. 2026-08-11対応: シフト編集者専用URL

対象: `shift/owner.html`、`shift-owner.html`、`database.rules.json`（`v1.67`）

### 対応内容

- オーナー画面へ「編集者」ボタンを追加し、編集者1・編集者2の2人分の専用URLを個別に発行できるようにした。
- 編集者はGoogleアカウント登録なしで、推測困難な専用URLとFirebase匿名認証を使って開く。
- 編集者は、シフト作成・編集・コピー・要請・公開・印刷を利用できる。
- 編集者画面では、名前登録、スタッフ削除、スタッフ専用URL発行、全データ消去を表示しない。
- Realtime Databaseルールでも、編集者による名簿変更、スタッフ新規作成、スタッフ削除、氏名・所属店舗の変更、アクセストークン操作を拒否する。
- 2人のURLは別トークンで管理する。「再発行」した枠の古いURLだけを停止できる。
- 既存のGoogle管理者、スタッフ専用URL、スタッフ削除時のURL失効、既存シフトデータ、在庫・売上系の段階移行設定は変更しない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にする。

### 公開前後の確認項目

- GitHub Actions「Zaikon app safety check」が成功すること。
- Firebase本番へ更新後の `database.rules.json` を反映すること。
- オーナーから編集者1・編集者2のURLを発行できること。
- 編集者URLで既存シフトの読み込み、編集、コピー、要請、保存ができること。
- 編集者画面に名前登録・削除・全消去が出ず、直接通信でも名簿とスタッフ作成・削除が拒否されること。
- 片方を再発行すると、その枠の旧URLだけが停止し、もう片方とオーナー・スタッフURLは動き続けること。

### 2026-08-11 本番確認状況

- Firebase本番ルールの公開は完了している。
- 編集者1の専用URLはInPrivate実機確認まで完了している。
- 編集者1・編集者2の専用URLは、まだ他の人へ送っていない。
- 発行済みURLとトークンはGitHubへ記録しない。

## 24. 2026-08-11対応済み: LINE用スタッフURL一覧の整形

対象: `shift/owner.html` と `shift-owner.html`（`v1.68`）

### 対応結果

- 「店舗全員のURLをまとめてコピー」は、従来どおり1回の操作で全員分をコピーする。
- LINEへ貼り付けたとき、各スタッフを「名前」「次の行にURL」「空行」の順で表示する。
- スタッフ専用URLの発行・失効、名簿、シフトデータ、Firebase設定、Realtime Databaseルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- コピー文字列がスタッフごとに名前・URL・空行の順になること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 25. 2026-08-12対応済み: シフト・オーナーの専用ポップアップ

対象: `shift/owner.html` と `shift-owner.html`（`v1.69`）

### 対応結果

- ブラウザ名が出る標準ポップアップを廃止し、シフト画面の黄色・ピンクに合わせたアプリ専用デザインへ変更した。
- シフト公開・公開取消、スタッフ削除、全データ消去・復元、編集者URL再発行の確認画面を統一した。
- 完了・お知らせ・エラー表示も同じデザインへ統一し、内容に応じてアイコンと色を切り替える。
- URLの自動コピーが使えない場合は、コピー対象を読みやすい専用画面内に表示する。
- 「キャンセル」「実行」ボタンと右上の閉じるボタンを設け、パソコン・携帯の両方で押しやすくした。
- 確認後に行う公開・削除・復元・再発行などの既存処理は変更していない。
- Firebase設定、Realtime Databaseルール、既存シフトデータ、発行済みURL、在庫・売上系の公開ルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- ブラウザ標準の `confirm`、`alert`、URL表示用 `prompt` が残っていないこと。
- 公開確認で「キャンセル」は変更せず、「公開する」だけが従来処理を実行すること。
- 危険な操作はピンク・赤系、通常の公開は黄色系で表示すること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 26. 2026-08-12対応済み: スタッフURLの名前付き個別送信

対象: `shift/owner.html` と `shift-owner.html`（`v1.70`）

### 対応結果

- 名簿の「専用URLをコピー」は、URLだけでなく「スタッフ名＋改行＋本人専用URL」をコピーする。
- 名前登録画面へ「1人ずつコピーして送る」を追加した。
- 順番式操作では、1人分をコピーするたびにLINEへ貼り付けて送信し、アプリへ戻って「次の人をコピー」を押す。
- 最後のスタッフでは「完了」を表示する。途中で「終了」または右上の閉じるを押すと安全に中止できる。
- LINEの仕様上、1回の貼り付けを自動で複数の吹き出しへ分割できないため、別々の吹き出しにする場合はこの順番式操作を使用する。
- 従来の「店舗全員のURLをまとめてコピー」も残し、1つのメッセージで送りたい場合に使用できる。
- スタッフ専用URLの発行・失効、名簿、既存シフトデータ、Firebase設定、Realtime Databaseルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- 個別コピーがスタッフ名と本人専用URLの2行になること。
- 順番式操作が選択中店舗の名簿順で1人ずつ進むこと。
- キャンセル時に次のスタッフへ進まないこと。
- 従来の全員まとめてコピーが残っていること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 27. 2026-08-12対応済み: スタッフ専用URLの個別再発行

対象: `shift/owner.html` と `shift-owner.html`（`v1.71`）

### 対応結果

- 名前登録画面の各スタッフ行へ「新しいURL」ボタンを追加した。
- 確認画面で実行した場合だけ、そのスタッフの古いURLを利用停止にし、新しいランダムトークンの専用URLを発行する。
- 新しいURLは「スタッフ名＋改行＋本人専用URL」の形式で自動コピーする。
- 再発行した本人以外のスタッフURL、編集者URL、オーナーURLには影響しない。
- 通常の「URLコピー」、店舗全員まとめてコピー、1人ずつコピーして送る機能は残している。
- Firebase設定、Realtime Databaseルール、既存シフトデータ、名簿は変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- 「新しいURL」で必ず確認画面が表示されること。
- 再発行後は旧URLだけが停止し、新URLが有効になること。
- 他スタッフのURLと既存シフトデータへ影響しないこと。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 28. 2026-08-12対応済み: スタッフ専用URL停止の確認番号

対象: `shift/index.html` と `shift-staff.html`（`v1.58`）

### 現在の状況

- オーナー `v1.71` の「新しいURL」で店長のURLを実際に再発行したが、新しいURLでも「このURLは利用停止されています」と表示された。
- スタッフ `v1.58` で同じ新URLを再確認し、確認番号 `M1`（シフト公開情報 `meta` の読取拒否）を確認した。匿名認証・セッション保存・URLトークン読取と有効性確認までは成功している。
- `database.rules.json` のスタッフセッショントークン参照9箇所を、Firebase公式例と同じスラッシュ連結の明示パス形式へ変更した。在庫・売上・温度・わらび餅の6公開ルール、既存データ、既存URLは変更していない。
- 2026-08-12、修正済みルール全文をFirebase Realtime Database本番へ公開した。画面から「公開されていない変更」の帯が消えたことを確認済み。次は同じ店長URLで実機確認する。
- 公開後も `M1` が継続した。原因は、スタッフ認証時には存在しない編集者セッション値を先に `.child()` へ渡す式が、スタッフ許可判定より前に失敗すること。編集者1はセッションが存在するため通り、スタッフ全員だけ失敗する状況と一致する。
- `database.rules.json` の編集者セッショントークン参照9箇所も、空値でエラーにならない明示パス形式へ修正した。
- 2026-08-12、この2回目の修正版ルールもFirebase Realtime Database本番へ公開し、「公開されていない変更」の帯が消えたことを確認済み。次は同じ店長URLで再確認する。
- 2回目の公開後も `M1` が継続。明示パス形式でも空値を文字列へ組み立てる前に型エラーとなるため、編集者セッション9箇所・スタッフセッション5箇所すべてに `.isString()` の先行ガードを追加した（コミット `fa53c26ff6a87f851840c1c4cb3dfc11e24efa55`）。
- 2026-08-12、この3回目の修正版ルールをFirebase Realtime Database本番へ公開した。同じ店長の新URLで `M1` が解消し、店舗「大須」・名前「店長」が固定されたカレンダー画面の読込成功をWindows Edge実機で確認した。
- スタッフ専用URLの匿名認証・セッション保存・トークン確認・公開情報読取・本人データ読取は正常。
- 店長URLで2026年8月31日にテストの「×」を保存し、5秒後のブラウザ再読込でも残ることをWindows Edge実機で確認した。その後「この日の希望を解除」を実行し、「×」が消えてテストデータが残っていないことも確認済み。
- 店長URLについて、閲覧・入力・保存・再読込・解除までの実機確認が完了した。
- 続けてヒナキの既存専用URLを別ウィンドウで開き、店舗「大須」・名前「ヒナキ」が固定されたカレンダー画面の読込成功を確認した。
- 別スタッフの既存URLも有効なため、店長以外の全員分を再発行する必要はない。店長は再発行後の新URLを使用し、他スタッフは現在表示されている既存URLを配布できる。
- Firebase本番ルールは `database.rules.json` と一致しており、在庫・売上・温度・わらび餅の既存公開ルールも維持されている。
- 原因を推測で変更せず、匿名認証・セッション保存・トークン読取・データ読取のどこで停止したかを画面の「確認番号」で判別できるようにした。
- 停止画面に「もう一度確認」ボタンを追加した。これは再読込だけを行い、URLの再発行やデータ変更はしない。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。
- 調査中のため、スタッフURLを他の人へ配布するのは原因解決と実機確認が終わるまで待つ。

### 次の確認

- 店長URLとヒナキの既存URLの実機確認は完了済み。
- 名前付き個別コピーまたは「1人ずつコピーして送る」で配布を開始できる。
- 店長には再発行後の新URL、他スタッフには現在の既存URLを送る。不要な再発行はしない。
- 追加の停止画面が出た場合は「確認番号」を記録する。
- 確認番号に対応する認証段階だけを調査し、Firebaseルールや既存データを推測で変更しない。
- 既存の在庫・売上・温度・わらび餅の公開ルールを閉じない。

## 29. 2026-08-12対応済み: 一日OKの時間表示と携帯カレンダー幅

対象: `shift/index.html` と `shift-staff.html`（`v1.59`）

### 対応結果

- 入力ポップアップの「◎」は従来どおり一日OKとして保存する。
- スタッフカレンダーでは「◎」ではなく `9:00〜23:00` を2行の時間表記で表示する。
- 幅480px以下では7曜日が画面内に収まるよう、カレンダー列を `minmax(0,1fr)` にし、余白・列間隔・マス・文字・店舗表示・上部年月欄を小さくした。
- 画面全体の不要な横スクロールを止めた。
- Firebaseルール、専用URL、認証、既存シフトデータ、保存形式は変更していない。既存の `allday:true` データも時間表記になる。
- Windows Edge実機で、既存の一日OKが各日 `9:00〜23:00` の2行表示へ変わったことを確認済み。携帯縦画面での7列表示は実機確認待ち。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。

### 確認項目

- 「◎」を押した日のカレンダー表示が `9:00〜23:00` になること。
- 携帯の縦画面で日曜から土曜まで7列すべてが欠けずに見えること。
- 日付タップ、月スワイプ、保存、解除が従来どおり動くこと。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 30. 2026-08-12対応済み: 携帯の拡大縮小・曜日・固定名表示

対象: `shift/index.html` と `shift-staff.html`（`v1.60`）

### 対応結果

- カレンダーの通常サイズは変更せず、携帯で2本指を広げると拡大、つまむと縮小できるよう `pinch-zoom` を許可した。
- 幅480px以下の曜日文字を10pxから20pxへ拡大し、太字にした。
- 専用URLで固定された店舗名とスタッフ名は、無効化された選択欄でも薄くならないよう、濃い文字・太字・白背景へ変更した。
- 日付タップ、月スワイプ、保存、認証、既存データは変更していない。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。

### 確認項目

- 携帯で曜日が大きく表示され、7列が画面内に収まること。
- 店舗名と本人名が濃く見えること。
- カレンダー上で2本指の拡大・縮小ができること。
- 1本指の月スワイプと日付タップが従来どおり動くこと。
- GitHub ActionsとVercel本番公開が成功すること。

## 31. 2026-08-12対応済み: 全部一日OKの安全な解除と日付拡大

対象: `shift/index.html` と `shift-staff.html`（`v1.61`）

### 対応結果

- 「全部 ◎ 一日OK」は空欄の日だけに `allday:true, bulkAllDay:true` を保存し、既存の個別時間・「×」・備考を上書きしない。
- 初回のv1.61実機確認では、以前の一括入力に `bulkAllDay` がないため解除されないことを確認した。v1.62では一日OKが月の半数以上ある従来データも一括状態として認識する。
- 一括追加分がある場合はボタンを「全部一日OKを解除」へ変更する。
- 解除ボタンを押すと専用確認ポップアップを表示し、「解除する」を選んだ場合だけ、その月の `allday:true` を解除する。
- 個別に設定した一日OK、個別時間、「×」、備考は解除対象にしない。
- 日付入力ポップアップの見出しを14pxから21pxへ拡大した。
- Firebaseルール、専用URL、認証、確定シフト、既存データは変更していない。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。

### 確認項目

- 空欄と個別入力が混在する月で「全部」を押しても個別入力が維持されること。
- 2回目は確認ポップアップが出て、戻るでは変化せず、解除するで一括追加分だけ消えること。
- 日付ポップアップの見出しが約1.5倍になっていること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 32. 2026-08-12対応済み: 今日の強調・本日のシフト・カレンダー装飾

対象: `shift/index.html` と `shift-staff.html`（`v1.64`）

### 対応結果

- 実際の今日と表示中の年月が一致する場合、今日の日付マスを黄色い太枠・淡い黄色背景・「今日」バッジで強調する。
- 表示中の月に関係なく、カレンダー下へ実際の今日を対象にした「本日のシフト」カードを常時表示する。
- シフト未公開または今日が未確定の場合は「未定」と表示する。本人が希望で「×」を提出していても、確定前は「未定」とする。
- 今日が休みとして確定した場合、または確定済みで勤務時間がない場合は「お休み」と表示する。
- 勤務確定時は所属店舗名と時間を表示し、鉄板応援があれば鉄板と時間も追加表示する。
- カレンダーを淡いピンクのカード調にし、曜日見出し・丸い日付マス・薄い影・日曜の淡いピンク・土曜の淡い水色を追加した。
- 携帯で7列が収まる幅、2本指拡大、月スワイプ、日付タップ、保存、認証、既存データは維持した。
- Firebaseルール、専用URL、確定シフトデータは変更していない。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。
- v1.65として試した7色曜日・水玉・丸い日付・花付きカードは、ユーザー確認により取り消し、v1.64のデザインへ復帰した。

### 確認項目

- 今日のマスが黄色い枠と「今日」で分かること。
- カレンダー下の本日のシフトが「未定」「お休み」または店舗・時間で正しく表示されること。
- 携帯で7列が画面内に収まり、かわいい装飾と大きい曜日が見えること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 33. 2026-08-12対応済み: 2026年9月開始と終了月の自動ロック

対象: `shift/index.html` と `shift-staff.html`（`v1.66`）

### 対応結果

- スタッフカレンダーの初期表示と最初に選べる月を2026年9月に変更した。
- 端末に2026年7月・8月の表示履歴が残っていても、起動時に2026年9月へ安全に補正する。
- 2026年7月・8月は年月選択と左右スワイプから開けない。既存の希望・確定シフトデータは削除しない。
- 月が終わると、翌月1日から終了した月を日本時間（Asia/Tokyo）で自動的に「受付終了」へ切り替える。毎月の手動設定は不要。
- 受付終了月は内容を閲覧できるが、日付入力、「全部 ◎ 一日OK」、一括解除、備考変更、要請の承認・拒否を実行できない。
- 画面を開いたまま月をまたいだ場合にも保存直前の再確認で変更を止める。
- 「全部」状態の判定を表示中の月だけに限定し、別月の一日OKがボタン表示へ影響しないようにした。
- スタッフ専用URL、認証、Firebase設定、Realtime Databaseルール、保存形式、既存データ、確定シフト、オーナー画面は変更していない。
- 在庫・売上・温度・わらび餅の既存公開ルールは変更していない。
- 通常版 `shift/index.html` と携帯互換版 `shift-staff.html` は同一内容にした。

### 確認項目

- 初回表示と旧端末保存値が2026年9月より前にならないこと。
- 2026年9月から左へスワイプしても8月へ戻らないこと。
- 翌月1日以降、前月に「受付終了」が表示され、内容を見られる一方で変更保存できないこと。
- 現在月・未来月は従来どおり入力・保存・解除できること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 34. 2026-08-12対応済み: オーナー・編集者の終了月誤編集防止

対象: `shift/owner.html` と `shift-owner.html`（`v1.72`）

### 対応結果

- オーナー画面と編集者専用URLのカレンダー開始月を2026年9月に変更した。
- 2026年7月・8月は年月選択から外し、2026年9月より前へ戻れないようにした。既存データは削除しない。
- 月が終わると、翌月1日から終了した月を日本時間（Asia/Tokyo）で自動的に「受付終了・閲覧のみ」へ切り替える。
- 終了月でも月表・日別表・希望・確定シフト・メモを閲覧および印刷できる。
- 終了月では、月公開／公開取消、日別決定／解除、勤務時間の追加・変更・削除、休み指定、仮ブロック、棒の伸縮、応援配置、要請、店舗休み、店舗メモを変更できない。
- 画面を開いたまま月をまたいでも、各保存操作の直前に終了月を再確認して変更を止める。
- 名前登録・削除・並べ替え、スタッフ専用URL、編集者URL、印刷、設定は月別シフト編集とは別の管理機能として維持した。
- Firebase設定、Realtime Databaseルール、保存形式、既存シフトデータ、発行済みURL、スタッフ画面は変更していない。
- 在庫・売上・温度・わらび餅の既存公開ルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- 初期表示が2026年9月で、2026年7月・8月を選択できないこと。
- 翌月1日以降、前月に「受付終了・閲覧のみ」が表示され、シフトを変更保存できないこと。
- 終了月の閲覧と印刷ができること。
- 現在月・未来月は従来どおり編集・公開できること。
- オーナーと編集者専用URLの両方で同じロックが働くこと。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 35. 2026-08-12対応済み: 印刷プレビューのスタッフ名拡大

対象: `shift/owner.html` と `shift-owner.html`（`v1.73`）

### 対応結果

- 「決定シフト」「希望シフト」のA4横1枚・月間印刷プレビューで、左端のスタッフ名を従来より大きく太く表示するようにした。
- 名前欄を少し広げ、短い名前は大きく、5文字以上・7文字以上の長い名前は段階的に縮小して枠内へ収める。
- 店舗見出し、日付、シフト時間、印刷の用紙サイズと月間1枚レイアウトは維持した。
- Firebase設定、Realtime Databaseルール、保存形式、既存シフトデータ、発行済みURL、スタッフ画面は変更していない。
- 在庫・売上・温度・わらび餅の既存公開ルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- 印刷プレビューの左端でスタッフ名が大きく太く見えること。
- 5文字以上の名前も隣の日付枠へはみ出さないこと。
- 実際のA4横印刷でも月間1枚に収まること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 36. 2026-08-12対応済み: 印刷プレビューの名前を約3倍・中央配置

対象: `shift/owner.html` と `shift-owner.html`（`v1.74`）

### 対応結果

- v1.73の印刷名拡大ではまだ小さいとの実機確認を受け、A4横1枚の月間印刷プレビューの名前をさらに大きくした。
- 2文字程度の短い名前はv1.73の約3倍を基準に表示し、名前欄の左右中央・上下中央へ配置した。
- 名前欄を88pxへ広げ、3文字・4文字・5文字・6文字・7文字以上の順に段階的に縮小して、長い名前も1マス内へ収める。
- 日付・曜日・シフト時間・店舗見出し・A4横1枚レイアウトは維持した。
- Firebase設定、Realtime Databaseルール、保存形式、既存シフトデータ、発行済みURL、スタッフ画面は変更していない。
- 在庫・売上・温度・わらび餅の既存公開ルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- BK、リズ、店長などの短い名前が大きく中央に表示されること。
- 3文字以上の名前も1つの名前欄からはみ出さないこと。
- 実際のA4横印刷でも月間1枚に収まること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 37. 2026-08-12対応済み: 印刷名を「シファト」相当へ統一

対象: `shift/owner.html` と `shift-owner.html`（`v1.75`）

### 対応結果

- v1.74の短い名前が大きすぎるとの実機確認を受け、2〜4文字のスタッフ名を、v1.74で表示された「シファト」と同じ18px基準へ統一した。
- 名前欄の左右中央・上下中央配置と88px幅は維持した。
- 5文字以上の名前だけ文字数に合わせて段階的に縮小し、1つの名前枠内へ収める。
- 日付・曜日・シフト時間・店舗見出し・A4横1枚レイアウトは変更していない。
- Firebase設定、Realtime Databaseルール、保存形式、既存シフトデータ、発行済みURL、スタッフ画面は変更していない。
- 在庫・売上・温度・わらび餅の既存公開ルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- BK、アニサ、シファト、店長など2〜4文字の名前が同じ大きさで中央表示されること。
- 5文字以上の名前は1つの名前欄からはみ出さないこと。
- 実際のA4横印刷でも月間1枚に収まること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 38. 2026-08-14対応済み: 日別画面へ全スタッフの希望時間を横棒表示

対象: `shift/owner.html` と `shift-owner.html`（`v1.76`）

### 対応結果

- 日別画面で、一日OKだけでなく、全スタッフの一日・AM・PM・旧形式の時間希望も薄い「希望」横棒として表示する。
- AM希望は薄い緑、PM希望は薄いピンク、一日希望は緑とピンクの横棒で区別する。
- 希望が未決定の間だけ希望横棒を表示し、決定シフトがある場合は従来どおり決定シフトの横棒を表示する。
- 希望データ、決定シフト、Firebase設定、Realtime Databaseルール、保存形式、既存シフトデータ、発行済みURLは変更していない。
- 在庫・売上・温度・わらび餅の既存公開ルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- 加藤を含む全スタッフのAM・PM・一日希望が、日別画面の時間位置に横棒で表示されること。
- 希望横棒は決定シフトとして保存されず、既存の希望データも変更しないこと。
- 決定済みのスタッフは従来どおり編集可能な決定シフトの横棒を表示すること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 39. 2026-08-14対応済み: 希望シフトを決定状態に関係なく全日表示

対象: `shift/owner.html` と `shift-owner.html`（`v1.77`）

### 対応結果

- `v1.76` では未決定の日だけ希望横棒を表示していたため、決定データがある日は希望が見えない状態だった。
- 全スタッフ・全日について、決定済みかどうかに関係なく、一日・AM・PM・旧形式の時間希望を薄い「希望」横棒で表示する。
- 希望と決定シフトの両方がある日は行を上下2段に広げ、上段へ希望、下段へ決定シフトを表示して両方を確認できる。
- 希望だけの日は従来の行高で希望横棒を表示する。決定時間の編集・伸縮機能は維持する。
- 希望データ、決定シフト、Firebase設定、Realtime Databaseルール、保存形式、既存シフトデータ、発行済みURLは変更していない。
- 在庫・売上・温度・わらび餅の既存公開ルールは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- 特定のスタッフや日付だけでなく、希望があるすべてのスタッフ・すべての日で希望横棒が表示されること。
- 希望と決定が両方ある日は上下2段で両方を確認できること。
- 決定時間の編集・伸縮、休み、要請、水色ブロックが従来どおり動くこと。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 40. 2026-08-14対応済み: 希望横棒を薄いグレーへ統一

対象: `shift/owner.html` と `shift-owner.html`（`v1.78`）

### 正式な作成・公開の流れ

1. スタッフが希望シフトを提出する。
2. オーナーが日別画面で全スタッフの希望を確認する。
3. 希望を見ながら、オーナーが1日ずつ決定シフトを作成する。
4. 全日完成後、オーナーが公開操作を行う。
5. 公開後にスタッフ画面へ確定シフトを表示する。

### 対応結果

- AM・PM・一日を含むすべての希望横棒を薄いグレーへ統一した。
- 希望横棒は「希望」の文字とグレーの破線枠で表示し、緑・ピンクの決定シフトと区別する。
- 希望と決定が両方ある日は、上段の薄いグレーが希望、下段の色付き横棒が決定シフトとなる。
- 希望を自動的に決定へ変換せず、決定入力だけで自動公開もしない。最後にオーナーが公開操作を行う。
- 希望データ、決定シフト、Firebase設定、Realtime Databaseルール、保存形式、既存シフトデータ、発行済みURLは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

## 41. 2026-08-14対応済み: 日別画面の削除操作を明確化

対象: `shift/owner.html` と `shift-owner.html`（`v1.79`）

### 対応結果

- 水色の仮ブロックは、既存の削除処理と×印用CSSがあったが、×要素が画面へ出力されていなかった。
- 水色ブロック右上へ明示的な×を表示し、×または水色ブロック本体のタップでそのブロックだけを削除できるようにした。
- 決定シフトの棒本体またはスタッフ名をタップすると、既存の全体編集画面を開く。画面内の「🗑 消す」でその日の決定シフトを削除できる。
- 時間数字の個別変更、棒の端の伸縮、要請、希望表示、休み、公開の既存操作は維持する。
- 希望データ、決定シフトの既存値、Firebase設定、Realtime Databaseルール、保存形式、既存シフトデータ、発行済みURLは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

## 42. 2026-08-15対応済み: 選んだ決定シフトの線だけを削除・PC日別画面調整

対象: `shift/owner.html` と `shift-owner.html`（`v1.80`）

### 対応結果

- 日別画面で決定シフトの開始・終了時刻をタップしたときに開く個別時間画面へ、「この時間帯だけ削除」を追加した。
- 削除対象は、そのスタッフ・その日・選択した一日／AM／PMの1本だけ。同じスタッフの別時間帯、他スタッフの決定シフト、希望シフトは変更しない。
- 旧形式の開始・終了だけで保存された1本線も、その線だけを削除できる。
- 全体編集画面の従来の削除は「この人のこの日を全部消す」と明記し、実行前に専用確認画面を表示するようにした。
- PCの日別画面で表を高さ方向まで無理に収め、画面自体も横へ広げすぎていたため、表が左側へ小さく寄り、右側に大きな白い余白ができていた。日別画面を表に合う最大1120px幅で中央表示し、横幅を基準に表を調整する。手動で横へ広げた場合は表も一緒に広がり、縦方向は従来のスクロールで確認する。
- 携帯では従来どおり画面横幅に収まる縮小を維持する。
- 希望データ、他の決定シフト、Firebase設定、Realtime Databaseルール、保存形式、既存シフトデータ、発行済みURLは変更していない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にした。

### 確認項目

- 時間数字をタップした画面に「この時間帯だけ削除」が表示されること。
- AMを削除しても同じ人のPMが残るなど、選択した1本以外は消えないこと。
- 他スタッフと希望シフトが一切変更されないこと。
- PCの日別画面で表が横幅を有効に使い、右側に大きな空白が残らないこと。
- 縦方向の全スタッフをスクロールして確認できること。
- 通常版と携帯互換版が完全一致すること。
- GitHub ActionsとVercel本番公開が成功すること。

## 43. 2026-08-15対応済み: 月単位の希望提出・締切・未完了警告・オーナー日別画面調整

対象: `shift/index.html`、`shift-staff.html`（`v1.67`）、`shift/owner.html`、`shift-owner.html`（`v1.81`）

### 対応結果

- スタッフの希望シフトは1日単位ではなく、1か月分をまとめて「この月の希望を提出」する流れにした。提出後も締切日までは修正できる。
- 希望シフトの締切は対象月の前月20日。前月21日以降は対象月全体を閲覧専用にし、変更操作時は「変更は責任者に連絡してください」とカレンダー上へ表示する。
- 開始・終了の片方だけが保存された日はスタッフカレンダーと未入力欄を点滅表示する。両方が空白の日は点滅しない。不完全な時間のまま日別画面を閉じたり、月提出したりできない。
- PMの終了時刻を `23:30` にできる「ラスト 23:30」ボタンを追加した。PM開始時刻を先に入力してから使用する。
- オーナー画面に月の提出人数と各スタッフの「提出済」を表示する。
- オーナー日別画面では希望シフトを薄いグレー、決定シフトを色付きの線として上下に分け、青い休憩ブロックを決定シフトの色付き線内へ収めた。
- オーナー日別画面の要請マークを青い丸と白い紙飛行機へ変更し、「この時間帯だけ削除」ボタンを黒背景・白文字にした。
- 時間入力欄などの入力前の例示文字を薄いグレーにした。
- 希望データ、決定シフト、Firebase設定、Realtime Databaseルール、既存シフトデータ、発行済みスタッフURL・編集者URL、Vercelの正式URLは変更していない。
- 通常版と互換版はスタッフ・オーナーとも、それぞれ同一内容にした。

### 確認項目

- スタッフ通常版・互換版が `v1.67`、オーナー通常版・互換版が `v1.81` と表示されること。
- 前月20日までは提出後も修正でき、21日以降は対象月を変更できないこと。
- 開始・終了の片方だけの日だけが点滅し、空白日と入力完了日は点滅しないこと。
- 「ラスト 23:30」でPM終了が保存されること。
- オーナーの提出状況、希望の薄いグレー線、決定の色付き線、青い休憩ブロック、紙飛行機、黒い個別削除ボタンを確認すること。
- GitHub Actionsと2つのVercel本番公開が成功すること。

## 44. チャット引継ぎ・本番公開ルール

- チャットが長くなったら、作業者側から新しいチャットへの移動を案内し、未完了作業・確認済み事項・禁止事項・正式URL・公開状況をこのファイルへ記録する。
- 細かな修正ごとにVercel公開しない。複数の修正をローカルで確認し、ユーザーから最終公開の明示指示を受けてから、まとめて1回だけ公開する。
- 公開前に通常版と互換版の完全一致、JavaScript構文、差分、バージョン表示、`APP_VERSION`、引継ぎ記録を確認する。
- 公開対象はGitHub `main` へ1コミットで記録し、GitHub Actionsを確認する。
- Vercel本番はTeam ID `team_yuQNiQYLg82iiBgdIiM5wfkU` の次の2プロジェクトへ、同じGitHub `main` の内容を各1回公開する。
  - `zaiko-app-qhy7`: `prj_emDnHZflYDTTCHFi2rHn9Te03Ee1`
  - `zaiko-app`: `prj_DXGgsZQYojVDaDZOIp7G8Lq9L70z`
- 正式URLと既存ドメインは変更しない。公開後は通常版・互換版、両プロジェクトの `READY`、本番バージョン、主要表示、コンソール／ランタイムエラーを確認する。
- Vercelのビルド回数制限が出た場合、設定変更・不要な空コミット・別ドメイン作成などの回避を勝手に行わず、公開できなかったプロジェクトと制限内容を報告する。
- Firebase設定・Realtime Databaseルール・既存シフトデータ・スタッフ専用URL・編集者専用URL・GAS設定・在庫／売上／温度／わらび餅の公開ルールは、本件の公開作業では変更しない。

## 45. 2026-08-15対応済み: 要請マーク統一・拒否表示・携帯時間入力欄の整列

対象: `shift/index.html`、`shift-staff.html`（`v1.68`）、`shift/owner.html`、`shift-owner.html`（`v1.82`）

### 対応結果

- スタッフアプリに表示するオーナーからの要請を、文字ではなく青い丸と白い紙飛行機で表示する。
- スタッフが要請を拒否した後は「拒否」の漢字や「✕」を表示せず、スタッフ・オーナー両画面で赤い丸と白い紙飛行機を表示する。
- オーナー日別画面では、拒否された要請時間の棒の位置へ赤い紙飛行機を表示する。
- オーナー日別画面の青い要請マークはスタッフ名の長さに関係なく、名前欄の右端へ縦一列で揃える。
- 携帯のスタッフ希望入力画面で「ラスト 23:30」がPM時間欄と同じ横一列に入り、PM欄が画面外へ崩れていた。「ラスト 23:30」をPM入力欄の下の専用行へ移動し、開始・終了・区切りを画面内へ整列した。
- 希望データ、決定シフト、Firebase設定、Realtime Databaseルール、既存シフトデータ、発行済みURL、Vercelの正式URLは変更していない。
- 通常版と互換版はスタッフ・オーナーとも、それぞれ同一内容にした。

### 確認項目

- スタッフ通常版・互換版が `v1.68`、オーナー通常版・互換版が `v1.82` と表示されること。
- 要請中は青い紙飛行機、拒否後は赤い紙飛行機となり、「拒否」「✕」が状態表示に残らないこと。
- オーナーの日別画面で青い要請マークが名前欄右端に揃うこと。
- 携帯の希望入力画面でAM・PM・一日の時間欄が崩れず、「ラスト 23:30」がPM欄の下へ収まること。
- GitHub Actionsと2つのVercel本番公開が成功すること。

## 46. 次のチャットへ渡す文章

次の文章だけで開始できる。ただし、作業者は必ずこのファイルを全文読むこと。

> `booandmoomoo1011-maker/zaiko-app` の `main` にある `ZAIKON-HANDOFF.md` を最初に全文読んでください。私の管理v1.5、毎日在庫v10.65、在庫管理v4.8.0、売上入力v2.73、シフト・スタッフv1.70、シフト・オーナーv1.85が正式版です。シフト希望は1か月単位で提出し、対象月の前月20日までは提出後も修正可能、21日以降は変更不可です。開始・終了の片方だけの日は点滅し、月提出できません。スタッフの一日〇／一日OKは9:00〜23:30です。PMにはラスト23:30ボタンが時間欄の下にあります。未提出時の月提出ボタンは黒背景・白文字、提出後の「20日まで修正できます」は緑のままです。スタッフはオーナー要請に対して開始・終了時間を入力して承認するか「✕ 否認」を選び、回答後は同じ日の通常希望を別に再提出しません。要請中は青、承認済みは緑、否認済みは赤の紙飛行機で、オーナー月表・日別画面にも記録されます。オーナーは要請マークまたは要請線を開き、その要請1件だけを変更・削除できます。オーナーの要請マークは名前欄右端へ揃えています。オーナーには提出状況、薄いグレーの希望線、色付きの決定線、決定線内の青い休憩ブロック、黒い時間帯削除ボタンがあります。スタッフ専用URLの停止問題はFirebase本番ルール修正で解消し、店長URLの閲覧・入力・保存・再読込・解除と、ヒナキの既存URLの読込まで実機確認済みです。セクション23のシフト編集者専用URLは、Firebase本番ルール公開と編集者1のInPrivate実機確認まで完了し、編集者1・編集者2のURLはまだ他の人へ送っていません。セクション21の全アプリGitHub記録確認は完了しています。セクション22の在庫・売上系段階的認証は、既存端末とスプレッドシートを止めない第1段階です。Firebase本番 `app_access`、匿名認証、Google管理者認証、2つのGAS認証対応と外部通信権限付与、鎌倉専用URLからの認証付き売上データ読み込み・上書き保存・日報保存、旧売上入力v2.71からの読み込み・保存、旧毎日在庫v10.64と専用URL版v10.65の既存記録・同期確認までは完了しています。2026-08-11に確認用Windowsパソコンへ専用URL版の毎日在庫v10.65と鎌倉店売上入力v2.73をPWAとして設置し、正式アイコンからの再起動も確認しました。ほかの実運用端末と温度・わらび餅を含む全経路の移行は未完了です。コード確認により旧版はFirebase未認証で、今規則を閉じると停止することを確認したため、第1段階では6つの公開規則を維持しています。全実運用端末と連携経路の認証移行が終わるまでは、既存保存先を閉じず、GASの必須認証を有効にしないでください。シフトの認証済み機能、Firebase設定、既存データ、通常版／互換版同期を壊さず、変更後はGitHub ActionsとVercel本番を確認してください。

## 47. 2026-08-15対応: 要請回答・要請記録・個別削除の整理

対象: `shift/index.html`、`shift-staff.html`（`v1.69`）、`shift/owner.html`、`shift-owner.html`（`v1.83`）

### 対応結果

- 未提出時の「この月の希望を提出」を黒背景・白文字へ変更した。提出後の緑色と「20日まで修正できます」は維持した。
- スタッフの「一日〇／一日OK」は `9:00〜23:30` として表示し、オーナーの月表・日別希望線・印刷表示でも同じ終了時刻を使用する。
- スタッフの要請回答画面を、1本の開始・終了時間欄、「この時間で承認」、「✕ 否認」の2操作へ整理し、携帯で文字が縦に崩れない同幅ボタンにした。
- 承認には開始・終了の両方と正しい時間順を必須とした。否認は時間入力なしで選べる。
- 要請への承認・否認後は表示中の月を提出済みとして扱い、同じ要請日の通常希望入力をロックした。月希望締切後も、終了済みの月でなければオーナー要請へ回答できる。
- スタッフとオーナーの要請状態は、回答待ちを青、承認済みを緑、否認済みを赤の丸い紙飛行機で表示する。
- オーナー月表へも各日の要請状態を紙飛行機で記録し、否認済みは赤で残す。
- オーナー月表の紙飛行機、日別画面の要請線、名前横の要請ボタンから要請画面を開ける。既存要請は時間変更または「この要請だけ削除」ができる。
- 要請削除は対象スタッフ・対象日付の `req` 1件だけを削除し、希望シフト、決定シフト、他スタッフ、他日付の要請は変更しない。実行前に専用確認画面を表示する。
- Firebase設定・Realtime Databaseルール・既存シフトデータ・発行済みスタッフURL／編集者URL・Vercelの正式URLは変更していない。
- 通常版と互換版はスタッフ・オーナーとも、それぞれ同一内容にした。

### 確認項目

- スタッフ通常版・互換版が `v1.69`、オーナー通常版・互換版が `v1.83` と表示されること。
- 携帯幅で「この時間で承認」と「✕ 否認」が横並びで折り返さないこと。
- 承認・否認後に月提出済み表示となり、同じ日を通常希望として再提出できないこと。
- オーナー月表と日別画面で青・緑・赤の要請状態が一致すること。
- 既存要請を削除しても希望・決定シフトと別の要請が残ること。
- GitHub Actionsと2つのVercel本番公開が成功すること。

## 48. 2026-08-15対応: 否認済み要請の回答変更

対象: `shift/index.html`、`shift-staff.html`（`v1.70`）

### 対応結果

- スタッフがいったん否認したオーナー要請は、カレンダーの赤い紙飛行機をタップして要請回答画面を再表示できる。
- 再表示後、開始・終了時間を確認または変更し、「この時間で承認」を押すと承認済みへ変更できる。
- 月希望の締切後も、終了済みの月でなければ回答を変更できる既存ルールを維持する。
- 希望シフト、決定シフト、オーナー要請の時間、Firebase設定、既存データ、発行済みURLは変更しない。
- スタッフ通常版と互換版は同一内容にする。

### 確認項目

- 赤い紙飛行機をタップすると、保存済み要請時間が入った回答画面が開くこと。
- 「この時間で承認」後に緑の紙飛行機へ変わること。
- 赤い紙飛行機以外の日の希望・要請状態が変わらないこと。
- スタッフ通常版・互換版が `v1.70` と表示されること。
- GitHub Actionsと2つのVercel本番公開が成功すること。

## 49. 2026-08-15対応: オーナーによる希望時間の変更

対象: `shift/owner.html`、`shift-owner.html`（`v1.84`）

### 対応結果

- オーナー月間表の「希望」表示で、時間表示または休みの印をタップすると、そのスタッフ・その日の希望編集画面を開ける。
- 一日、AM、PMの開始・終了時間と、一日OK・休みをオーナー側から変更できる。
- 編集画面にはスタッフ名と対象日を表示し、保存後はオーナー月間表へ戻る。
- 変更対象は選択したスタッフ・日付の希望データだけとし、決定シフト、他スタッフ、他日付は変更しない。
- Firebase設定、Realtime Databaseルール、発行済みURL、Vercelの正式URLは変更しない。
- オーナー通常版と互換版は同一内容にする。

### 確認項目

- 希望時間・休みの印をタップして対象スタッフ・対象日の編集画面が開くこと。
- 変更が月間表へ反映され、決定シフトと他の日のデータが維持されること。
- オーナー通常版・互換版が `v1.84` と表示されること。
- GitHub Actionsと2つのVercel本番公開が成功すること。

## 50. 2026-08-15対応中: 売上オーナー共有者1人用の専用URL

対象: `sales-owner.html`、`database.rules.json`、`.github/workflows/shift-sync-check.yml`

### 対応内容

- 売上オーナーの設定画面へ「共有者1」の専用URL発行・再発行を追加する。
- 共有者はGoogleアカウント登録なしで、推測困難な専用URLとFirebase匿名認証を使って開く。
- 共有者は全店舗の売上履歴を確認し、各店舗の売上入力画面を開いて入力・修正できる。
- 共有者画面では設定ボタンを隠し、売上入力URL、毎日在庫URL、売上オーナー共有URLの発行・再発行を許可しない。
- 共有URLを再発行した場合は、共有者1の古いURLだけを即時停止する。Google管理者、店舗別売上入力URL、毎日在庫URL、シフトのスタッフURL・編集者URLには影響させない。
- 共有URLの発行時に4店舗の既存売上入力専用URLを確認し、未発行の店舗だけ安全に発行する。既存の有効な店舗URLは変更しない。
- Realtime Databaseルールへ売上オーナー共有者専用のトークン・1人枠・匿名セッションを追加する。共有者は4店舗の現在の売上入力トークンを読み取れるが、発行・再発行・無効化はできない。
- 現在進行中の在庫・売上系段階的認証（第1段階）は維持する。既存6保存先の公開規則を閉じず、GASの `requireForAllRequests` も変更しない。
- 正式URL `https://zaiko-app-two.vercel.app/sales-owner.html`、Firebase設定、既存売上データ、GAS、スプレッドシート、アイコン、OGP、シフト機能は変更しない。

### 公開前後の確認項目

- Google管理者が従来どおり売上オーナーへログインできること。
- 管理者の設定画面から共有者1の専用URLを発行・コピーできること。
- 共有URLをInPrivate相当で開き、Googleログインなしで全店舗の売上履歴を確認できること。
- 共有者が4店舗を開き、既存データの読み込み・値を変えない上書き保存・再表示まで確認できること。
- 共有者画面に設定ボタンが表示されず、管理用URLの発行・再発行ができないこと。
- 再発行後に旧共有URLだけが停止し、新URL、Google管理者、4店舗の既存URLが動き続けること。
- GitHub Actions、Firebase本番ルール、2つのVercel本番公開が成功すること。

### 現在の公開状況

- GitHub `main` はコミット `21234bbeb4ca8488f0aa830226b47f3429e607fd` へ反映済み。GitHub Actions「Zaikon app safety check」は成功している。
- GitHub連携により2つのVercel本番へ自動公開され、`zaiko-app-qhy7` は `dpl_CsWE3NHxSpP7NeCQvM4qioJVEHQh`、`zaiko-app` は `dpl_F2MXLb7YXfcpEhkiAjGAyB53jWod` でともに `READY`。
- Firebase管理画面から取得した現在のルール全文をGitHubの `database.rules.json` と照合し、完全一致を確認済み。`salesOwnerTokens`、`salesOwnerSlots`、`salesOwnerSessions` があり、既存6保存先の公開規則も維持されている。
- **未完了:** Google管理者画面から共有者1の専用URLを実際に発行し、InPrivate相当で履歴確認・4店舗の起動・設定非表示・旧URL停止を確認する。

## 51. 2026-08-15対応済み: 承認済みオーナーアイコンの公開

対象: `icons/sales-owner.*`、`icons/shift-owner.*`、`my-management.html`、`sales-owner.html`、`shift/owner.html`、`shift-owner.html`、`sales-owner.webmanifest`、`shift-owner.webmanifest`

### 対応内容

- 「私の管理」は承認済みINB候補A、売上オーナーは立体的な3本の棒グラフと王冠、シフト・オーナーは立体的な白いカレンダーと緑のチェックを正式採用した。
- 3種類とも明るい金色の背景を使用する。
- 「私の管理」は `v1.6`、オーナー2種の画像参照は `v=6`。
- 誤った平面的画像が入っていた `Zaikon-approved-icons-A-publish.zip` は再利用しない。
- 正しい承認済み画像はGitHub `main` コミット `4f7a0f610906d03fedad0d14df42931c34ebc88d` に公開済み。
- シフト・オーナー通常版と携帯互換版は完全一致を維持した。
- Firebase設定、Realtime Databaseルール、既存データ、発行済みURL、GAS設定、アプリ機能、正式ドメインは変更していない。

### 公開確認

- GitHub Actions成功、変更は指定24ファイルのみ。
- 2つのVercel本番は `READY`。
- `zaiko-app-qhy7`: `dpl_Fa2RQmNM8Co8pZwxGBY1gjMSHfQW`
- `zaiko-app`: `dpl_HTb4KgJsbTKDrQWfwDfR6E7VwkEn`

## 52. 2026-08-15対応: Windows Edgeの古いデスクトップアイコン再利用防止

対象: `sales-owner.html`、`shift/owner.html`、`shift-owner.html`、`sales-owner.webmanifest`、`shift-owner.webmanifest`、オーナー2種の新規固有名PNG

### 原因

- Edgeのアプリ設定画面では承認済みの金色アイコンへ更新されていたが、Windowsのデスクトップショートカット作成時に、以前インストールした黒背景アイコンを再利用していた。
- URL末尾の `?v=6` だけでは、既存PWAとWindows側のアイコン記憶を確実に分離できなかった。

### 対応内容

- 承認済み画像を変更せず、16px、32px、180px、192px、512pxへキャッシュと重複しない固有ファイル名を追加した。
- manifestのアイコン参照を固有ファイル名へ変更した。
- Edgeが古いPWAと同一アプリとして扱わないよう、売上オーナーとシフト・オーナーのmanifest `id` を承認済みv6用の固有IDへ変更した。
- manifestの背景色・テーマ色を承認済みの明るい金色 `#f6cd58` へ統一した。
- 正式URL、画面の機能、表示バージョン、Firebase設定、Realtime Databaseルール、既存データ、発行済みURL、GAS設定は変更していない。
- `shift/owner.html` と `shift-owner.html` は完全一致を維持する。

### 確認項目

- 新規固有名PNGが元の承認済みv6画像とバイト単位で一致すること。
- manifest JSONが正常で、固有ID・固有アイコン・金色背景を参照すること。
- オーナー通常版・携帯互換版が完全一致すること。
- GitHub Actionsと2つのVercel本番公開が成功すること。


## 53. 2026-08-15実機確認済み: Windowsデスクトップの正式アイコン

対象: Windows 11、Microsoft Edgeでインストールした「私の管理」「売上オーナー」「シフト・オーナー」

### 実機確認結果

- 「私の管理」は承認済みの明るい金色INB候補Aでデスクトップ表示された。
- 「売上オーナー」は承認済みの明るい金色・立体的な3本の棒グラフ＋王冠でデスクトップ表示された。
- 「シフト・オーナー」は承認済みの明るい金色・立体的な白いカレンダー＋緑のチェックでデスクトップ表示された。
- ユーザー提供の最終スクリーンショットで、3種類が同時に正しいアイコンで並ぶことを確認した。

### Windows Edgeで白いアイコンまたは旧アイコンが残る場合

- デスクトップのショートカットだけを削除しても、Edgeに古いPWA本体が残っていると、再作成したショートカットが白いアイコンまたは旧アイコンになる。
- 対象アプリを開き、右上の「…」→「アプリの設定」→「アンインストール」で古いPWA本体を削除する。
- その後、正式URLをEdgeで開き直してインストールする。
- 「デスクトップ ショートカットを作成する」を繰り返すだけでは白いショートカットが増えることがあるため、先に古いPWA本体をアンインストールする。
- アプリのアンインストールやデスクトップショートカットの削除では、Firebase上の既存データは削除されない。

### 公開基準

- Windows Edgeのアイコン再利用防止はGitHub `main` コミット `8cf8c350cc09b5911e3dac2954a2c0e29d096a57` で対応済み。
- GitHub Actions「Zaikon app safety check」は成功済み。
- 2つのVercel本番は対応コミットから公開され、`zaiko-app-qhy7` は `dpl_5QPCjVXqzwrpwjHPFSZkPDtLBaeR`、`zaiko-app` は `dpl_Bvms4mPNF61kADiXAsqStka6nJjm` でともに `READY` を確認済み。
- Firebase設定、`database.rules.json`、`firebase.json`、既存データ、専用URL、GAS設定、Vercelドメイン、アプリ機能は変更していない。

## 54. 2026-08-21対応: 未決定シフトを決定シフト印刷から除外

対象: `shift/owner.html` と `shift-owner.html`（`v1.85`）

### 原因

- オーナーが入力した作成途中の決定シフトと、「この日を決定」で保存する日単位の決定状態は別々に保持している。
- 従来の決定シフト印刷は日単位の決定状態を確認せず、作成途中の時間を月間表へ表示していた。
- 日別の決定シフト印刷では、決定シフトがない場合にスタッフ希望を代わりに表示していた。

### 対応内容

- 月間・日別の決定シフト印刷は、「この日を決定」済みの日だけ決定シフトを表示する。
- 未決定日は印刷上だけ空白にする。スタッフの提出済み希望、オーナーが入力した作成途中の決定シフト、備考、要請は削除・変更しない。
- 後から「この日を決定」にすると、保存済みの決定シフトが印刷へ表示される。
- 希望シフト印刷の内容と、画面上の希望・作成途中の決定シフト表示は変更しない。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存データ、発行済みURL、正式ドメインは変更しない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は同一内容にする。

### 公開前の確認項目

- 未決定日に作成途中の決定シフトやスタッフ希望があっても、決定シフト印刷では空白になること。
- 決定済み日は、保存済みの決定シフトだけが印刷されること。
- 希望シフト印刷では提出済み希望が従来どおり表示されること。
- 通常版と携帯互換版が完全一致し、JavaScript構文エラーがないこと。
- ユーザーの「公開して」という明示指示まではGitHub `main` とVercel本番へ反映しないこと。

## 55. 2026-08-25対応: 空白セルから口頭の希望シフトを入力

対象: `shift/owner.html` と `shift-owner.html`（`v1.111`）

### 対応内容

- オーナー月間一覧の「希望」表示で、希望が入っていないスタッフ・日付の空白セルを1回押すと、既存の希望入力画面を開く。
- 口頭で聞いた希望として、一日OK、休み、AM・PM・一日の希望時間と備考を入力できる。
- オーナーから開いた入力画面の保存ボタンは「希望を保存」と表示し、決定シフトと誤解しないようにする。
- 保存先は既存の `S.staff[name].wish[YYYY-MM-DD]` を使用し、決定シフト、決定済み状態、日付別公開状態は変更しない。
- 保存した希望はオーナー月間一覧へ表示し、同じ希望データを読むスタッフアプリの本人カレンダーにも同期して表示する。
- 希望締切後もオーナーは入力できる。スタッフ本人の締切後ロックは従来どおり維持する。
- 空白セルの1回押しと、既存のダブルタップ操作が競合しないよう、ダブルタップ時は希望入力を開かない。
- Firebase設定、Realtime Databaseルール、既存シフトデータ、発行済みURL、正式ドメイン、スタッフアプリのコードは変更しない。
- `shift/owner.html` と `shift-owner.html` は完全一致を維持する。

### 確認項目

- 希望表示中の空白セルから、選択したスタッフ・日付の希望入力画面が開くこと。
- 一日OK、休み、時間入力を保存すると、オーナー月間一覧とスタッフカレンダーへ同じ内容が表示されること。
- 決定シフト表示中、終了済み月、スタッフ名のない予備行では希望入力が開かないこと。
- 決定シフト、決定済み状態、公開状態、他スタッフ、他日付が変わらないこと。
- 通常版と互換版が完全一致し、JavaScript構文エラーがないこと。

## 56. 2026-08-29対応: シフト編集者名とオーナー専用編集履歴（v1.160）

対象: `shift/owner.html`、`shift-owner.html`、`database.rules.json`

### 対応内容

- 既存の編集者3枠を、`editor1=店長`、`editor2=ヒナキ`、`editor3=兄`として表示・記録する。
- 発行済みの編集者専用URLは再発行せず、そのまま利用できる。オーナーが「URLをコピー」を押した際は、既存トークンへ表示名だけを安全に補完する。
- オーナーまたは編集者がシフトを保存した際、変更者名、日本時間で確認できる日時、対象スタッフ・日付・変更種類を `zaiko/shift/editHistory` へ記録する。
- オーナー画面へ「編集履歴」を追加し、新しい順に最大200件を表示する。
- 編集履歴はGoogle認証済みオーナーだけが読み取れる。編集者画面には履歴ボタンを表示せず、Realtime Databaseルールでも編集者からの履歴読み取りを許可しない。
- 履歴はv1.160公開後の変更から記録する。過去の変更を推測して作成しない。
- Firebaseの既存シフト、希望、決定、公開状態、スタッフデータ、保存先、正式URL、発行済みURLは変更しない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は完全一致を維持する。

### 公開確認項目

- JavaScript構文、Realtime DatabaseルールJSON、通常版・互換版の一致を確認する。
- 店長・ヒナキ・兄の各専用URLから保存し、記録名が本人名になることを確認する。
- オーナーだけが「編集履歴」を開け、編集者にはボタンも履歴データも表示されないことを確認する。
- GitHub Actions成功、Firebaseルール反映、2つのVercel本番がREADYであることを確認する。

## 57. 2026-08-30対応: 携帯オーナーの決定シフト閲覧画面（v1.162）

対象: `shift/owner.html`、`shift-owner.html`

### 対応内容

- 幅600px以下の携帯では、起動直後を希望入力画面ではなく決定シフトの閲覧専用画面にする。PCの希望シフト中心の月間画面は変更しない。
- 携帯閲覧画面には「この日を決定」済みの勤務時間だけを色付きで表示し、そのスタッフに希望があった場合は希望時間を決定時間の上へ薄いグレー文字で添える。希望の色ラインは表示しない。
- 表示中の月に決定シフトが一件もないスタッフ、名前追加行、空白行は携帯閲覧画面へ表示しない。
- 携帯閲覧画面のスタッフ行はタップ・ダブルタップ・長押しを受け付けず、日付ヘッダーを押した場合だけ既存の日別編集画面を開く。
- 携帯起動時は日本時間の当月へ移動し、今日の列が見える位置へ自動スクロールする。運用開始月の2026年9月より前の場合は9月を表示する。
- 日別編集画面は従来どおり各店舗の全スタッフを表示し、「＋ 名前を追加」から所属店舗外の全スタッフを選べる。希望未提出のスタッフも追加対象にする。
- 日別編集画面へ「閉じる」を追加し、決定シフト閲覧画面へ安全に戻れるようにする。
- 携帯の印刷、まとめて決定・未確定、公開・非公開、名前、編集者URL、編集履歴、オーナーURLを歯車の設定メニューへ収納する。PCの上部ボタン配置は維持する。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存シフトデータ、発行済みURL、スタッフアプリ、印刷仕様は変更しない。
- 通常版 `shift/owner.html` と携帯互換版 `shift-owner.html` は完全一致を維持する。

### 公開前の確認項目

- 携帯幅で月間スタッフ行を触っても希望・決定・休み・案内・要請が変化しないこと。
- 日付タップだけが日別編集を開き、日別では全スタッフと「＋ 名前を追加」が表示されること。
- 携帯の希望時間が薄いグレー文字、決定時間が従来の店舗・AM・PM色で表示されること。
- PC幅で従来の希望月間画面、上部ボタン、日別編集が変わらないこと。
- JavaScript構文、通常版・互換版の一致、主要操作、横スクロール、コンソールエラーを確認すること。
- 2026-08-30、ユーザーから確認後の公開指示を受けたため、検査合格後にGitHub `main` とVercel本番へ反映する。

## 58. 2026-08-31対応: 日別タイムライン固定・23:30・スタッフ休憩表示

対象: `shift/index.html`、`shift-staff.html`（`v1.82`）、`shift/owner.html`、`shift-owner.html`（`v1.167`）

### 対応内容

- オーナー日別編集は、名前欄と9:00〜23:30の時間軸を同じ基準幅・同じ倍率で描画し、拡大縮小後に時間線だけがずれる状態を防ぐ。
- 日別編集を開き直したときは、以前の手動サイズとスクロール位置を引き継がず、左上から安定して表示する。
- スタッフの公開済み決定シフト日別画面は、時間見出しを上、名前を左へ固定し、23:30を右端の内側へ表示する。
- 決定済み勤務線へ休憩を水色で重ね、連続した30分枠をまとめて60分・120分などの分数で表示する。
- 休憩は既存の勤務・休憩データから公開表示用スナップショットを作る。元の勤務時間、休憩時間、希望、決定、公開状態は書き換えない。
- 古い公開スナップショットに休憩情報がなくても従来どおり勤務線を表示し、エラーにしない。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存シフトデータ、発行済みスタッフURL・編集者URL、正式URLは変更しない。
- 通常版と互換版はスタッフ・オーナーとも、それぞれ完全一致を維持する。

### 公開前確認

- HTML内の通常スクリプトとモジュールスクリプトの構文検査に合格すること。
- 休憩30分枠が連続した場合、60分・120分へ正しくまとまること。
- スタッフ日別画面で23:30、60分、120分の表示生成を確認すること。
- オーナーの時間軸と全スタッフ行が同じ倍率を使用すること。
- `shift/index.html` と `shift-staff.html`、`shift/owner.html` と `shift-owner.html` がそれぞれ完全一致すること。
- 変更対象がシフト4画面と本引継ぎ文書だけで、Firebase設定・ルール・既存データを変更していないこと。

## 59. 2026-08-31対応: 2026年9月開始の修正・日別画面の拡大負荷対策

対象: `shift/index.html`、`shift-staff.html`（`v1.83`）、`shift/owner.html`、`shift-owner.html`（`v1.169`）

### 原因

- 引継ぎ上は2026年9月開始となっていたが、スタッフ・オーナー両コードの開始月定数が8になっており、2026年8月を選べる状態だった。
- スタッフの公開済み決定シフト日別画面は、指の拡大を利用者倍率5倍・実描画3倍まで許可し、連続した指移動ごとに同期レイアウトを再計算していた。携帯で過度に拡大すると、名前欄と時間線が重なり、画面が停止・終了する原因になっていた。
- オーナーの日別編集画面も実描画3倍まで許可し、連続した指移動ごとに同期レイアウトを再計算していたため、同じ過拡大・重なり・停止の危険があった。

### 対応内容

- スタッフ・オーナーとも開始月定数を2026年9月へ修正し、8月以前を月選択と前月移動から除外する。
- 端末に以前の8月表示状態が残っていても、起動時に2026年9月へ補正する。
- 2026年8月以前の既存データは削除・変更せず、画面から選べなくするだけとする。
- スタッフ日別画面の2本指拡大縮小は維持し、利用者倍率を0.7〜2倍、実描画倍率を最大1.2倍へ制限する。
- オーナー日別編集画面も2本指拡大縮小を維持し、利用者倍率を0.7〜2倍、実描画倍率を最大1.2倍へ制限する。
- 指を動かすたびの即時再計算をやめ、`requestAnimationFrame`で1画面描画につき最新1回だけ処理する。画面を閉じる・開き直す際は待機中の拡大処理を解除する。
- 日別画面を開いた直後は従来どおり3店舗全体を収め、必要な範囲だけ安全に拡大できるようにする。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存シフトデータ、休憩データ、発行済みURL、正式URLは変更しない。
- 通常版と互換版はスタッフ・オーナーとも、それぞれ完全一致を維持する。

### 公開前確認

- 2026年8月を指定した状態がスタッフ・オーナーとも2026年9月へ補正されること。
- 2026年9月から前月へ移動できないこと。
- スタッフ日別画面・オーナー日別編集画面の倍率が設定上限を超えず、連続した拡大操作が描画単位でまとめられること。
- 23:30、時間見出し・名前固定、休憩表示、3店舗表示を維持すること。
- シフト4画面のJavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 60. 2026-08-31対応: 日別画面の二重ズーム・固定名座標ずれ修正

対象: `shift/index.html`、`shift-staff.html`（`v1.84`）、`shift/owner.html`、`shift-owner.html`（`v1.170`）

### 再調査で判明した原因

- v1.83／v1.169ではアプリ内の日別表倍率だけを制限したが、通常のViewportはページ全体のピンチズームを許可したままだった。
- Androidのブラウザ／PWAでは、指の開始位置によって日別表の独自ズームとページ全体ズームが同時または交互に動き、上部ボタンを含む画面全体が拡大されていた。
- オーナー日別編集は、`position: sticky`で固定する名前欄を含む表全体へ`transform: scale()`を適用していた。拡大後の横スクロールでは固定位置と変形後の座標系が一致せず、名前・時間軸・シフト線が別々の位置へずれていた。
- 前回の倍率上限だけでは、この2経路を止められず不十分だった。

### 対応内容

- 通常画面のViewportは従来どおりズーム可能な設定を維持する。
- スタッフ決定シフト日別画面・オーナー日別編集画面を開いている間だけ、ページ全体のブラウザズームを一時停止する。閉じた時は元のViewport設定を必ず復元する。
- 日別画面内の2本指操作は従来どおり表専用の拡大縮小として処理し、ネイティブのページピンチだけを抑止する。
- 固定名を含む表の拡大を`transform`からレイアウト計算へ反映される`zoom`へ変更し、時間軸・名前・背景・シフト線を同じ座標系で描画する。
- 9:00〜23:30の時刻座標は固定pxの正確な間隔を維持する。時間軸をFlexboxで均等分配せず、勤務開始・終了位置を変えない。
- 開いた直後の3店舗表示、名前と時間見出しの固定、2本指拡大、23:30、休憩表示を維持する。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存シフト・希望・休憩データ、発行済みURL、印刷仕様は変更しない。
- 通常版と互換版はスタッフ・オーナーとも、それぞれ完全一致を維持する。

### 公開前確認

- 日別画面の表示中だけViewportがページズーム停止設定になり、閉じると元の設定へ戻ること。
- 日別表の拡大に`transform: scale()`を使用せず、名前・時間軸・シフト線へ同じ倍率を使用すること。
- 連続した2本指操作は`requestAnimationFrame`でまとめ、最大実描画倍率1.2を超えないこと。
- スタッフ通常版・互換版、オーナー通常版・互換版がそれぞれ完全一致すること。
- JavaScript構文、自動安全検査、変更対象、Firebase無変更を確認すること。

## 61. 2026-08-31対応: 日別編集の固定名分離・携帯ログイン復元（v1.171）

対象: `shift/owner.html`、`shift-owner.html`

### 原因

- v1.170は`transform`を廃止したが、`#dayGrid`全体へ`zoom`を適用していたため、固定スタッフ名も時間軸・勤務線と一緒に拡大されていた。
- スタッフ名の表示階層が勤務線・休憩・重要文字より低く、拡大後や前日・翌日への再描画後に勤務線が名前の上へ重なる余地が残っていた。
- Google認証は`browserLocalPersistence`だけを後から指定し、認証復元前からログイン案内とボタンを表示していた。携帯PWAで復元に時間がかかる場合、ログイン済みでも利用者が再ログイン操作へ進みやすかった。

### 対応内容

- スタッフ名・店舗名は等倍の固定列として日別表の拡大対象から分離する。
- 時間軸、背景グリッド、希望線、勤務線、休憩、重要文字だけへ同じ倍率を適用する。
- 名前列を不透明背景・高い表示階層で固定し、最小倍率・最大倍率・横スクロール・日付移動後も色ラインが名前の上へ出ないようにする。
- 拡大時の指位置補正は、等倍の名前列を除いた時間領域の座標で計算する。
- Google認証はIndexedDBを優先し、利用できない場合にブラウザのローカル保存へ切り替える永続化構成にする。
- 認証復元が完了するまでは「前回のログインを確認しています…」と表示してログインボタンを隠す。保存済み認証がないことを確認した場合だけログインボタンを表示する。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存データ、発行済みURL、印刷仕様は変更しない。
- `shift/owner.html` と `shift-owner.html` は完全一致を維持する。

### 公開前確認

- 最小・通常・最大倍率でスタッフ名の幅と文字サイズが変わらず、時間軸・グリッド・勤務線だけが同倍率で変わること。
- 横へスクロールしても名前列が左端・最前面に残ること。
- 前日・翌日へボタンまたは横スワイプで移動した後も同じ表示階層と倍率分離が維持されること。
- 認証復元中はログインボタンが表示されず、保存済みGoogle認証がある場合はアカウント選択を出さずにオーナー確認へ進むこと。
- 保存済み認証がない場合だけGoogleログインボタンが表示されること。
- JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 62. 2026-08-31対応: スタッフ決定シフトの前日・翌日移動と当日表示（v1.85）

対象: `shift/index.html`、`shift-staff.html`

### 原因

- スタッフが決定シフトの日付を押して開く日別画面には、2本指の拡大縮小だけがあり、1本指で前日・翌日へ移動する処理がなかった。
- 決定シフト月間表には当日の色と「今日」表示があったが、横長の表を当日列まで自動で移動していなかった。

### 対応内容

- 日別画面の1本指横スワイプに対応し、左で翌日、右で前日を開く。
- 誤操作を防ぐため、55px以上かつ縦移動より明確に大きい横移動だけを日付切り替えとして扱う。
- 拡大中の横移動を維持するため、横スクロール可能な状態では端まで到達した後のスワイプだけを日付切り替えに使用する。
- 公開済みの決定シフトがある隣の日だけを開き、未公開日の内容は表示しない。
- 2026年9月1日より前への移動を禁止し、日別スワイプでも2026年8月を表示しない。
- 「決定シフト」を開いた時は運用上の当日月へ戻し、当日列が固定スタッフ名のすぐ右に見える位置まで自動スクロールする。
- 運用開始前の2026年8月31日に開いた場合は、2026年9月と9月1日を表示し、8月表記を出さない。
- 当日列の薄黄色と「今日」表示、日付タップ、2本指拡大縮小、縦スクロールを維持する。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存シフトデータ、発行済みURLは変更しない。
- `shift/index.html` と `shift-staff.html` は完全一致を維持する。

### 公開前確認

- 日別画面で左スワイプが翌日、右スワイプが前日となり、縦スクロールと2本指拡大縮小に干渉しないこと。
- 2026年9月1日から右へスワイプしても2026年8月へ移動しないこと。
- 決定シフトを開いた直後、当日が対象月内なら当日列、運用開始前なら2026年9月1日がすぐ見えること。
- 小さい表示・大きい表示の両方で日別画面の勤務線、休憩、23:30表示を維持すること。
- JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 63. 2026-08-31対応: 日別ライン時間枠を0:00位置まで延長（スタッフv1.87／オーナーv1.172）

対象: `shift/index.html`、`shift-staff.html`、`shift/owner.html`、`shift-owner.html`

### 原因

- オーナー日別編集とスタッフ決定シフト日別閲覧は、表示領域の終端を勤務入力上限と同じ23:30にしていた。
- そのため23:30の数字が右端に重なり、23:30で枠が途切れて見えていた。

### 対応内容

- 勤務時間の入力・勤務線の上限23:30は変更しない。
- 表示用の時間軸だけ0:00位置まで30分延長し、「23:30〜0:00」の最後の枠を追加する。
- 23:30の数字は最後の30分枠の開始位置へ表示し、0:00位置の右端線で枠を閉じる。
- オーナー日別編集では23:30位置にも縦線を追加する。
- 各店舗の見出しにある時間表示は、店舗ごとの確認に必要なため削除しない。
- オーナーの名前固定・時間領域だけの拡大縮小、スタッフの前日・翌日スワイプを維持する。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存シフト・希望・休憩データ、発行済みURL、印刷仕様は変更しない。
- スタッフ通常版・互換版、オーナー通常版・互換版はそれぞれ完全一致を維持する。

### 公開前確認

- オーナー編集とスタッフ閲覧の両方で23:30の数字が表示され、その右側に30分枠があること。
- 右端が0:00位置の縦線で閉じ、勤務線は従来どおり23:30を超えないこと。
- 各店舗の時間表示、23:30勤務、休憩、名前固定、拡大縮小、日付移動を維持すること。
- JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 64. 2026-08-31対応: スタッフ重要情報の上部配置と設定画面（v1.87）

対象: `shift/index.html`、`shift-staff.html`

### 対応内容

- スタッフ名の直下へ重要情報エリアを配置し、「本日のシフト」「連絡事項」「あなたへの案内」の順で常時表示する。
- 重要情報は希望カレンダー・決定シフトのどちらへ切り替えても見える位置に置く。
- 未確認の個人案内がある場合は、重要情報の直下に件数と確認ボタンを表示する。
- 画面上に常時展開していた古い「使い方」を削除し、スタッフ名の横へプロ仕様の「⚙ 設定」ボタンを追加する。
- 設定画面に「使い方」「アプリのお知らせ」「この端末からログアウト」を収納する。
- 使い方は現行操作へ更新し、希望入力、月提出と20日締切、月スワイプ、決定シフト、日別表示、前日・翌日スワイプ、2本指拡大縮小、案内、要請を説明する。
- ログアウトは確認後にこの端末の保存済みアクセストークンだけを外し、シフトデータ・専用URL・Firebaseデータは削除しない。
- 本日の重要連絡ポップアップ、案内確認、決定シフト閲覧、スタッフ専用URL認証は維持する。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存シフトデータ、発行済みURLは変更しない。
- `shift/index.html` と `shift-staff.html` は完全一致を維持する。

### 公開前確認

- 名前の直下に本日のシフト、連絡事項、個人案内が指定順で表示されること。
- 希望カレンダー・決定シフトの切り替え後も重要情報が上部に残ること。
- 設定の開閉、使い方とお知らせの展開、ログアウト確認が動作すること。
- JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 65. 2026-08-31対応: スタッフ決定シフトの日付ボタン明確化（v1.87）

対象: `shift/index.html`、`shift-staff.html`

- 決定シフト表の日付へ枠、角丸、明暗、影を付け、押せるボタンであることを明確にする。
- 公開済みで開ける日は金色系のボタン、未公開で開けない日はグレーの無効ボタンとして区別する。
- 押した時の沈み込みを追加し、日付タップで日別3店舗表示を開く既存動作は変更しない。
- 当日列の薄黄色と「今日」表示、固定スタッフ名、横スクロール、前日・翌日スワイプ、拡大縮小を維持する。
- Firebase設定、保存データ、発行済みURLは変更しない。
- スタッフ通常版・互換版の一致、JavaScript構文、自動安全検査を確認する。

## 66. 2026-08-31対応: 日別時間軸の重複解消・日付強調（スタッフv1.88／オーナーv1.173）

対象: `shift/index.html`、`shift-staff.html`、`shift/owner.html`、`shift-owner.html`

### 原因

- オーナー日別編集は、上部の固定時間軸に加えて各店舗見出しでも同じ時間数字を生成していたため、パソコン画面で時間軸が二重に見えていた。
- 23時と23:30は画面上で20px相当しか離れておらず、同じ高さへ置くと拡大率や文字描画によって必ず重なる余地があった。
- 携帯オーナー日別編集の日付が操作ボタンと同じ見出し内にあり、文字が小さく、開いている日を判別しにくかった。

### 対応内容

- 最新の利用者指示を優先し、オーナー日別編集は上部の固定時間軸だけを残す。各店舗見出しでは時間数字を繰り返さない。
- 23時は上段、23:30は下段に表示して、パソコン・携帯と拡大率の大小にかかわらず文字が重ならない構造にする。
- 表示枠は従来どおり0:00位置の右端線まで、勤務入力と勤務線の上限は23:30のまま維持する。
- 携帯オーナー日別編集は、操作ボタンの下へ独立した日付欄を設け、22pxで「年・月・日・曜日」を表示する。
- スタッフ決定シフト日別画面も23:30を下段に配置し、右端の乱れと23時との重なりを防ぐ。
- 名前固定、時間領域だけの拡大縮小、日付スワイプ、休憩表示、Firebase設定、既存データ、発行済みURL、印刷仕様は変更しない。
- スタッフ通常版・互換版、オーナー通常版・互換版はそれぞれ完全一致を維持する。

### 公開前確認

- オーナー日別編集で時間数字の列が上部に1つだけ表示され、各店舗見出しには重複表示されないこと。
- オーナー・スタッフとも最小・通常・最大倍率で23時と23:30が重ならず、右端線が0:00位置にあること。
- 携帯オーナー日別編集で、開いている日付が独立した大きな欄に表示されること。
- JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 67. 2026-08-31対応: 公開後の日別変更履歴（オーナーv1.174）

対象: `shift/owner.html`、`shift-owner.html`

### 原因

- 既存の編集履歴は保存失敗を画面へ通知せず、保存対象も「公開後」に限定されていなかった。
- 履歴に対象日付の構造化項目がなく、日別編集画面から日付ごとに絞り込めなかった。
- 編集履歴は全体モーダルだけにあり、日別編集画面右側の余白には表示されていなかった。

### 対応内容

- すでに公開済みだった日の変更だけを日付付きで記録する。初回公開自体は変更履歴に含めず、その後の決定シフト、公開状態、店舗休み、店舗メモ、連絡事項、スタッフ別シフト・備考・案内などを対象にする。
- 履歴レコードへ対象日の `dates` と日付・内容の `items` を追加する。既存の `actor`、`actorSlot`、`at`、`summary`、`version` は維持し、過去の `summary` 形式も日付文字列から読み取れるようにする。
- パソコンのオーナー日別編集画面右側へ「変更者・時間・変更内容」の3列を追加し、新しい履歴を上、古い履歴を下に表示する。
- 未公開日は「公開後に変更した内容から記録」、公開済みで履歴がない日は「公開後の変更はありません」と明示する。
- オーナーだけが日別履歴を読み取れる既存権限を維持し、編集者画面と幅900px以下では右側パネルを表示しない。全体の「編集履歴」モーダルも維持する。
- 履歴保存に失敗した場合は画面へ警告し、未保存項目を次の保存に残す。シフト本体の既存送信処理は変更しない。
- Firebase設定、Realtime Databaseルール、保存先、既存履歴・シフトデータ、発行済みURL、印刷仕様は変更しない。
- オーナー通常版・互換版は完全一致を維持する。

### 公開前確認

- 初回公開では履歴が作られず、公開後の変更で日付付き履歴が作られること。
- 日別画面右側に対象日の履歴だけが新しい順で表示され、変更者・時間・変更内容を確認できること。
- 過去形式の履歴も対象日付を含む場合は表示でき、未公開・履歴なし・通信失敗の案内が区別されること。
- 日別グリッド、名前固定、時間軸、拡大縮小、日付移動、全体履歴、編集者権限を維持すること。
- JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 68. 2026-08-31対応: 携帯の時刻タップ編集・決定公開履歴（オーナーv1.175）

対象: `shift/owner.html`、`shift-owner.html`

### 最新指示による仕様整理

- v1.174の「公開済み日の変更時に即時記録」ではなく、作業中の変更は下書きとして画面内に保持し、オーナーまたは編集者がその日を決定して公開した時点で履歴へ確定記録する。
- 初回公開も「このシフトを決定して公開」として記録する。再編集した場合は、変更内容をため、再決定・再公開した時点で次の履歴として追加する。
- 明示的に公開を取り消した操作も日付別履歴へ残し、公開版の変更の流れを新しい順に追えるようにする。

### 対応内容

- 幅600px以下の携帯日別編集では、勤務線両端のドラッグ用つまみを非表示にし、指による伸縮を無効化する。タッチ入力は画面幅に関係なくドラッグ伸縮を開始しない。
- 勤務線の開始・終了時刻を大きなタップ領域と下線付き表示にし、時刻をタップすると既存の個別時間入力画面を開く。
- PCではマウスによる勤務線両端の伸縮を維持し、開始・終了時刻のクリック編集も引き続き利用できる。
- 作業中のスタッフ別決定時間、店舗休み、店舗メモ、連絡事項などの日付別変更を一時的にまとめ、日別公開またはまとめて公開の操作時だけ履歴レコードへ確定する。
- 日別履歴の案内を「決定して公開した内容」に統一し、公開前の未完成な操作が正式履歴に見えないようにする。
- 履歴の変更者、時間、変更内容、新しい順、オーナーだけが閲覧できる権限、過去形式互換、保存失敗時の再試行保持を維持する。
- Firebase設定、Realtime Databaseルール、保存先、既存履歴・シフトデータ、発行済みURL、印刷仕様は変更しない。
- オーナー通常版・携帯互換版は完全一致を維持する。

### 公開前確認

- 携帯幅およびタッチ入力で勤務線の端を引っ張っても時間が変わらず、開始・終了の時刻タップから個別時間画面を開けること。
- PCのマウス操作では既存の端ドラッグと時刻クリックの両方が動くこと。
- 作業途中では履歴を送信せず、決定して公開した時点で、変更者・時間・変更内容と「このシフトを決定して公開」が対象日の履歴になること。
- 初回公開、再編集後の再公開、明示的な公開取消を区別して記録できること。
- JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 69. 2026-08-31対応: 年次棚卸の店舗別プロフェッショナルデザイン（在庫管理v4.8.1）

対象: `inventory.html`

### 対応内容

- 既存の1つの在庫管理アプリ・4店舗固定URLの構造を維持し、選択中店舗に応じて画面テーマだけを切り替える。
- 大須は薄ピンク、那古野は薄黄色、鉄板は水色、鎌倉は薄紫を基調にする。
- ヘッダー、店舗タブ、集計カード、検索・カテゴリー操作、商品カード・表、入力欄、ボタン、モーダルへ統一感のある余白・影・角丸・フォーカス表示を適用する。
- 携帯とパソコンの既存レイアウトを維持し、店舗固定URLでは開いた店舗の色を最初から適用する。
- 入力済み商品の薄グレー表示、危険操作の赤、保存状態、LINE共有、年度管理、Excel出力、スプレッドシート連携は維持する。
- Firebase設定、Firestore保存先 `inventory/main`、localStorageキー `zaikoApp_v1`、IndexedDB、既存商品・数量・単価・年度・カテゴリー、管理者／編集者URL、共有URLは変更しない。
- アプリの画面構造、操作順、店舗切替ロジック、保存形式、既存機能は変更しない。

### 公開前確認

- 4店舗それぞれの固定URLで指定した店舗カラーが適用されること。
- 管理者画面で店舗タブを切り替えると、データを変更せず画面テーマだけが切り替わること。
- JavaScript構文、HTML構造、主要DOM、保存先・保存キー、バージョン表示を確認すること。
- 変更対象が `inventory.html` と本引継ぎ文書だけであること。

## 70. 2026-08-31対応: 管理者設定の店舗URL・端末共通インストール（在庫管理v4.8.2）

対象: `inventory.html`

### 対応内容

- 管理者の設定画面へ、大須・那古野・鉄板・鎌倉の4店舗の編集者URLを一覧表示する。
- 各店舗に「開く」と「コピー」を設け、管理者URLを店舗担当者へ誤共有しない案内を表示する。
- 管理者・編集者の設定画面へ「アプリをインストール」を追加し、未インストール時は画面右下にも同じ操作を表示する。
- パソコンでブラウザの直接インストール通知が届かない場合もボタンを表示し、Edge／Chromeの手順を案内する。
- iPhone、iPadOS、Android、LINE／LIFFを判定し、直接インストールできない環境ではSafari／Chromeからホーム画面へ追加する手順を表示する。
- インストール操作時の管理者または店舗編集者URLだけを端末へ記憶し、共通manifestの起動後も同じ役割・店舗の固定URLへ戻す。管理者は従来どおりパスワード確認を必要とする。
- Firebase設定、Firestore保存先 `inventory/main`、localStorageキー `zaikoApp_v1`、IndexedDB、既存商品・数量・単価・年度・カテゴリー、4店舗の正式編集者URL、管理者URL、共有URLは変更しない。

### 公開前確認

- 管理者設定に4店舗の正しい編集者URLと「開く」「コピー」が表示されること。
- 管理者・4店舗編集者の各URLで、未インストール時にインストール操作が表示されること。
- インストール起動用 `?launch=pwa` から、記憶した管理者または店舗編集者URLだけへ安全に戻ること。
- インストール済みのstandalone画面では右下ボタンを表示しないこと。
- JavaScript構文、manifest、Service Worker、保存先・保存キー、既存機能を確認し、変更対象が `inventory.html` と本引継ぎ文書だけであること。

## 71. 2026-09-01対応: 「私の管理」から棚卸管理者画面を開く（私の管理v1.8）

対象: `my-management.html`

### 対応内容

- 本人専用の「私の管理」にある「在庫管理」カードのリンクを、共通URLから管理者専用URL `inventory.html?role=admin` へ変更する。
- カードを押すと棚卸管理者パスワードの確認画面を開き、編集者画面へ誤って入らないようにする。
- 店舗担当者は、棚卸管理者設定に表示される各店舗の編集者専用URLを引き続き使用する。
- 棚卸アプリ本体、管理者パスワード、店舗編集者URL、Firebase設定、既存データ、保存先、ほかのアプリへのリンクは変更しない。

### 公開前確認

- 「私の管理」の在庫管理カードが `inventory.html?role=admin` を開くこと。
- 既存のキャッシュ回避 `_latest` が追加されても `role=admin` が保持されること。
- 表示バージョンが `My Management v1.8` であること。
- 変更対象が `my-management.html` と本引継ぎ文書だけであること。

## 72. 2026-09-01対応: 携帯印刷プレビューの一括縮小・スタッフ公開ミニチュア（スタッフv1.89／オーナーv1.176）

対象: `shift/index.html`、`shift-staff.html`、`shift/owner.html`、`shift-owner.html`

### 対応内容

- オーナーの携帯印刷プレビューは、PC版の1040px印刷レイアウトを組み替えず、スタッフ名・時間軸・店舗・色ラインを含む表全体を携帯幅へ同じ倍率で縮小する。
- 携帯で列幅や文字だけが個別に縮んで崩れないよう、プレビュー本体の固定幅を維持して表示倍率だけを計算する。
- PCの印刷プレビューと実際のA4横印刷には携帯用倍率を適用しない。
- スタッフカレンダーの下へ、公開済みの日だけを3日単位で並べた閲覧専用ミニチュア表を追加する。
- スタッフのミニチュアは、公開時に作成済みの `_publishedSchedule` だけを使用する。未公開日、編集中の決定シフト、希望シフトは表示しない。
- スタッフのミニチュアには3店舗、名前、9:00〜23:00の時間軸、勤務ライン、休憩ライン、休み、既存の印刷用空白行を表示する。
- スタッフ画面には印刷ボタンと印刷処理を追加せず、「閲覧のみ・印刷なし」と明示する。
- Firebase設定、Realtime Databaseルール、保存先、保存形式、既存シフト・希望・休憩データ、公開判定、発行済みURL、正式URLは変更しない。
- スタッフ通常版・互換版、オーナー通常版・互換版はそれぞれ完全一致を維持する。

### 公開前確認

- 携帯幅でオーナーの印刷プレビューがPC版と同じ配置のまま全体表示されること。
- PC幅の印刷プレビューとA4横印刷の既存寸法・配置が変わらないこと。
- スタッフのミニチュアへ公開済みの日だけが表示され、未公開日が混ざらないこと。
- スタッフ画面に印刷ボタンや `window.print()` の新規導線がないこと。
- JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 73. 2026-09-01対応: 短時間表示・スタッフ休み色・編集者PWA起動（スタッフv1.90／オーナーv1.177）

対象: `shift/index.html`、`shift-staff.html`、`shift/owner.html`、`shift-owner.html`、`shift-owner.webmanifest`

### 対応内容

- 3時間以下の短い勤務ラインは開始・終了時刻を上下へ十分に離し、携帯の縮小表示でも文字同士が重ならないようにする。
- 同じ店舗で重複している勤務区間は表示・公開スナップショット作成前に1本へ整理し、元の保存データ自体は削除・書換えしない。
- スタッフが休みの場合は古い別店舗勤務を公開ミニチュアへ出さず、既存公開スナップショットに残る別店舗線も表示しない。
- スタッフの公開ミニチュアの店舗見出しを、オーナー印刷と同じ淡い店舗カラー・黒文字へ統一する。
- スタッフの「本日のシフト」にある日付を大きく太くし、休みの日は本日のカードとカレンダー日付枠を薄い水色にする。
- 編集者専用URLを開いた端末では、専用トークンを端末内に保存し、インストール済みPWAの `launch=pwa` 起動時だけ復元する。
- 復元した編集者トークンはRealtime Databaseで毎回有効性を確認し、無効・再発行済みの場合は端末内から削除する。
- オーナーPWAの開始URLを正式URL `/shift/owner.html`、対象範囲を `/shift/` へ揃える。Firebaseの管理者・編集者権限、発行済みURL、既存データは変更しない。

### 公開前確認

- 短時間の開始・終了時刻が重ならず、重複区間と休み中の古い別店舗線が表示されないこと。
- スタッフの公開ミニチュアがオーナー印刷と同じ淡い店舗カラーになり、休み表示が薄い水色になること。
- 直接の編集者URLを一度開いた後、`?launch=pwa` から同じ編集者権限を復元できること。
- 無効な保存トークンは削除され、オーナー管理者認証へ誤って権限を拡張しないこと。
- manifest、Service Worker、JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 74. 2026-09-01対応: 売上オーナー管理者の月別売上まとめ印刷

対象: `sales-owner.html`

### 対応内容

- 売上オーナーの全店舗売上履歴にある月選択の直下へ「A4印刷用を開く」と「⭐ 売上まとめスプレッドシートを開く」を追加する。
- 操作のために履歴一覧の最下部まで移動しなくて済むよう、印刷とスプレッドシートのボタンは月選択の直下・履歴一覧の直前へ配置する。
- Google未確認アプリの警告を避けるため、追加のOAuth認証やSheets APIによる自動操作は行わない。
- ボタンは既存スプレッドシートの `A4印刷用` タブを直接開く。携帯ではB1で月を選び、右上の「︙」→「共有とエクスポート」→「印刷」を使用する。
- 印刷操作とスプレッドシート導線はGoogle認証済み管理者だけに表示・許可する。売上オーナー共有者1の画面と、那古野・大須・鉄板・鎌倉の売上入力アプリには追加しない。
- スプレッドシート導線は既存のGoogleスプレッドシート「⭐売上まとめ ⭐」を新しいタブで開き、アプリからスプレッドシートを変更しない。
- Firebase設定、Realtime Databaseルール、店舗専用URL、GAS、各月の売上シートは変更しない。

### 公開前確認

- 管理者画面だけに印刷ボタンとスプレッドシートボタンが表示され、共有者画面では非表示であること。
- 「A4印刷用を開く」で `A4印刷用` タブへ直接移動し、Google未確認アプリの画面が出ないこと。
- アプリが `A4印刷用!B1` や売上データを変更しないこと。
- アプリ独自の印刷表や `window.print()` を使用しないこと。
- JavaScript構文、自動安全検査に合格すること。
- 変更対象が `sales-owner.html` と本引継ぎ文書だけであり、各店舗の `sales-input.html` を変更していないこと。

## 75. 2026-09-02対応: 編集者専用URLを名前付きでLINE共有（オーナーv1.178）

対象: `shift/owner.html`、`shift-owner.html`

### 対応内容

- 編集者専用URLの「店長 URLをコピー」「ヒナキ URLをコピー」「兄 URLをコピー」で、URLだけでなく本人名とURLを2行でコピーする。
- LINEへ貼り付けたときに、1行目へ「店長」「ヒナキ」「兄」、2行目へ各自の既存専用URLが表示されるようにする。
- 通常のコピーと再発行後のコピーの両方へ同じ名前付き形式を適用する。
- 発行済みURL、編集者権限、Firebase設定、Realtime Databaseルール、保存先、既存シフトデータは変更しない。
- オーナー通常版・携帯互換版は完全一致を維持する。

### 公開前確認

- 店長・ヒナキ・兄の各コピーボタンが、それぞれ正しい名前とURLをコピーすること。
- コピー失敗時の手動コピー画面にも名前とURLの両方が表示されること。
- 通常版・互換版の一致、JavaScript構文、自動安全検査に合格すること。

## 76. 2026-09-02対応: 年次棚卸のExcel機能を必要時読込（在庫管理v4.8.3）

対象: `inventory.html`

### 対応内容

- 起動時に読み込んでいたExcel出力ライブラリを、Excel出力・棚卸完了・在庫ゼロ保存の操作時だけ読み込む。
- 同じ操作中に複数回読み込まず、読み込み済みの場合はそのまま再利用する。
- 読み込みに失敗した場合はエラーを表示し、棚卸完了保存または在庫ゼロ処理を中止する。
- Firebase設定、Firestore保存先 `inventory/main`、localStorageキー `zaikoApp_v1`、IndexedDB、既存商品・数量・単価・年度・カテゴリー、管理者／編集者URL、共有URL、画面構造は変更しない。

### 公開前確認

- 通常起動時にExcel外部ライブラリを読み込まないこと。
- Excel出力、年度別Excel出力、棚卸完了時のExcel保存、在庫ゼロ前のExcel保存の全経路で必要時に読み込むこと。
- Excel読込失敗時に在庫データを変更しないこと。
- JavaScript構文、既存保存先・保存キー、バージョン表示、自動安全検査に合格すること。

## 77. 2026-09-02対応: シフト・オーナー起動時の二重描画を削減（オーナーv1.179）

対象: `shift/owner.html`、`shift-owner.html`

### 対応内容

- Google／編集者認証前に画面裏で行っていた月間表の初回描画を停止する。
- 認証後は「シフトを読み込んでいます…」を表示し、Firebaseからmeta・staffの最新データを両方受信した後に月間表を1回描画する。
- 起動後にオーナータブへ戻る操作、月変更、編集後の再描画は従来どおり維持する。
- 印刷・印刷プレビュー・編集履歴・日別編集・Firebase設定・Realtime Databaseルール・保存先・既存シフト・発行済みURL・公開状態は変更しない。
- オーナー通常版・携帯互換版は完全一致を維持する。

### 公開前確認

- 認証前に月間表を描画せず、最新データ受信後に表示すること。
- 認証中の案内が表示され、受信完了後に認証画面が閉じること。
- 通常のオーナータブ操作では月間表が表示されること。
- JavaScript構文、通常版・互換版の一致、自動安全検査に合格すること。

## 78. 2026-09-02対応: 毎日在庫の起動HTML軽量化（毎日在庫v10.67）

- 対象は正式ファイル `index.html` のみ。
- パスコード画面に約47KBのJPEGをHTML内へ直接埋め込んでいた構造を廃止し、承認済み正式アイコン `/icons/daily-inventory-180.png?v=4` を参照する。
- HTML本体の転送量と解析量を減らし、起動開始を軽くする。
- Firebase設定、匿名認証、専用URL、保存先、LocalStorageキー、既存在庫・温度・わらび餅データ、画面機能は変更しない。

## 79. 2026-09-02対応: 売上入力の起動時重複通信を削減（売上入力v2.77）

- 対象は正式ファイル `sales-input.html` のみ。
- 起動直後に同じHTML全体を `no-store` でもう一度取得していた更新確認を停止し、起動時の重複通信を削減する。
- 起動後5分ごとの更新確認と更新バナーは維持する。
- Firebase設定、匿名認証、店舗専用URL、GAS、保存先、入力・日報・履歴機能は変更しない。

## 80. 2026-09-02対応: 売上オーナーの初期描画集約

- 対象は正式ファイル `sales-owner.html` のみ。
- 起動時に4店舗のFirebase受信ごとに行っていた履歴画面の再描画を30msの短い待機で集約し、連続した初期受信では最後に1回だけ描画する。
- 店舗ごとのリアルタイム受信は維持する。
- Firebase設定、Google管理者認証、共有者専用URL、売上データ、印刷、スプレッドシート機能は変更しない。

## 81. 2026-09-02対応: 温度・わらび餅の初期画面生成を遅延（毎日在庫v10.68）

- 対象は正式ファイル `index.html` のみ。
- 毎日在庫本体の表示後、ブラウザの空き時間に温度記録・わらび餅の選択欄と履歴を初期化する。
- `requestIdleCallback` 非対応環境では200ms後、対応環境でも最大800ms以内に必ず初期化する。
- Firebase同期、温度・わらび餅データ、起動後のバックアップ、専用URL、保存先、LocalStorageキーは変更しない。

## 82. 2026-09-02対応: 在庫管理のFirebase初回取得を一本化（在庫管理v4.8.4）

- 対象は正式ファイル `inventory.html` のみ。
- 起動時の `getDoc` 取得後に同じ場所へ `onSnapshot` 監視を開始していた重複取得をやめ、監視の初回受信1回でクラウドとの差分を確認する。
- 端末保存データを先に表示する構造と、クラウドが新しい場合の再描画、他端末のリアルタイム同期は維持する。
- Firebase設定、保存先、LocalStorageキー、既存棚卸データ、年度、4店舗固定URL、管理者・編集者構造は変更しない。

## 83. 2026-09-02対応: スタッフシフトの初期描画を1回に集約（スタッフv1.96）

- 対象は `shift/index.html` と携帯互換版 `shift-staff.html`。両ファイルは完全一致を維持する。
- Firebaseからスタッフ本人データと共通設定が両方揃ってから `shiftApplyRemote` を実行し、受信順による未完成状態の描画と直後の再描画を防ぐ。
- Firebase匿名認証、スタッフ専用URL、既存希望・決定シフト、連絡事項、印刷、日別表示は変更しない。

## 84. 2026-09-02対応: 私の管理のサービスワーカー登録を一本化（私の管理v1.9）

- 対象は正式ファイル `my-management.html` のみ。
- 起動時に共通 `/sw.js` と専用 `/my-management-sw.js` を重複登録していた処理から共通側を外し、専用サービスワーカーだけを登録する。
- 各アプリのカードを押すたびに `_latest` を付けて最新版を開く仕組み、正式URL、カード、アイコン、PWAインストール機能は維持する。

## 85. 2026-09-02対応: 仕入価格管理・翌年度価格引継ぎ（価格管理v1.0.0／在庫管理v4.9.0／私の管理v2.0）

- 新規ファイル `price-management.html` を、既存棚卸とは別の黒テーマの価格管理画面として追加する。
- 既存商品は変更前価格・変更後価格・変更日・仕入先・変更者・備考を確認後に保存し、履歴は `inventoryPriceHistory` へ追加する。過年度の確定タブは変更しない。
- 新商品は店舗・商品名・仕入先・カテゴリー・単位・初回価格・登録日・税率を入力し、同一店舗の同名重複を止めた上で `inventory/main` の商品一覧へ追加する。
- 棚卸画面からの単価直接上書きを止め、価格管理画面へ移動する。前年基準から価格が変わった商品は黒文字を維持し、単価枠を薄い黄色にして「前回→現在」を表示する。
- 翌年度開始では商品情報と最新価格を残し、数量・入力済み状態・更新日時だけをリセットする。年度切替時の価格を次年度比較基準として保持する。
- 管理者は全店舗の在庫金額、全店舗の商品・履歴、新商品、スプレッドシート連携を使用できる。編集者は自店舗だけを表示し、全店舗金額とスプレッドシート操作を表示しない。管理者2名は操作時の表示名を履歴へ記録する。
- 店舗はFirestoreの `stores` を基準に動的表示し、新規店舗は数値店舗IDの編集者URLと `連携_店舗名`・年度確定タブの対象にする。既存4店舗URLは維持する。
- スプレッドシートへ管理者用 `価格マスター` を追加し、新価格・変更日・変更者を入力後、「価格変更を確認・反映」で確認した変更だけを書き戻す。同時更新を検知した場合は中止する。
- 既存の2026年度データ、Firestore `inventory/main` の場所、localStorageキー `zaikoApp_v1`、既存4店舗タブ、過年度タブ、管理者・編集者URL、正式ドメインは変更しない。
- 公開前にJavaScript構文、GAS構文、価格変更・新商品・重複停止・年度更新・黄色表示・管理者／編集者表示・新規店舗・既存保存先無変更を確認し、ユーザーの「公開して」指示までGitHub・Vercelへ反映しない。

## 86. 2026-09-02対応予定: 仕入価格管理の携帯向け配色と専用アイコン（価格管理v1.1.0／私の管理v2.1）

- 仕入価格管理は黒いヘッダーと金色の操作ボタンを維持し、背景・入力欄・集計・商品一覧を明るい色へ変更して携帯での視認性を上げる。
- 携帯幅では横長の表を縦長の商品カードへ切り替え、商品・仕入先・現在価格・状態・変更ボタンを横スクロールなしで操作できるようにする。
- 管理者は携帯・パソコンごとに初回確認後「次回から省略」を選択でき、管理者名も端末ごとに保持する。共有端末では記憶せず、ヘッダーの「記憶解除」で再確認状態へ戻せる。
- 既存在庫アイコンを基にした黒背景の専用アイコンを追加し、棚卸アプリと仕入価格管理を区別する。
- 本人専用の「私の管理」から、スタッフ専用URLが必要な「シフト・スタッフ」カードだけを外す。スタッフアプリ本体、正式URL、専用URL、既存シフトデータは変更しない。
- 価格改定、新商品、履歴、管理者／編集者URL、Firestore保存先、既存棚卸データは変更しない。
- ユーザーの公開指示まではGitHub・Vercelへ反映しない。

## 87. 2026-09-03対応: 価格表示・年度切替・共有導線（在庫v4.9.1／価格管理v1.1.4）

- 仕入価格管理の価格変更画面で「変更後価格」の見出しと入力数字を赤字・太字にし、現在価格との取り違えを防ぐ。
- Apps Scriptへ価格専用の5分間隔トリガーを追加し、アプリで保存した最新価格を「価格マスター」へボタン操作なしで反映する。
- 自動更新前に入力途中の新価格・変更日・変更者・備考を商品ID単位で退避し、価格マスター更新後も保持する。
- 自動更新対象は「価格マスター」だけとし、連携シートと確定年度タブは変更しない。
- Google Apps Scriptの日次実行時間を抑えるため1分ではなく5分間隔とする。
- 在庫アプリの設定から一般スタッフ向け「アプリを共有／LINEで共有する」を削除する。管理者だけに表示する店舗別・編集者URL発行機能は残す。
- 一回限りの年度移行 `migrateLegacy2025To2026` を追加する。元4タブと2025年度4タブの全セル値が一致した場合だけ、2025年度タブを固定保存したまま元4タブを削除する。
- `2026大須`、`2026那古野`、`2026鉄板`、`2026鎌倉` を連携タブから作成し、現行商品・価格を保持して数量0／未完了で開始する。同時にFirestore `inventory/main` の `inventoryYear` を2026、全商品の数量を0・未完了へ更新する。
- Firebase設定、Firestore保存先 `inventory/main`、既存商品・価格履歴、2025年度タブ、管理者／編集者URLは変更しない。
- ユーザーから、2025年度保持、重複4タブ削除、2026年度作成、数量0、公開まで明示承認済み。

## 88. 2026-09-03対応予定: 誤った価格登録の取消（価格管理v1.1.5）

- 前年度価格から変更中の商品に「誤登録を戻す」を表示し、最新の価格変更だけを取り消せるようにする。
- 取消前に店舗・商品名・現在価格・戻る価格を表示し、「取り消して元に戻す」を押した場合だけ実行する。
- 取消はFirestoreトランザクションで、現在価格を変更前価格へ戻す処理と、誤った `inventoryPriceHistory` 1件の削除を同時に行う。片方だけ成功する状態を作らない。
- 現在価格が対象履歴の変更後価格と一致しない場合、またはその後の価格変更がある場合は取消を中止する。
- 今後の価格変更では履歴IDと変更前仕入先も保存し、取り消す際に価格・仕入先・変更表示を元へ戻せるようにする。
- 2025年度タブ、他の商品、新商品履歴、Firebase設定、保存先、管理者／編集者URL、GAS設定は変更しない。
- 現在の大須「奥三河モモ肉」990円の実データ取消は、機能公開とは分けて対象を明示確認してから実行する。
- 公開前にJavaScript構文、取消対象の限定、同時更新停止、携帯表示、変更範囲を確認し、ユーザーの公開指示までGitHub・Vercelへ反映しない。

## 89. 2026-09-03対応: 鎌倉毎日在庫の過去訂正再計算・完了確認整理（毎日在庫v10.69）

- 対象は正式ファイル `index.html` のみ。鎌倉を含む既存店舗専用URL、Firebase設定、保存先、データ形式は変更しない。
- 過去日の販売管理品目の在庫数を訂正した場合、その品目だけを訂正日翌日から最新日まで「前日在庫＋入荷－販売－無料」で再計算する。月をまたぐ履歴も連続して反映し、各日の自動再計算内容を編集履歴へ残す。
- 過去日の編集開始時に保存済みUber Eatsを復元し、Uberを変更しない場合は再入力を求めず既存値を維持する。当日のUber記録も任意とする。
- 完了時、入荷記録が1件も無い場合だけ「本日は入荷がありませんでしたか？」を表示し、「はい、ありません」の承認後は別の「戻って確認」画面へ進まず、そのまま保存完了する。取消時は同じ入力画面に留まる。
- Uber Eatsと冷蔵庫温度は完了必須にせず、記録が無い日は在庫数だけで完了できる。
- 本日の確認・完了カードでは、冷蔵庫温度・わらび餅入荷・Uber Eats・スタンプ引き換えは実記録がある項目だけを表示する。スタンプ引き換えの0件項目は表示しない。
- AM温度記録済みの場合はAM入力欄を隠し、未完了のPM入力だけを表示する。保存済みAMは温度履歴から確認・編集できる。
- 公開前にJavaScript構文、過去日再計算、月跨ぎ、Uber保持、入荷なし確認、0件カード非表示、AM完了後のPM表示を確認する。

## 90. 2026-09-03対応: 釜揚げの箱・単品在庫組み替え（毎日在庫v10.70）

- 毎日在庫へ「箱を崩す（箱−1・単品＋6）」と「箱を作る（単品−6・箱＋1）」を追加する。
- 1箱は単品6個として扱い、組み替えは入荷・販売・無料へ含めず、専用の `conversions` と備考の `【在庫組み替え】` に記録する。
- 箱を作る場合は単品6個未満、箱を崩す場合は箱1箱未満では実行しない。
- 組み替え後の正しい在庫計算は「前日在庫＋入荷＋組み替え増減−販売−無料」とするため、通常の在庫差異として警告しない。
- 過去日の組み替えを編集した場合は、単品と箱の両方を翌日以降へ連続再計算し、各日の編集履歴へ自動再計算を残す。
- 差異理由にも「箱を崩して単品6個に変更」「単品6個を箱1箱に変更」を追加する。
- Firebase設定、保存先、既存在庫、入荷・販売・温度・わらび餅・Uber・スタンプの各機能は変更しない。

## 91. 2026-09-03対応: 組み替え配置と入荷なし最終確認の視認性（毎日在庫v10.71）

- 箱・単品の在庫組み替え欄を販売品目一覧の末尾から、釜揚げ（単品）・釜揚げ（箱）カードの直下へ移動する。
- 組み替え欄は薄いグレー紫と控えめな枠線にし、派手なオレンジ・紫の大ボタンを使用しない。
- 組み替え記録がない場合の文言は表示せず、記録がある場合だけ内容と取消操作を表示する。
- 入荷記録がない日の確認はブラウザ標準confirmを廃止し、暗い背景・大きな「最終確認」見出し・22pxの質問文を持つ専用画面に変更する。
- 専用画面の選択肢は「はい、ありません」と「戻って確認」とし、前者はそのまま完了、後者は入力画面へ戻る。
- 在庫計算、組み替え比率、Firebase設定、保存先、既存データ、その他の完了処理は変更しない。

## 92. 2026-09-03対応: 釜揚げ（箱）カードの識別（毎日在庫v10.72）

- 「釜揚げ（箱）」の商品カード外枠だけを、落ち着いた濃い紫に変更する。
- 「釜揚げ（単品）」、数字の入力欄、箱・単品の在庫組み替え欄の配色は変更しない。
- 在庫差異がある場合は従来の赤い警告枠を優先し、異常を見落とさないようにする。
- 在庫計算、組み替え、Firebase設定、保存先、既存データは変更しない。

## 93. 2026-09-03対応: 本日の記録確認の携帯表示（毎日在庫v10.73）

- 最終確認画面には冷蔵庫温度を表示せず、温度記録そのものと温度履歴は従来どおり保存・表示する。
- 携帯の最終確認画面では「本日の在庫」の見出し、商品名、在庫数、Uber表示を大きくする。
- 商品一覧を携帯では1列にし、商品ごとの上下間隔とカード内余白を広げ、押し詰まって見えないようにする。
- わらび餅入荷・スタンプ引き換えは記録がある場合だけ従来どおり表示する。
- v10.72で「釜揚げ（箱）」の商品カードへ付けた濃い紫枠を外し、正しい対象である「箱・単品の在庫組み替え」欄の外枠を濃い紫にする。欄内の落ち着いた配色は維持する。
- 在庫計算、保存内容、Firebase設定、保存先、既存データは変更しない。

## 94. 2026-09-03対応: 入荷・ポイントカード・本日の在庫の表示順（毎日在庫v10.74）

- 記録がある場合だけ、わらび餅入荷とポイントカード引き換えを「本日の在庫」より上に表示する。
- 表示順は「入荷記録 → ポイントカード引き換え → 本日の在庫」とする。
- 携帯では入荷記録を少し大きくし、ポイントカード引き換えは以前の大きさを維持する。
- 大きな1列表示は「本日の在庫」カードだけに限定し、ポイントカードへ適用しない。
- 在庫・入荷・ポイントカードの記録内容、Firebase設定、保存先、既存データは変更しない。

## 95. 2026-09-04対応: 売上オーナーの携帯向けA4横1枚PDF

- 対象は正式ファイル `sales-owner.html` のみ。
- 管理者の印刷欄を「① 印刷月を選ぶ」「② A4横・1枚PDFを開く」の2段階に整理する。
- ①は従来どおり既存スプレッドシートの `A4印刷用` タブを開き、B1で印刷月を選ぶ。
- ②は `A4印刷用` の `A1:U39` だけを、A4横・用紙に合わせる・余計なシート名／ページ番号／グリッド線なしのPDFとして直接開く。
- Googleスプレッドシート画面そのものをEPSONへ渡さないため、空白範囲を含む大量ページとWeb画面印刷エラーを避ける。
- 追加のOAuth権限やSheets APIによる書き込みは行わず、`A4印刷用!B1`、売上データ、Firebase設定、共有者画面、各店舗の売上入力アプリは変更しない。

## 96. 2026-09-04対応予定: 売上オーナー内で完結する携帯印刷

- 対象は正式ファイル `sales-owner.html` のみ。
- 携帯印刷ではGoogleスプレッドシートを開かず、売上オーナーで選択中の年月とFirebaseから読み込んだ既存売上を使って印刷表を生成する。
- 印刷表は既存の21列順、店舗別の淡い色、1～7日・8～14日・15～21日・22～28日・29日～月末の週間売上、月合計を維持する。
- `@page` でA4横・5mm余白を指定し、印刷時は生成した表だけを表示する。Google Sheets全タブ由来の108ページを印刷対象に含めない。
- 「選択中の月をA4横1枚で印刷」1操作にし、Googleログイン、月選択後に売上オーナーへ戻る操作、Sheets API権限を不要にする。
- スプレッドシートを開く管理用リンクは維持する。売上データ、Firebase設定、保存先、共有者画面、各店舗の売上入力アプリは変更しない。

## 97. 2026-09-04対応: 売上オーナー独自印刷の取消

- `v96` で追加したFirebase売上データから独自に組み立てる印刷表は、正式な `A4印刷用` シートと内容・計算・表示が一致しないため取り消す。
- `sales-owner.html` は、公開コミット `156ff91eb37713347b387c23755115f945c2b6b1` の正式印刷状態へファイル単位で完全復元する。
- 印刷欄は「① 印刷月を選ぶ」と「② A4横・1枚PDFを開く」に戻し、②は既存 `A4印刷用` タブの `A1:U39` をそのままPDF出力する。
- 正式な21列の内容、計算式、週次売上、月合計、店舗色をアプリ側で再構築しない。
- 売上データ、Firebase設定、保存先、スプレッドシート、共有者画面、各店舗の売上入力アプリは変更しない。

## 98. 2026-09-04対応: 売上オーナーの印刷導線を1ボタンへ簡略化

- 対象は正式ファイル `sales-owner.html` のみ。
- 管理者の印刷欄は「⭐ 売上まとめスプレッドシートを開く」1ボタンだけにする。
- スプレッドシートの `A4印刷用` でB1の月を選び、携帯の「共有とエクスポート」→「印刷」から必要な1ページを選んで印刷する。
- アプリ独自の印刷表、PDF直接出力、Sheets APIによる月の自動変更は使用しない。
- 正式な `A4印刷用` の21列、数式、週次売上、月合計、店舗カラーは変更しない。
- 売上データ、Firebase設定、保存先、スプレッドシート、共有者画面、各店舗の売上入力アプリは変更しない。

## 99. 2026-09-04対応予定: シフト・オーナーのログイン再発防止（オーナーv1.180）

- 対象は正式ファイル `shift/owner.html` と携帯互換版 `shift-owner.html`。両ファイルは完全一致を維持する。
- 編集者専用URLで使用したFirebase匿名認証が同じ端末に残っていても、通常のオーナーURLでは匿名状態を自動解除し、Google管理者ログインへ戻す。
- 携帯、LINE内ブラウザ、ホーム画面アプリでは、ブロックされやすいポップアップ方式を使わずGoogleのリダイレクト認証を使用する。パソコンは従来のポップアップを維持し、失敗時はリダイレクトへ切り替える。
- 一時的な通信・読込エラーを「オーナー権限なし」と誤表示せず、同じアカウントのまま再確認できるボタンを残す。本当に管理者登録が無い場合も操作不能にはしない。
- GitHub Actionsへ、匿名認証の自動解除、再確認導線、通信エラー分離が失われた場合に公開を止める検査を追加する。
- 既存Google管理者アカウント、Firebase設定・ルール・保存先、シフトデータ、編集者専用URL、スタッフ画面、印刷、「私の管理」の他アプリは変更しない。
- ユーザーの「公開して」指示まではGitHub・Vercelへ反映しない。
