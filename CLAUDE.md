# Claude Code 作業指示 — Zaikon

このリポジトリは運用中の業務アプリです。作業開始前に、必ずルートの `OFFICIAL-STRUCTURE.md` を全文読み、正式構成・URL・互換用URL・禁止事項を確認してください。

## 最優先ルール

1. ユーザーが明示した対象ファイル以外は変更しない。
2. 在庫・売上・シフトを一つのアプリや一つの画面へ統合しない。
3. スタッフ用とオーナー用を分離する。スタッフ用画面からオーナー機能へ入れる状態を作らない。
4. 正式ファイル名、通常利用URL、LINE共有URLを勝手に変更しない。
5. `owner.html`、`shift/index.html`、`shift/owner.html` などの互換用ファイルを、確認なしに削除・改名しない。
6. LocalStorage、IndexedDB、Cookie、Firebaseのパス・キー・設定を、影響確認なしに変更または削除しない。
7. ブラウザ保存データの削除を安易に案内しない。
8. `zaiko-app`、Vercel連携、ChatGPT Codex Connectorを削除・改名・停止しない。
9. 別リポジトリへ同名の正式版を作らない。
10. 不明点や正式文書との矛盾を見つけた場合は、推測で直さず、変更前にユーザーへ報告する。

## 作業前

- `OFFICIAL-STRUCTURE.md` を読む。
- `git status`、現在のブランチ、対象ファイル、差分を確認する。
- 対象アプリの利用者を確認する。
- 通常利用URLとLINE共有URLへの影響を確認する。
- 保存データ、保存キー、Firebaseへの影響を確認する。
- 変更前のコミットSHAを復元点として記録する。

## 作業中

- 必要最小限の差分にする。
- ファイル全体の置換や、複数アプリへの一括変更を避ける。
- URL、保存キー、認証・共有処理の変更は、依頼範囲に明記されている場合だけ行う。
- 既存のユーザー変更を上書きしない。
- 破壊的なGit操作を行わない。

## 作業後

- 対象ファイル以外が変更されていないことを確認する。
- 正式URLと互換URLを確認する。
- スタッフ画面とオーナー画面が分離されていることを確認する。
- LINE共有ボタンが意図したURLを生成することを確認する。
- 保存データへの互換性を確認する。
- Vercelデプロイ成功を確認する。
- 変更ファイル、変更理由、検証結果、復元用コミットSHAを日本語で報告する。

## 現在確認されている要確認事項

次の事項は、ユーザーの明示的な修正依頼なしに変更しない。見つけた場合は影響を説明して確認を取る。

- `shift-staff.html` にオーナー画面への切替機能が残っている。
- `OFFICIAL-STRUCTURE.md` の「既存LINE共有URL」と、現在の一部アプリ内の共有先に差異がある。
- 動作中URLを先に変更せず、必要なら正式文書の記載、互換性、移行方法を整理してから修正する。

## Claude Codeへ依頼するときの解釈

「このアプリを修正して」という依頼は、そのアプリだけを変更する許可です。他のアプリ、正式URL、LINE共有先、保存データまで変更する許可ではありません。

ユーザーが「まとめる」と表現した場合でも、ソース・画面・利用者データを統合してはいけません。「私の管理」のような本人専用の入口から、独立した各アプリを開ける構成を維持してください。
