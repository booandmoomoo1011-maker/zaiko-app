# Zaikon 在庫スプレッドシート安全連携

この手順は、既存の「大須」「那古野」「鉄板」「鎌倉」シートと現在入力されている数字を変更せず、Zaikon在庫管理のFirestoreデータを別シートで確認し、西暦年度ごとに保存するためのものです。

## 安全設計

- 既存4シートは読み取り専用として扱います。
- 新しく作成する `連携_大須`、`連携_那古野`、`連携_鉄板`、`連携_鎌倉` だけを同期で更新します。
- `Zaikon連携設定` シートに同期状態を記録します。
- スプレッドシートからFirestoreへ書き戻す処理はありません。
- 在庫アプリで入力した西暦を使用し、`2026大須` のような年度確定シートを登録店舗分作成します。新規店舗も自動的に対象になります。
- 同じ年度の保存シートが1つでも存在する場合は、過年度データを守るため全店舗とも上書きせず中止します。
- 在庫アプリの `active:false`（この店舗では使用しない商品）は連携シートへ出力しません。
- 初回同期だけ、元シートの「保管場所」「8%／10%」「備考」を見出し名と商品名で照合して連携シートへ参照コピーします。2回目以降は連携シート側の変更を保持し、前年の税率・備考が勝手に復活しないようにします。元シート側は変更しません。

## 初回設定

1. 対象スプレッドシートで「拡張機能」→「Apps Script」を開きます。
2. `コード.gs` にある初期コードを削除し、`inventory-spreadsheet-sync.gs` の全文を貼り付けます。
3. 保存ボタンを押します。
4. 上部の関数選択で `setupZaikonSafeSync` を選び、「実行」を押します。
5. 初回だけGoogleの権限確認が出るため、このスプレッドシートを使用しているGoogleアカウントで許可します。
6. スプレッドシートを再読み込みします。
7. 上部メニューの「Zaikon連携」→「② 連携シートへ安全同期」を実行します。

初回同期後も、元の4シートの数字が変わっていないことを確認してください。確認できるまでは自動同期を開始しません。

## 税率について

在庫アプリには現在、商品の税率項目がありません。そのため、元シートの棚卸額8%または10%の入力状況から税率を判定します。どちらにも数字がない商品は、連携シートの税率と棚卸額を空欄にします。

## 現在の2025年3月データを保存する

1. 最新のApps Scriptへ貼り替えてスプレッドシートを再読み込みします。
2. 「Zaikon連携」→「④ 元4シートを年度名でコピー保存」を押します。
3. 西暦に `2025` と入力します。
4. `2025大須`、`2025那古野`、`2025鉄板`、`2025鎌倉` が作成されたことを確認します。

元の「大須」「那古野」「鉄板」「鎌倉」は変更も改名もしません。作成した2025年タブは固定値で、その後は自動更新されません。同名タブが1つでも存在する場合は4店舗とも作成せず、上書きしません。

## 年1回の棚卸手順

1. 在庫アプリで棚卸年を西暦4桁（例: `2026`）で入力します。
2. 各店舗で棚卸数を入力します。
3. スプレッドシートの「Zaikon連携」→「② 連携シートへ安全同期」を実行します。
4. 連携4シートの数量・単価・税率・棚卸額を確認します。
5. 「Zaikon連携」→「③ 入力した年度で4店舗を保存」を実行します。
6. `2026大須`、`2026那古野`、`2026鉄板`、`2026鎌倉` が作成されたことを確認します。
7. 確定保存後、連携シートは翌年度用になります。商品ID・物品名・カテゴリ／仕入先・保管場所・単位・最新単価を残し、数量・税率・棚卸額・更新日・備考・状態は空欄になります。
8. 在庫アプリの管理者設定で「翌年度を開始（数量だけリセット）」を押します。管理者パスワードと最終確認後、最新価格を残してアプリは自動で `2027` 年度へ進みます。
9. 次年度に必要なときだけ「② 連携シートへ安全同期」を実行します。

1時間ごとの自動同期は使用しません。以前開始している場合は「Zaikon連携」→「自動同期を停止（年1回運用）」を押します。必要なときだけ「② 連携シートへ安全同期」で更新します。

年度保存シートは作成時点の値を固定保存し、その後は自動更新しません。

`2027大須`などの確定タブは2026年度の確定時には先に作りません。2027年度中は「連携_」4シートを作業用として使用し、2027年度の棚卸が確定した時点で初めて `2027大須` などを固定作成します。

アプリの西暦入力欄を手で変更しただけでは、単価や数量は消えません。誤入力による全件消去を防ぐため、数量のリセットは管理者の「翌年度を開始」操作でだけ実行します。

## 価格マスターから変更する

価格マスターとスプレッドシート連携は管理者だけが使用します。編集者画面にはスプレッドシート操作を表示しません。Googleスプレッドシート自体の共有権限も、管理者として運用するアカウントだけに設定します。

1. 「Zaikon連携」→「価格マスターを更新」を実行します。
2. 薄黄色の「新価格」「価格変更日」「変更者」「備考」を入力します。
3. 「Zaikon連携」→「価格変更を確認・反映」を実行します。
4. 変更前後の価格を確認し、「はい」を選んだ変更だけを棚卸へ反映します。
5. 反映した内容は価格改定履歴へ追加され、過年度の確定タブは変更されません。

同時にアプリ側の価格が変わっていた場合は処理を中止し、価格マスターを更新してからやり直します。

## 元シートへ反映する場合

この段階では行いません。商品名の重複、保管場所、税率、備考、既存の数量を照合してから、別の承認済み手順として実装します。
